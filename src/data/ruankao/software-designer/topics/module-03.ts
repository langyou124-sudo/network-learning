import { Topic } from '@/types';

export const topics: Topic[] = [
  {
    id: 'sd-03-01',
    moduleId: 'sd-data-structures',
    title: '线性表',
    description: '顺序表与链表的存储结构、插入删除复杂度、栈队列与串，软考上午题高频基础',
    content: `# 线性表

## 考情分析

- **分值归属**：数据结构是软件设计师上午题的稳定大户，线性表单独命题约 1–2 分，常与算法复杂度、栈队列应用题交叉出现。
- **题型**：几乎全是选择题。考查形式为：给操作求时间复杂度、比较顺序表/链表优劣、给场景选存储结构、栈队列进出序列判断。
- **考频**：★★★。属于「送分但易丢」的基础题——概念不难，但细节混淆点多（头指针/头结点、插入删除指针改序、循环队列空满判断）。

## 一、核心概念

**线性表**是 n 个数据元素的有限序列，逻辑特征是：存在唯一的第一元素和最后元素，除首尾外每个元素有唯一前驱和唯一后驱。线性表只描述逻辑关系，具体怎么存进内存由**存储结构**决定。

**顺序表**：用一段地址连续的存储单元依次存放元素（典型是一维数组）。逻辑相邻则物理相邻，可实现**随机存取**——已知首地址和元素大小，下标定位是 O(1)。
优点：存储密度为 1（无需额外指针），可随机存取，缓存友好。
缺点：插入删除需要成片移动元素；表长需预先估计，过大浪费、过小溢出；静态数组扩容代价高。

**链表**：结点 = 数据域 + 指针域，靠指针把逻辑相邻的结点串起来，物理位置任意。
- **单链表**：每个结点一个后继指针。找第 i 个元素必须从头走，O(n)；已知某结点指针后在其后插入是 O(1)，在其前插入若无前驱指针则仍要 O(n)。
- **双向链表**：增加 prior 指针，可双向遍历，删除已知结点本身是 O(1)（仍要自己改四个指针方向）。
- **循环链表**：尾结点的 next 指向头，适合环形扫描（约瑟夫环、约时轮转）。
- **头结点**：在首元结点前附加的空结点，好处是首元结点的插入删除与其他结点统一，无需特判空表；**头指针**指向头结点（或链表第一个结点），是链表的「入口」。注意：头结点不是数据元素，不算表长。

**栈**：仅在表尾（栈顶）插入删除的受限线性表，后进先出（LIFO）。运算：push / pop / getTop。顺序栈的栈满条件常为 top == MaxSize-1，下溢指对空栈 pop。
**队列**：表尾插入、表头删除，先进先出（FIFO）。**循环队列**用取模把数组首尾相接，空队列与满队列的区分是高频点：
- 牺牲一个单元法：空为 front == rear，满为 (rear+1) % M == front，队列长度 = (rear - front + M) % M。
- 计数器法：另设 count，空为 count==0，满为 count==M。
**双端队列**可在两端进出；输出受限、输入受限双端队列常用于「合法输出序列」判断题。

**串**：元素为字符的线性表。重点是模式匹配：朴素匹配最坏 O(nm)；KMP 利用 next 数组跳过已匹配前缀，把复杂度降到 O(n+m)。next 数组含义是「模式串前 j-1 个字符中最长相等前后缀长度」。串的基本运算包括赋值、比较、求长、连接、求子串、定位（模式匹配）。串比较按字典序从前向后逐字符比，先比 ASCII/编码值，前缀相同则短者小。空串长度为 0，空格串不是空串。子串是串中任意个连续字符组成的序列，位置从 1 开始计数是教材习惯，编程里常从 0 开始，答题看清题干约定。

**广义表**：线性表的推广，元素可以是原子或子表。表头 head 可以是原子或表，表尾 tail 一定是表。例如 LS=(a,(b,c))，head= a，tail= ((b,c))。长度看顶层元素个数，深度看括号嵌套层数。

## 二、关键原理/公式/机制

### 1. 基本操作时间复杂度（长度 n）

| 操作 | 顺序表 | 单链表 |
|------|--------|--------|
| 按位查找 | O(1) | O(n) |
| 按值查找 | O(n) | O(n) |
| 插入（第 i 个位置） | 平均移动 n/2，O(n) | 找位置 O(n)+改指针 O(1)，整体 O(n) |
| 删除第 i 个 | 平均移动 (n-1)/2，O(n) | 找+释放，O(n) |
| 建表 | O(n) | 头插 O(n)、尾插 O(n) |

结论：链表并不让「按位插入删除」变快，因为瓶颈在查找；链表赢在**不用成片搬移**、**长度灵活**。若程序频繁随机存取，用顺序表；频繁在已知位置中间插删、长度不定，用链表。

从移动次数看更直观：顺序表插入第 1 个元素要搬 n 个，插入第 n+1 个不用搬；删除同理。链表无论位置都只改常数个指针，但「找到那个位置」本身是 O(n)。所以若程序中已经持有了结点指针（例如迭代器当前位置），链表的插删才是真正的 O(1)；若每次都从下标或从头定位，链表并不比顺序表快。这一结论在选择题「链表插入删除时间复杂度是 O(1)」的表述上要打叉——必须区分「已知指针」与「按位」两种前提。空间上，单链表每结点附加一个指针域，双向链表附加两个；元素本身很小（如只有 int）时指针开销占比更高，存储密度下降更明显。

### 2. 顺序表地址计算

LOC(ai) = LOC(a0) + i × size。注意下标从 0 开始时第 i 个元素对应下标 i-1。

### 3. 栈与队列的应用机制

- 递归进退、表达式求值、括号匹配、进制转换、迷宫回溯 → **栈**。
- 作业调度、缓冲区、广度优先搜索、消息队列 → **队列**。
- 中缀转后缀：运算符栈；扫描时数字直接输出，左括号入栈，右括号弹栈至左括号，当前运算符优先级不高于栈顶则先弹出再入栈。
- 后缀表达式求值：操作数进栈，遇运算符弹两数（先弹的是右操作数）计算再入栈。

### 4. 循环队列关键式

设容量 M（牺牲一格时可用 M-1 个位置）：
- 进队：rear = (rear+1) % M，再赋值
- 出队：front = (front+1) % M
- 元素个数 = (rear - front + M) % M
- 满： (rear+1)%M == front（牺牲一格法）

### 5. KMP 要义

主串指针 i 一般不回溯。失配时模式串指针 j 回退到 next[j]。next 数组只与模式串自身有关。改进的 nextval 在 next 基础上，若 p[next[j]]==p[j] 则继续前跳，避免重复比较。

## 三、常考题型与解题方法

**题型 1：复杂度比较与存储结构选择**
方法：先看是否需要随机存取。需要 → 顺序表。再看插入删除是否频繁且已知结点位置 → 链表。空间紧张 → 顺序表存储密度高。

**题型 2：链表指针操作「改链顺序」**
方法：画结点框，口诀「先接后断」。在 s 插入到 p 之后：s->next = p->next; p->next = s。删除 p 的后继 q：p->next = q->next; free(q)。顺序反了会丢链。

**题型 3：栈输出序列合法性**
方法：模拟入栈出栈。入栈序号递增进栈，出栈序列只要符合 LIFO 可行性即可。判定口诀：出栈元素后面不得出现「比它后进且比它小却还在栈中压着」的非法交错。最可靠是手模拟。

**题型 4：循环队列空满与长度**
方法：看题干有没有「牺牲一格」「计数器」提示。默认牺牲一格法套公式，不要凭感觉用 rear==front 判断满。

**题型 5：串匹配次数估算**
方法：朴素匹配最坏比较次数 = (n-m+1)×m 量级；KMP 最坏 O(n+m)。选择题直接选复杂度即可，next 数组一般只考含义不考手算。

**题型 6：广义表 head/tail 求值**
方法：从最外层括号数顶层元素个数得长度；从最内层嵌套看深度。head 取第一个元素（原子或子表），tail 一定去掉第一个元素后**剩余部分组成的新表**。例如 L=(a,b,(c,d))，head(L)=a，tail(L)=(b,(c,d))，不要写成 b。若对 tail 结果再取 head，才得到 b。

**解题总习惯**：线性表的题先画结构（数组下标或结点框图），再套公式；能用「平均移动 n/2」这类现成结论就不要从头加和。

## 四、典型例题

**例1**（顺序表删除）长度为 100 的顺序表，删除任一元素的概率相同，问删除时平均移动多少个元素？

解：删除第 i 个元素需移动 n-i 个。等概率下平均 = (0+1+…+99)/100 = 49.5。同理插入平均移动 50 个。答案选平均约 50（或 49.5）。考点：记住插入平均 n/2、删除平均 (n-1)/2，不必每次重推。

**例2**（栈序列）元素 1,2,3,4 依次进栈（进出可交错），下列哪一个不可能是出栈序列？
A. 1 2 3 4　B. 4 3 2 1　C. 1 4 3 2　D. 3 1 4 2

解：手模拟。A：进1出1进2出2…可行。B：全部压完再弹，可行。C：出1 后 2,3,4 进，弹4弹3弹2，可行。D：想先出3，则 1,2,3 进后出3，栈内剩余 2,1，下一出必须是 2 或更后的…无法在 2 还压着时先出 1 再出 4——出 1 前 2 必须已出，与序列 3 1 矛盾。故 D 不可能。

**例3**（循环队列）容量 M=8 的循环队列（牺牲一格），front=5，rear=2，队中元素个数是？

解：count = (2 - 5 + 8) % 8 = 5。画个圆环数一数也能验证。注意不要写成 rear-front 负数忘了加 M。

## 五、易错点

1. **头结点与头指针混淆**：头结点可有可无，头指针必有；带头结点的空表是 head->next == NULL，不是 head == NULL。
2. **链表插入写成先断后接**，导致丢后继，正确顺序永远是先让新结点接住后面，再让前面接住新结点。
3. **顺序表删除后元素个数与表长**：删除后后面元素前移，表长减 1；静态数组容量不变。
4. **循环队列满判定**：在牺牲一格法下 rear==front 是**空**不是满；漏掉取模导致负下标。
5. **栈的下溢与上溢**：空栈 pop 是下溢（错误），满栈 push 是上溢。
6. **广义表 tail 一定是表**：tail((a,b)) 得 (b) 而不是 b；head 才可能得到原子。
7. **把队列当双向通道用**：队列只能一端进一端出；要两端都进出用双端队列。
8. **空串与空格串**：长度为 0 的串才是空串；含空格的串长度不为 0，求子串个数时按 n(n+1)/2+1（含空串）的公式口径要看清是否计空串。
9. **链表「按位插入是 O(1)」**：未说明已知指针时是错误表述，正确整体复杂度是 O(n)。
10. **循环队列长度公式漏加 M**：rear 小于 front 时 (rear-front) 为负，必须写成 (rear-front+M)%M。

## 六、考点提示

- 顺序表 vs 链表的对比表必须能默写，年年换皮考。
- 循环队列空满条件、长度公式是计算题最爱，务必带公式不靠感觉。
- 栈的出栈序列合法性用模拟法，不要背「结论」。
- KNP 只考「i 不回溯、j 回到 next[j]」的思想与复杂度 O(n+m)，别在 next 手算上钻牛角尖。
- 存储密度：顺序表 1，单链表小于 1；随机存取：仅顺序表具备。
- 本课题与「算法复杂度」联合命题概率高，建议连着 O(1)/O(n)/O(n²) 的含义一并复习。
- 广义表 head/tail 与深度长度计算属小而确定的送分点，五分钟就能掌握，不要放弃。
- 顺序表插入/删除平均移动次数（插入约 n/2、删除约 (n-1)/2）建议直接记结论，考场上重推容易算错下标。
- 栈与队列的应用场景归类：递归、括号匹配、表达式→栈；作业调度、缓冲、BFS→队列。选项常拿场景描述来问用哪种结构。`,
    quizzes: [
      {
        id: 'sd-quiz-03-01a',
        type: 'choice',
        question: '在长度为 n 的顺序表中，等概率情况下插入一个元素平均需要移动多少个元素？',
        options: ['n', 'n/2', '(n-1)/2', 'n+1'],
        answer: 'B',
        explanation: '插入第 i 个位置需移动 n-i+1 个元素，等概率平均为 (n+1)/2，通常近似说 n/2。删除才是 (n-1)/2。'
      },
      {
        id: 'sd-quiz-03-01b',
        type: 'choice',
        question: '单链表中，在已知指针 p 所指结点之后插入新结点 s，正确的操作序列是？',
        options: [
          'p->next = s; s->next = p->next',
          's->next = p->next; p->next = s',
          's->next = p; p->next = s->next',
          'p->next = s->next; s->next = p'
        ],
        answer: 'B',
        explanation: '先接后断：先把 s 的 next 接到 p 的后继，再让 p 的 next 指向 s，否则会丢失原后继链。'
      },
      {
        id: 'sd-quiz-03-01c',
        type: 'choice',
        question: '容量为 M 的循环队列采用「牺牲一个存储单元」的方法，则队列为空的判定条件是？',
        options: ['front == rear', '(rear+1)%M == front', '(front+1)%M == rear', 'rear == 0'],
        answer: 'A',
        explanation: '牺牲一格法中空为 front==rear，满为 (rear+1)%M==front，队列长度为 (rear-front+M)%M。'
      },
      {
        id: 'sd-quiz-03-01d',
        type: 'choice',
        question: '关于顺序表和单链表，下列叙述正确的是？',
        options: [
          '单链表按位插入比顺序表快，因为不需要移动元素',
          '顺序表和单链表都可以随机存取',
          '顺序表存储密度更高，适合频繁随机访问',
          '单链表每个结点的存储密度为 1'
        ],
        answer: 'C',
        explanation: '顺序表存储密度为 1 且支持 O(1) 随机存取。链表插入仍需 O(n) 找位置，整体并不更快；链表因指针域存储密度小于 1。'
      }
    ],
    references: [],
  },
  {
    id: 'sd-03-02',
    moduleId: 'sd-data-structures',
    title: '树与二叉树',
    description: '二叉树性质与遍历、线索树、哈夫曼树、二叉排序树与平衡树，下午数据结构大题基石',
    content: `# 树与二叉树

## 考情分析

- **分值归属**：上午题约 2–3 分（性质计算、遍历结果、哈夫曼带权路径长度）；下午数据结构大题若考树，多为二叉排序树、哈夫曼编码或遍历填空。
- **题型**：选择题为主（给遍历序列反推、结点数与深度关系、WPL 计算）；下午题考算法填空与复杂度。
- **考频**：★★★★。二叉树性质与遍历几乎年年出现，哈夫曼与 BST 是「会了就稳」的得分点。

## 一、核心概念

**树**是 n 个结点的有限集合（n≥1 时有且仅有一个根，其余结点可分为 m 个互不相交的有限集，每个又是一棵树）。术语：度（结点子树数）、叶子（度为 0）、树的度（各结点度的最大值）、深度/高度、有序树、森林。树适合表示具有层次与嵌套关系的数据，如文件系统、组织结构、表达式、HTML 文档结构。比较二叉树时，树的结点数 n 与边数关系为 n−1 条边，因为除根外每个结点恰有一条指向双亲的边，这一性质在推导 n0=n2+1 时会被反复用到。

**二叉树**：每个结点最多两棵子树，且子树有左右之分、次序不可颠倒。特殊形态：满二叉树（每层都满）、完全二叉树（除最后一层外每层满，最后一层结点连续靠左）。

**二叉树性质（必背）**：
1. 第 i 层最多有 2^(i-1) 个结点（i≥1）。
2. 深度为 k 的二叉树最多有 2^k − 1 个结点；满二叉树刚好取到最大值。
3. 任意二叉树：叶子数 n0 = 度为 2 的结点数 n2 + 1，即 **n0 = n2 + 1**。
4. 具有 n 个结点的完全二叉树深度为 floor(log2 n) + 1。
5. 完全二叉树按层序从 1 编号：双亲是 floor(i/2)；左孩子 2i（≤n 才有）；右孩子 2i+1（≤n 才有）。

**遍历**：把非线性结构线性化。
- 先序（根左右）、中序（左根右）、后序（左右根）；层序用队列。
- 由**先序+中序**或**后序+中序**可唯一确定二叉树；**先序+后序**不行（无法区分左右子树）。

**线索二叉树**：利用空指针存放遍历前驱/后继线索，加快遍历。中序线索树中最左结点无前驱，最右结点无后继。

**哈夫曼树（最优二叉树）**：带权路径长度 WPL = Σ(叶子权值 × 根到该叶子路径长度) 最小的树。构造：每次选权值最小的两棵树合并为新树，新根权为两者之和，重复至单树。没有度为 1 的结点；权大的叶子离根近。**哈夫曼编码**用从根到叶子的 0/1 路径作编码，是前缀码，且平均码长最短。

**二叉排序树（BST）**：左子树所有关键字小于根，右子树所有关键字大于根，左右子树亦为 BST。中序遍历 BST 得递增序列。查找效率与树形有关：最好 O(log n)，最坏（退化成单支）O(n)。

**平衡二叉树（AVL）**：任意结点左右子树高度差的绝对值 ≤ 1。插入导致失衡时用 LL、RR、LR、RL 旋转调整，保证查找 O(log n)。B 树/B+ 树是多路平衡查找树，用于文件系统与数据库索引：B+ 树叶子结点用链表相连，适合范围查询。

**树的存储**：双亲表示法（求双亲易、求孩子难）、孩子表示法、孩子兄弟表示法（二叉链表，可把树/森林转成二叉树：左指针指向第一个孩子，右指针指向下一个兄弟）。森林转二叉树时先把每棵树按孩子兄弟规则转好，再把各树根看作兄弟用右链串起来。反向转换同理拆右链。这一转换是「树的问题化归为二叉树问题」的理论依据，也让遍历算法只需实现一套。

## 二、关键原理/公式/机制

### 1. 结点数关系式

设 n 为总结点数，n0/n1/n2 分别为度 0/1/2 的结点数：
- n = n0 + n1 + n2
- 从孩子角度数边：n = n1 + 2×n2 + 1（除根外每结点一条入边），相减得 **n0 = n2 + 1**。

这一公式是计算题的万能钥匙：给叶子数求 n2，或给总结点判断形态。

### 2. 完全二叉树编号导航

数组从下标 1 存：结点 i 的双亲 floor(i/2)，左 2i，右 2i+1。若从 0 存则双亲 floor((i-1)/2)。考试默认从 1 编号较多，读题看清。

### 3. 遍历递归本质

- 先序：进入结点就访问，再左后右。
- 中序：先走完左子树再访问根，再右。
- 后序：左右都走完才访问根。求表达式树的值、释放结点用后序。

**由遍历反推树**：先/后序定根，中序分左右。例：先序 ABDECF，中序 DBEACF → 根 A，中序左 DBE 右 CF；递归拆解。层序序列可辅助验证。

### 4. 哈夫曼 WPL 计算

构造完成后按「叶子权 × 深度」累加。等权 n 个叶子时，哈夫曼树近似满树，WPL 最小；若把顺序合并（相邻两两）当哈夫曼，WPL 会偏大。

哈夫曼编码长度 = 路径上 0/1 个数 = 叶子深度。电文总长 = Σ(字符频率 × 码长)。

### 5. BST 查找/插入/删除

- 查找：与根比较，小走左、大走右，直到空或命中。
- 插入：按查找路径落到空位安放。
- 删除：叶子直接删；单支接上唯一孩子；双支用**中序前驱（左子树最大）或中序后继（右子树最小）**顶替再删该顶替结点。

### 6. 平衡旋转（了解即可）

LL 型右单旋，RR 型左单旋，LR 型先左后右，RL 型先右后左。选择题会问「插入后树形属于哪种、要做什么旋转」。

### 7. 遍历的应用场景对应

- 先序：复制一棵树、输出目录树前序、表达式前缀（波兰式）。
- 中序：BST 有序输出、表达式中缀（要括号处理优先级）。
- 后序：释放结点、求表达式树的值、计算目录占用空间（先孩子后自己）。
- 层序：按层打印、求树的宽度、最短层数路径。表达式树中叶子是操作数、内部结点是运算符，后序求值即「左右根」式计算。

## 三、常考题型与解题方法

**题型 1：由 n0 = n2+1 推结点数**
方法：列 n = n0+n1+n2 与 n0=n2+1，再代入题目给的深度约束（深度 k 最多 2^k−1）排除。求「最多/最少叶子」优先考虑完全二叉树与单支树两个极端。

**题型 2：遍历序列还原二叉树**
方法：先/后序找根 → 中序画左右区间 → 递归。写在草稿上三层就出来了。给出「先序+后序」问是否唯一，答案一般是**不唯一**。

**题型 3：WPL / 哈夫曼编码**
方法：严格小顶堆式两两合并，合并后的新权值要放回再参与比较。编码题先画树再写 0/1，检查是不是前缀码（任一编码不能是另一编码的前缀）。

**题型 4：BST 操作后的形态**
方法：按 BST 定义重画插入过程。删除双支结点时先明确题目要求用前驱还是后继（默认后继较多）。

**题型 5：完全二叉树深度与孩子编号**
方法：套深度公式 floor(log2 n)+1；编号题直接公式，不画图。

**题型 6：判断树形态与叶子最值**
方法：极端形态优先——满二叉树/完全二叉树叶子最多或结点最多，单支树（每层 1 个结点）结点最少。深度为 k 的二叉树：结点最少 k 个，最多 2^k−1 个。求「叶子最少」在给定 n0=n2+1 的约束下尽量少用度为 2 的结点即可。

**题型 7：遍历应用辨识**
方法：题目出现「求表达式值、释放结点」多为后序；「有序输出」多为中序（且是 BST）；「按层输出」为层序。前缀后缀表达式题用表达式树转换最快。

## 四、典型例题

**例1**（性质）一棵二叉树有 10 个度为 2 的结点、8 个度为 1 的结点，则叶子结点个数是？

解：n0 = n2 + 1 = 10 + 1 = 11。与 n1 无关，这是该公式最迷惑的地方。

**例2**（遍历）二叉树先序序列为 ABDECF，中序为 DBEACF，求后序序列。

解：
1. 先序首位 A 是根；中序中 A 左为 DBE，右为 CF。
2. 左子树先序 BDE、中序 DBE → 根 B，B 的左 D，右 E。
3. 右子树先序 CF、中序 CF → 根 C，C 的左 F。
4. 后序左右根递归：D E B | F C | A → **DEBFCA**。

**例3**（哈夫曼）字符 A、B、C、D 的频率分别为 5、3、2、1，构造哈夫曼树并求 WPL。

解：合并顺序：(1,2)→3；(3,3)→6；(5,6)→11。深度：频率 1 与 2 的叶子深度为 3；频率 3 的叶子深度为 2；频率 5 的叶子深度为 2 或核对树形后得 2。精确 WPL = 1×3 + 2×3 + 3×2 + 5×2 = 3+6+6+10 = 25。（合并细节：第一次 (D1,C2)→3，第二次与 B3 合并 →6，第三次与 A5 →11；深度 D=3,C=3,B=2,A=2。）

## 五、易错点

1. **n0 = n2+1 中 n1 无关**，不要多事代入复杂方程。
2. **深度为 k 最多 2^k−1 个结点**，最少 k 个（单支）；「深度」与「高度」有人从 0 数有人从 1 数，读题看定义。
3. **先序+后序不能唯一确定二叉树**，先+中或后+中才可以。
4. **哈夫曼合并后新结点必须参与下一轮**，漏掉会导致 WPL 偏大。
5. **BST 中序才是有序的**，先序并不有序；删除双支结点不是直接拿空顶替。
6. **完全二叉树与满二叉树**：满一定是完全，完全不一定满。
7. **孩子兄弟表示法**转换后，「右链」是兄弟不是右孩子，画图时极易读错。
8. **哈夫曼树不要求是完全二叉树**，只要求 WPL 最小且无度为 1 的结点；权值互异时形态可能唯一，权值有相同解时形态不一定唯一，但 WPL 相同。
9. **二叉树第 i 层最多 2^(i-1) 个结点**，指数别写成 2^i；从 1 开始计层是教材默认。
10. **求树的深度**与求完全二叉树深度公式不同：一般二叉树深度=左右子树深度较大者+1，不能套 log 公式。

## 六、考点提示

- n0=n2+1、深度公式、完全二叉树编号是三个「秒杀公式」，必须滚瓜烂熟。
- 遍历反推树是下午题常见前奏，练习手速。
- 哈夫曼 WPL 与编码前缀性是送分计算，格式要写清楚合并步骤。
- BST 查找效率题注意最坏 O(n) 的退化情形，引出 AVL/B 树的意义（选择题概念题）。
- 线索树只需记「利用空指针存前驱后继」，细节很少深考。
- 树/森林与二叉树的转换：左孩子右兄弟，能画出来即可。
- 表达式树与前缀/中缀/后缀表达式互相转换，是树遍历的综合应用，下午题算法填空偶有涉及。
- AVL/B 树只需建立「为保持查找 O(log n) 而引入平衡」的动机，细节旋转方案不是上午题重点。
- 练习建议：遍历反推树手画 3 题，哈夫曼 WPL 手算 2 题，n0=n2+1 的变形题 2 题，即可覆盖本课题八成考法。`,
    quizzes: [
      {
        id: 'sd-quiz-03-02a',
        type: 'choice',
        question: '一棵二叉树中度为 2 的结点有 15 个，度为 1 的结点有 7 个，则叶子结点个数为？',
        options: ['14', '16', '15', '8'],
        answer: 'B',
        explanation: '由 n0 = n2 + 1 可得叶子数为 15+1=16。度为 1 的结点个数不影响叶子数。'
      },
      {
        id: 'sd-quiz-03-02b',
        type: 'choice',
        question: '某二叉树先序序列为 ABDECF，中序序列为 DBEACF，其后序序列为？',
        options: ['DEBFCA', 'DEBCFA', 'DBEACF', 'ABDECF'],
        answer: 'A',
        explanation: '根为 A，中序左 DBE 右 CF；递归还原左子树后序 DEB，右子树后序 FC，最后输出 A，得到 DEBFCA。'
      },
      {
        id: 'sd-quiz-03-02c',
        type: 'choice',
        question: '具有 20 个结点的完全二叉树的深度是？',
        options: ['4', '5', '6', '7'],
        answer: 'B',
        explanation: '深度为 floor(log2 20)+1 = 4+1 = 5。'
      },
      {
        id: 'sd-quiz-03-02d',
        type: 'choice',
        question: '关于哈夫曼树，下列说法正确的是？',
        options: [
          '哈夫曼树一定是满二叉树',
          '权值大的叶子结点离根更远',
          '哈夫曼树中没有度为 1 的结点，且 WPL 最小',
          '哈夫曼编码不是前缀码'
        ],
        answer: 'C',
        explanation: '哈夫曼树是 WPL 最小的最优树，构造中每次两两合并，故无度为 1 的结点；权大叶子应靠近根；编码是前缀码。'
      }
    ],
    references: [],
  },
  {
    id: 'sd-03-03',
    moduleId: 'sd-data-structures',
    title: '图与查找排序',
    description: '图的存储与遍历、最小生成树、最短路径、拓扑排序，以及顺序/二分查找与八大排序稳定性复杂度',
    content: `# 图与查找排序

## 考情分析

- **分值归属**：上午题约 3–5 分，是数据结构里计算与概念最密集的一块。图（遍历、MST、最短路、拓扑）+ 查找（二分 ASL）+ 排序（复杂度/稳定性/趟数）几乎各占一小题。
- **题型**：选择题绝对主力。给图求 Prim/Kruskal 结果、Dijkstra 中间结果、二分比较次数、排序趟数与稳定性判断。
- **考频**：★★★★★。排序稳定性与二分 ASL 是历年「原题换数字」大户，图算法每 1–2 考一次成组出现。

## 一、核心概念

### 图的基本概念

**图 G=(V,E)**：顶点集与边集。有向图（弧）、无向图（边）。完全图：无向 n(n-1)/2 条边，有向 n(n-1) 条弧。度（有向分出度入度）、连通图、强连通图、连通分量、网（带权图）。无向图中所有顶点的度之和等于边数的 2 倍；有向图中所有顶点入度之和等于出度之和，都等于弧数。图没有天然的「第一个顶点」，存储与遍历都必须人为选定起点，这也是图比树复杂的原因之一。稀疏图（边数远小于 n²）用邻接表，稠密图用邻接矩阵，判断标准常取 e 与 n log n 的相对大小。

**存储结构**：
- **邻接矩阵** n×n，矩阵元素表示边的存在与权值。空间 O(n²)，找某点的所有邻接点 O(n)，适合稠密图。
- **邻接表**：每个顶点一个单链表接其邻接点。空间 O(n+e)，适合稀疏图。有向图需逆邻接表求入度。
- **十字链表**（有向）、邻接多重表（无向）— 了解名称即可。

**遍历**：
- **DFS（深度优先）**：类似树的先序，用栈或递归，适合判断连通、找路径。
- **BFS（广度优先）**：用队列逐层扩散，求无权最短路径层数即最短边数。
两者在邻接表上都是 O(n+e)，邻接矩阵上 O(n²)。

**最小生成树（MST）**：连通网中边权和最小的生成树（n 个顶点 n−1 条边、无回路）。
- **Prim**：从一点出发，每次选连接「已选集合与未选集合」的最小权边。适合稠密图，O(n²)。
- **Kruskal**：边按权排序，从最小边起选，不成回路则并入。用并查集判回路。适合稀疏图，O(e log e)。
MST 性质：某环中最大权边必不在任何 MST 中；连接两集合的最小边必在某 MST 中。

**最短路径**：
- **Dijkstra（单源）**：按距离递增顺序定标顶点，不能有负权边。复杂度 O(n²)。
- **Floyd（各顶点对）**：动态规划，允许负权不能有负环，O(n³)。
- 无权图单源最短路直接 BFS。

**拓扑排序**：有向无环图（DAG）顶点的线性序列，使所有弧 u→v 中 u 排在 v 前。AOV 网用它排课程/工程顺序。能排出全部顶点则无环；否则有环。AOE 网用**关键路径**：源点到汇点最长路径，活动松弛时间 = 最迟开始 − 最早开始；关键活动松弛时间为 0。

### 查找

- **顺序查找**：平均比较次数 (n+1)/2，成功 ASL 约 n/2，失败 n 或 n+1，O(n)。带哨兵免下标越界判断。
- **二分查找**：仅对**有序顺序表**。mid = low + (high-low)/2。最多比较次数 = floor(log2 n) + 1。链表不能二分（无法随机定位 mid）。
- **分块查找**：块间有序、块内可无序，ASL 介于顺序与二分之间。
- **散列表**：理想 O(1)。冲突：开放定址（线性探测易堆积）、链地址法。装填因子越大冲突越多。

### 排序（必背对照表）

| 方法 | 平均时间 | 最坏 | 空间 | 稳定性 | 要点 |
|------|----------|------|------|--------|------|
| 直接插入 | O(n²) | O(n²) | O(1) | 稳定 | 近乎有序时接近 O(n) |
| 希尔 | O(n^1.3)~ | O(n²) | O(1) | 不稳定 | 分组插入 |
| 冒泡 | O(n²) | O(n²) | O(1) | 稳定 | 可判提前结束 |
| 简单选择 | O(n²) | O(n²) | O(1) | 不稳定 | 比较次数固定 n(n-1)/2 |
| 快速 | O(n log n) | O(n²) | O(log n) | 不稳定 | 近乎有序退化；枢纽选取 |
| 堆排序 | O(n log n) | O(n log n) | O(1) | 不稳定 | 选 top k 利器 |
| 归并 | O(n log n) | O(n log n) | O(n) | 稳定 | 外排常用 |
| 基数 | O(d(n+r)) | 同左 | O(r) | 稳定 | 整数/多关键字 |

**稳定性**：相同关键字相对次序不变。**稳定**：插入、冒泡、归并、基数。**不稳定**：希尔、快排、堆、简单选择。口诀可记「插冒归基稳，希快堆选不稳」。

**排序算法选择直觉**：
- 数据量小、基本有序 → 直接插入。
- 要稳定且规模大 → 归并。
- 要在无序数据中选前 k 个最大或最小 → 堆排序或基于堆的选择，不必全排序。
- 内存紧张 → 堆排序空间 O(1) 优于快排递归栈与归并辅助空间。
- 整数且范围不大 → 基数排序按位分配，不基于比较。
- 内部排序基于比较的下界是 O(n log n)，想更快只能用非比较的基数等方法，这是理论边界不是工程技巧。

## 二、关键原理/公式/机制

### 1. 二分查找判定树

对 n 个记录的有序表二分，判定树是平衡树形态，成功平均比较次数 ASL ≈ log2(n+1) − 1 量级；**最大比较次数 = floor(log2 n)+1**。n=11 时最多 4 次。失败走外结点（虚拟）计比较。

### 2. Dijkstra 更新式

dist[v] = min(dist[v], dist[u] + w(u,v))，每次从未定标集合取 dist 最小者定标。有负权则「已定标的最短路」假设失效。

### 3. Floyd 转移方程

A[k][i][j] = min(A[k-1][i][j], A[k-1][i][k] + A[k-1][k][j])，允许点 k 中转。

### 4. 关键路径

事件最早发生 ve(j) = max(ve(i) + w(i,j))；最迟发生 vl(i) = min(vl(j) − w(i,j))。
活动 e(i,j) = ve(i)，l(i,j) = vl(j) − w(i,j)，松弛 = l − e。松弛为 0 的活动是关键活动。**缩短关键路径才能缩短工期**，非关键路径有余量。

### 5. 堆

大根堆：双亲 ≥ 孩子。建堆 O(n)，取顶 O(log n)。选第 k 大用堆比全排序快。堆排序不稳定（跳跃交换）。

### 6. 快排分割

取枢纽，小的放左、大的放右，递归。基准划分均匀时 O(n log n)；若每次枢纽是极值则退化 O(n²)。初始序列有序或反序是经典陷阱。

## 三、常考题型与解题方法

**题型 1：MST 权值和**
方法：Prim 画「已选点集」逐步加边；Kruskal 列边排序逐条试，用回路判断。两种方法结果权和相同（相同权时形状可能不同）。选错法的代价是漏边或成环。

**题型 2：Dijkstra 表格推演**
方法：画表 dist[]、path[]、已定标集合 S。每轮从未进 S 的 dist 最小出发出发，尝试松弛所有出边。负权直接否决 Dijkstra。

**题型 3：拓扑序列合法性**
方法：检查每条边 u→v 是否 u 在 v 前。有环则拓扑序列长度不足 n。可配 Kahn（入度为 0 出队）验证。

**题型 4：二分查找次数 / 判定树**
方法：套最大次数 floor(log2 n)+1。画判定树算 ASL：成功 ASL = Σ(层数×该层结点数)/n。

**题型 5：排序稳定性与复杂度**
方法：默写对照表。题干关键词「保持相对次序」→ 只能选稳定四君子（插入、冒泡、归并、基数）。「适合外部排序」→ 归并。「选前 k 大」→ 堆。「最坏也是 n log n」→ 堆/归并。

**题型 6：一趟排序后序列**
方法：只执行一趟冒泡/一趟快排分割/一趟简单选择，手推中间序列，不要整表排完。

**题型 7：关键路径与拓扑交叉**
方法：给出 AOE 网时，先拓排出事件顺序，再按 ve 取最大、vl 取最小递推，标出松弛为 0 的活动。问「缩短工期改哪项」只在关键活动里挑；问「某活动最多可延多久」就是它的松弛时间。

**题型 8：图的存储与复杂度**
方法：邻接矩阵空间恒为 n²，与边数无关；邻接表空间 n+e。DFS/BFS 在邻接表上是 O(n+e)。题干问「稀疏图应采用」选邻接表，「判断两点是否有边快」选邻接矩阵。

## 四、典型例题

**例1**（二分）对含 50 个元素的有序表顺序存储做二分查找，最多比较多少次？

解：floor(log2 50)+1 = 5+1 = 6 次。（2^5=32，2^6=64）

**例2**（稳定性）下列排序算法中，稳定的是？

A. 简单选择　B. 快速排序　C. 归并排序　D. 堆排序

解：口诀「插冒归基稳」。选择、快排、堆都不稳定。答案 **C**。

**例3**（Dijkstra/图）无向图 4 个顶点 A,B,C,D，边权 AB=2，AC=5，BC=1，BD=3，CD=2，求 A 到 D 最短路径长度。

解：从 A 出发，dist[A]=0。第一轮定标 A，松弛得 dist[B]=2，dist[C]=5。第二轮定标 B，松弛：A-B-C：2+1=3 < 5，更新 dist[C]=3；A-B-D：2+3=5，dist[D]=5。第三轮定标 C，松弛 C-D：3+2=5，不优于 5。定标 D，最短路长 **5**（A-B-D 或 A-B-C-D 同为 5）。

**例4**（关键路径了解）若某 AOE 网关键路径长度为 20，则工程最短工期为 20；要缩短工期必须缩短关键活动。

## 五、易错点

1. **二分只适用于顺序存储的有序表**，单链表、无序表不能二分。
2. **稳定与不稳定背串**：简单选择、希尔、快排、堆是不稳定的；其余四个是稳定的。
3. **Dijkstra 不支持负权**；Floyd 支持负权但不支持负环。
4. **Prim 与 Kruskal 用途**：稠密 Prim、稀疏 Kruskal；两者都要求是无向连通网。
5. **拓扑排序只对有向图/DAG**；无向图谈拓扑无意义。
6. **快排初始有序反而最坏**；堆排序最坏也是 n log n。
7. **冒泡一趟**只把一个最大（或最小）数「沉底」，一趟后的序列别按完全有序理解。
8. **邻接矩阵表示边数**：无向图矩阵对称，非 0 元个数 = 2e。
9. **二分 ASL 与最大比较次数**别混：一个是平均，一个是上限。
10. **MST 只对无向连通网**定义；有向图的类似问题是最小树形图等，考试不考。
11. **归并排序稳定且最坏也是 n log n**，但需 O(n) 辅助空间；「最坏最好都是 n log n」还要想到堆排序。
12. **基数排序稳定性**：按位分配时必须用稳定收集方法，因此基数排序整体稳定；别把它归入不稳定。
13. **图的度数公式**：无向图 Σdeg = 2e，有向图 Σ入度 = Σ出度 = e，选择题常用来求数量关系。

## 六、考点提示

- 排序对照表（时间/空间/稳定性）是必考清单，建议做成卡片默写。
- 二分最大比较次数公式与判定树画法必须掌握。
- 图算法重在「一次正确推演」：Prim/Kruskal/Dijkstra 各练 2 题即可形成手感。
- 拓扑序列合法性判断 + 有环判定，是与软件工程、操作系统交叉的概念点。
- 关键路径在上午题偶发出现，记「e、l、松弛为 0」三件事。
- 散列表只需记装填因子与冲突处理名称，细节深挖性价比低。
- 关键路径三个量「e、l、l−e」务必会算，尤其 vl 逆推取最小值的写法。
- 排序「一趟结果」类题目按定义严格模拟，冒泡一趟只沉一个最大值，快排一趟只完成一次划分。
- 本课题与下午题算法填空（图遍历、排序实现）有衔接，上午基础打好下午更省力。`,
    quizzes: [
      {
        id: 'sd-quiz-03-03a',
        type: 'choice',
        question: '对含有 50 个元素的有序顺序表进行二分查找，最多需要比较多少次？',
        options: ['5', '6', '10', '25'],
        answer: 'B',
        explanation: '最大比较次数为 floor(log2 n)+1 = floor(log2 50)+1 = 5+1 = 6 次。'
      },
      {
        id: 'sd-quiz-03-03b',
        type: 'choice',
        question: '下列排序算法中，稳定的是？',
        options: ['简单选择排序', '快速排序', '归并排序', '堆排序'],
        answer: 'C',
        explanation: '插入、冒泡、归并、基数为稳定排序；希尔、快排、堆、简单选择为不稳定排序。'
      },
      {
        id: 'sd-quiz-03-03c',
        type: 'choice',
        question: '用 Kruskal 算法求最小生成树，算法每次选边的基本依据是？',
        options: [
          '从任意顶点出发，选连接已选点集的最小权边',
          '按边权递增顺序选边，不成回路则加入',
          '每次选度数最大的顶点',
          '按顶点编号顺序加入所有边'
        ],
        answer: 'B',
        explanation: 'Kruskal 是按边权从小到大枚举，用并查集判断是否成环，不成环则选入。A 选项描述的是 Prim 算法。'
      },
      {
        id: 'sd-quiz-03-03d',
        type: 'choice',
        question: '关于图的最短路径算法，下列叙述错误的是？',
        options: [
          '无权图求单源最短路径可以用 BFS',
          'Dijkstra 算法不适用于带负权边的图',
          'Floyd 算法可求所有顶点对之间的最短路径',
          'Dijkstra 算法可以正确处理带负权边的有向图'
        ],
        answer: 'D',
        explanation: 'Dijkstra 基于「已定标最短路不再更新」的贪心假设，负权边会破坏该假设；带负权应选用 Bellman-Ford 或 Floyd 等。'
      }
    ],
    references: [],
  },
];
