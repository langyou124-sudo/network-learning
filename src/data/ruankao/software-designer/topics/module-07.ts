import { Topic } from '@/types';

export const topics: Topic[] = [
  {
    id: 'sd-07-01',
    moduleId: 'sd-oo-tech',
    title: '面向对象分析与设计',
    description: '对象与类、封装继承多态、OOA与OOD活动与产物',
    content: `# 面向对象分析与设计

## 考情分析

- **分值**：上午综合知识约 2～4 分，常出 1～2 道选择；下午案例以 UML 大题形式出现，OOA/OOD 概念是读类图、补类与方法的理论底座，间接影响 10～15 分大题的作答质量
- **题型**：选择题（三大特征辨析、OOA/OOD 产物区分、多态类型）、填空（类图成分、消息三要素）、下午题（根据需求说明补类、补关系、补操作）
- **考频**：★★★ 近年几乎每次必考。单独背概念容易丢分，要和 UML、设计模式串起来记

## 一、核心概念

### 1. 对象、类与消息

**对象**是系统中客观存在事物的抽象，具有**标识、状态（属性）、行为（方法/操作）**三要素。对象之间通过**消息**通信，消息包含接收对象、操作名、参数（必要时含返回值）。注意：消息只说明“要求做什么”，不说明“怎么做”，这是封装的直接后果。

**类**是具有相同属性、操作、关系和语义的对象集合的描述，是对象的模板；对象是类的实例。类本身也可以看作一种特殊对象（类对象/元类层面在考试中不深究）。

### 2. 面向对象三大特征

- **封装**：把属性和操作的实现细节隐藏在对象内部，对外只暴露受控接口。目的是降低耦合、便于修改实现而不影响使用者。类图中的可见性符号：+ 公有、- 私有、# 受保护、~ 包内可见。
- **继承**：子类自动拥有父类的属性和操作，并可扩展或覆盖。表达 is-a 关系。父类抽象共性，子类表达差异。注意继承会破坏封装（白盒复用），组合/聚合是黑盒复用，更受推荐。
- **多态**：同一操作作用于不同对象产生不同行为。分两类：**编译时多态**（重载 overload，同一类中同名不同参）；**运行时多态**（覆盖/重写 override + 继承/实现接口，靠动态绑定）。考试最爱考“哪个多态是运行时的”。

### 3. OOA 与 OOD

| 维度 | OOA（分析） | OOD（设计） |
|------|-------------|-------------|
| 关注点 | 做什么（问题域） | 怎么做（系统结构与实现） |
| 与实现关系 | 与实现无关 | 必须考虑实现环境、复用、性能 |
| 典型产物 | 类图、用例模型、对象图、动态模型 | 设计类图、包图、构件部署、设计原则落地 |
| 稳定性 | 随需求变化 | 随技术/架构调整 |

**OOA 主要活动**：识别对象/类、识别属性与操作、识别对象间关系（一般-特殊、整体-部分、实例连接、消息连接）、划分主题/子系统、建立动态模型（状态图、顺序图）、建立功能模型（用例）。

**OOD 主要活动**：问题域设计（精化 OOA 结果）、人机交互设计、任务管理设计（并发/事件驱动）、数据管理设计（持久化/数据库映射）；高层设计划分子系统与包，低层设计细化每个类的属性、操作签名与协作。

### 4. 与结构化方法的对照（便于选择题辨析）

结构化方法以**过程/函数**为中心、按功能分解、模块之间靠调用与数据耦合；面向对象以**对象**为中心、按问题域事物分类，属性与操作封装在一起，靠消息协作。结构化的产物主要是数据流图、数据字典、模块结构图；面向对象的产物主要是用例图、类图、顺序图、状态图。考试中“以数据为中心、围绕实体建模、天然支持复用与增量”的描述指向 OO；“自顶向下逐层分解功能、以变换/事务为中心”的描述指向结构化。

### 5. 相关方法论（常考出处）

- **Coad/Yourdon 方法**：OOA 五层次——主题层、类与对象层、结构层（分类与组装）、属性层、服务层。
- **OMT（Rumbaugh）**：对象模型（静态，类图）、动态模型（状态图/事件）、功能模型（数据流图），三模型互补。
- **OOSE（Jacobson）**：以用例为中心，用例驱动、以架构为中心、迭代增量，UML 与 RUP 的源头之一。
- **Booch 方法**：宏过程（概念/分析/设计/进化/维护）与微过程（标识类对象、识别语义、识别关系、说明接口与实现）。

## 二、关键原理/方法

### 1. 类间关系的语义强度（从强到弱）

组合（实心菱形，整体亡则部分亡） > 聚合（空心菱形，部分可独立存在） > 关联（成员变量级引用） > 依赖（use-a，参数/局部/静态调用，最弱）。
泛化（空心三角箭头，is-a）与实现（虚线空心三角）不是强弱序列，而是类型层次关系。

**判断口诀**：生命周期是否与整体绑定 → 组合；可拆可换但属整体一员 → 聚合；长期知道对方 → 关联；临时用一下 → 依赖。构造时创建、析构时销毁 = 组合。

### 2. 好类设计的基本原则（OOD 落地）

- **单一职责**：一个类只因一类原因变化。
- **开闭原则 OCP**：对扩展开放、对修改关闭，靠抽象与多态加新子类，而不是改老代码。
- **里氏替换 LSP**：子类必须能替换父类而不破坏正确性。子类不能改写父类语义、不能收窄父类约定。
- **依赖倒置 DIP**：高层与低层都依赖抽象，抽象不依赖细节。
- **接口隔离 ISP**：不强迫客户依赖它不使用的接口。
- **合成/聚合复用**：优先 has-a / use-a，慎用继承。

### 3. 多态的实现机制

编译器为有覆盖关系的类生成虚函数表（或等价机制），调用时按对象实际类型查表绑定目标代码，这叫**动态绑定/迟绑定**。重载是编译期根据参数表选择，**不属于**运行时多态。Java 中 static 方法无多态；final 方法不可覆盖因而静态确定；private 方法不可覆盖。

## 三、常考题型与解题方法

**题型 1：三大特征辨析**。问“下列哪项体现多态/封装/继承”。方法：看代码是否有父类引用调子类对象且行为不同（多态）；是否只暴露接口隐藏细节（封装）；是否有 extends/继承语法（继承）。

**题型 2：OOA/OOD 产物与活动匹配**。方法：分析阶段产物一定是“问题域模型”，不出现数据库表、线程、框架类；出现技术选型、包划分、性能设计即为设计阶段。

**题型 3：类关系判断**（选择或下午填关系名）。方法：把题目句式翻译成英文动词——“包含/由…组成且生死与共”→组合；“拥有但可换”→聚合；“需要/调用/作参数”→依赖；“是一种”→泛化。

**题型 4：结合设计原则判断代码改动方向**。方法：需求又要扩展又怕改老代码 → 引入抽象 + 新子类（OCP）；父类方法在子类抛异常 → 违反 LSP；高层 new 具体类 → 违反 DIP。

## 四、典型例题

**例 1（选择）** 下列关于面向对象的叙述中，正确的是（）。
A. 方法重载是运行时多态的实现方式
B. 封装的目的是将类的属性与操作绑定，并隐藏实现细节
C. 继承是黑盒复用，组合是白盒复用
D. 多态性是指一个类可以有多个父类

**解析**：选 B。A 错在重载是编译期多态，运行时多态靠继承+覆盖+动态绑定；C 说反了，继承白盒、组合黑盒；D 描述的是多继承，不是多态，且 Java/C# 类的多继承受限。

**例 2（选择）** 在 Coad/Yourdon 的 OOA 五层次中，用于描述对象间分类关系与整体部分关系的是（）。
A. 主题层 B. 类与对象层 C. 结构层 D. 服务层

**解析**：选 C。结构层专门刻画分类结构（一般-特殊）与组装结构（整体-部分）。主题层是控制复杂度的分组，服务层对应操作/消息。

**例 3（分析）** 某“图形编辑器”中，Line、Circle 都要绘制并计算面积，但绘制方式不同。请判断适合的设计：定义抽象类 Shape，声明 draw() 与 area()，由 Line、Circle 继承并实现。问：draw() 在子类重写后，用 Shape 引用调用时执行哪一份？

**解析**：执行子类实际类型的 draw()，属于运行时多态。Shape 中 draw() 应为抽象或空实现可覆盖；调用点 Shape s = new Circle(); s.draw(); 在运行时绑定 Circle::draw。area() 若形状计算各异，同样覆盖；若部分形状共用公式，可在父类给默认实现供覆盖。进一步若还有“可移动、可组合成复杂图形”，则整体部分关系用组合结构，动态加边框职责用装饰结构，它们与本例的泛化层次可以共存：Shape 抽象解决“一种”，组合关系解决“有一个”。

**例 4（判断）** 判断下列分别体现什么特征：① 只提供 getBalance() 不暴露账户内部字段；② 子类 SavingsAccount 覆盖父类 computeFee()；③ 用 Account 类型引用指向不同子类实例并调用 computeFee()。

**解析**：① 封装；② 继承并准备多态；③ 运行时多态。三特征经常在同一段代码里同时出现，答题要分开写，不要一句“面向对象”笼统作答。若再问“③ 相对 if-else 按类型分支有何好处”，可答符合开闭原则，新增子类不用改调用点。

## 五、易错点

1. **重载与覆盖混淆**：重载同类同名不同参、返回值无关、编译期决定；覆盖是子类同名同参、返回值协变、运行期决定。考题常把重载说成多态。
2. **继承与组合张冠李戴**：继承是 is-a（一种），组合是 has-a（有一个）。用继承复用工具类代码是经典坏味道。
3. **OOA 与 OOD 产物混搭**：把“映射成关系表”“选择 MVC”放进分析产物必错。
4. **封装 ≠ 不可访问**：封装是受控访问，提供公有操作是正常的。
5. **消息 ≠ 函数调用的全部**：消息描述协议（谁、做什么），调用细节属于实现。
6. **多继承与多态**：C++ 多继承解决“是什么多个”，多态解决“同一接口不同行为”，不能互推。

## 六、考点提示

- 上午选择：三大特征定义、重载/覆盖、OOA 五层次、OMT 三模型、OOD 四项活动
- 下午案例：结合类图问“这段描述应抽象成什么类/什么关系”，答案要写类名 + 关系名 + 理由（生命周期/语义）
- 备考动作：把本页关系强度表 + OOA/OOD 对照表做成填空自测；再与下一课题 UML 图元对照着背
`,
    quizzes: [
      {
        id: 'sd-07-01-q1',
        type: 'choice',
        question: '下列关于面向对象多态的叙述中，正确的是（）',
        options: [
          '方法重载（overload）是运行时多态的主要实现方式',
          '运行时多态需要继承（或实现接口）、方法覆盖和动态绑定三个条件',
          '只要子类新增了父类没有的方法，就体现了多态',
          '多态允许一个类同时继承多个父类',
        ],
        answer: 'B',
        explanation:
          '运行时多态 = 继承/实现 + 覆盖 + 动态绑定，B 正确。A 混淆重载（编译期多态）；C 新增方法只是扩展，不是多态；D 说的是多继承，与多态无关。',
      },
      {
        id: 'sd-07-01-q2',
        type: 'choice',
        question: '对象生命周期与整体绑定，整体销毁时部分随之销毁，部分类与整体类之间的关系应设计为（）',
        options: ['泛化', '依赖', '聚合', '组合'],
        answer: 'D',
        explanation:
          '生命周期与整体绑定是组合（composition）的判据，实心菱形。聚合（空心菱形）中部分可脱离整体独立存在；依赖只是临时使用；泛化表达 is-a。',
      },
      {
        id: 'sd-07-01-q3',
        type: 'fill',
        question: 'Coad/Yourdon 的 OOA 模型中，刻画一般-特殊结构与整体-部分结构的是 ______ 层。',
        answer: '结构',
        explanation: '五层为主题、类与对象、结构、属性、服务；结构层负责分类结构与组装结构。填“结构层”也给分。',
      },
      {
        id: 'sd-07-01-q4',
        type: 'short-answer',
        question: '说明 OOA 与 OOD 的主要区别，并各写出两项典型活动或产物。',
        answer:
          'OOA 面向问题域、回答“做什么”，与实现无关；OOD 面向系统实现、回答“怎么做”，要考虑复用、技术约束与性能。OOA 活动/产物：识别对象与类、建立用例模型与类图、建立对象动态模型等。OOD 活动/产物：划分子系统与包、人机交互设计、任务管理与数据管理设计、细化设计类图与接口。',
        explanation:
          '评分要点：分析/设计关注点差异 2 分；OOA 两项 1.5 分；OOD 两项 1.5 分。只罗列名词不说明差异最多得 2 分。',
      },
    ],
    references: [],
  },
  {
    id: 'sd-07-02',
    moduleId: 'sd-oo-tech',
    title: 'UML建模',
    description: 'UML图分类、用例类图序列图、关系与消息的语义辨析',
    content: `# UML建模

## 考情分析

- **分值**：下午案例分析**大题常客**，一题 10～15 分（补用例、补类与关系、补序列图消息）；上午 1～3 分考图种归类与关系符号
- **题型**：读图填空（缺哪些用例/类/关系）、看代码或叙述补序列图、判断图的类型与使用场景、说明关系含义
- **考频**：★★★★ 下午拿分关键。符号记混、用例关系用错是最常见失分点

## 一、核心概念

### 0. UML 是什么

UML（统一建模语言）是面向对象的可视化建模标准，不是方法论也不是编程语言，而是一套**图示与术语**。它支持从需求到设计的多视图描述：结构（有什么）、行为（做什么、何时做）、交互（谁与谁如何对话）。使用原则是“视图驱动”：需求阶段画用例图，分析阶段画领域类图与关键交互图，设计阶段细化类图、包图、构件与部署。

### 1. UML 图的两大类十种图（常考归类）

**结构图（静态）**：类图、对象图、包图、构件图、部署图、组合结构图、轮廓图（Profile）。
**行为图（动态）**：用例图、活动图、状态机图、顺序图（序列图）、通信图（协作图）、定时图、交互概览图。

简化记法（软考常用“五图+三图”）：
- 静态：类图、对象图、包图、构件图、部署图
- 动态：用例图、顺序图、通信图、活动图、状态图

### 2. 用例图

**要素**：参与者（Actor，小人）、用例（椭圆）、系统边界（矩形）、关系。
- 参与者是**系统外**与系统交互的角色，可以是人、硬件、外部系统、时钟。
- 用例是系统为参与者提供的**有价值的服务单元**，命名用动宾短语（“登录系统”“查询余额”）。
- **包含 include**：基用例**必定**调用被包含用例，提取公共步骤（虚线箭头 + <>）。箭头从基用例指向被包含用例。
- **扩展 extend**：基用例**可选**在扩展点插入新行为，异常流/可选流常用（<>）。箭头从扩展用例指向基用例。
- **泛化**：用例之间 is-a，子用例继承父用例行为并可特化；参与者之间也可泛化。

### 3. 类图（出现频率最高）

**类的表示**：三栏——类名 / 属性（可见性 名:类型） / 操作（可见性 名(参):返回）。
**抽象类**类名斜体；抽象操作斜体或带 {abstract}；接口用 <> 或棒棒糖表示。
**关系**：
| 关系 | 符号 | 语义 | 记忆 |
|------|------|------|------|
| 依赖 | 虚线 + 开箭头 | use-a，临时使用 | 最弱，方法参数/局部变量 |
| 关联 | 实线 + 开箭头 | 结构性引用，有导航可带重数 | 成员变量 |
| 聚合 | 实线 + 空心菱形 | has-a，可独立 | 人-电脑（可换） |
| 组合 | 实线 + 实心菱形 | 强整体部分，同生命周期 | 窗口-按钮 |
| 泛化 | 实线 + 空心三角 | is-a 继承 | 父类在三角端 |
| 实现 | 虚线 + 空心三角 | 实现接口 | 同上 |

**重数（多重性）**：1、0..1、、0..、1..、m..n。写在关联线两端，表示该端“一个”对应对端“多少个”。

### 4. 顺序图（序列图）

纵轴生命线（对象:类），横轴时间向下。**消息**：
- 同步消息：实心三角箭头（调用并等待返回）
- 异步消息：开箭头（发后即走）
- 返回消息：虚线开箭头（可省略）
- **自关联消息**：自己调自己，折线回自身
- **创建/销毁**：create、destroy（生命线虚线终止）

生命线上**激活条（执行发生）**表示该对象正在执行。**框架/组合片段**（alt、loop、opt、par）表示分支、循环、可选、并行。

### 5. 状态图与活动图

**状态图**：针对**单个对象**生命周期。初态、终态、状态、事件/守卫/动作（触发 [守卫] / 动作）、迁移、复合状态、历史状态。
**活动图**：强调**过程/工作流**，可跨对象。动作节点、分叉汇合（粗横条）、判定合并（菱形）、泳道、对象节点。活动图在语义上是特殊的状态图，但考试按“流程”理解即可。

### 6. 对象图与包图

对象图是某一时刻的对象快照，写法“对象名:类名”，对象名可省略其一。包图组织元素、描述依赖（虚线开箭头）；注意**包循环依赖**是设计坏味道。

## 二、关键原理/方法

### 1. 用例关系怎么选（答题核心）

- 步骤在**多个用例中重复**且**每次都会做** → **include**（必做公共段）
- 行为**不一定发生**，是主流程的可选/异常补充 → **extend**（可选分支）
- 两个用例流程像且有替换关系、一方是另一方的特殊版 → 用例**泛化**

箭头方向口诀：include 箭头指向被复用的“零件”；extend 箭头指向被扩展的“基用例”（扩展件挂在基础件上）。

### 2. 关系选择决策树

1. 是否“是一种”？→ 泛化/实现。
2. 是否需要在属性里长期持有对方？→ 关联；进一步问生命周期是否同生共死 → 组合，否则聚合（或普通关联）。
3. 是否只在方法参数、返回值、局部变量、或调用其静态方法时用到？→ 依赖。
4. 关联再问导航：单向还是双向；写重数必须看需求中“一学生选多门课”这类措辞。

### 3. 顺序图与通信图的信息等价

二者都是**交互图**，可相互转换：顺序图强调**时间顺序**，通信图强调**对象链接结构**。若考“哪个图能看出对象间连接关系”，答通信图；考“哪个图能严格表示时间顺序、支持嵌套控制帧”，答顺序图。**同一交互信息量相同**。

### 4. 状态图与活动图的分工

对象状态迁移、异步事件、不能打断的锁之类 → 状态图。
业务流程、审批流、并行任务、泳道分工 → 活动图。
多个对象的协作时序 → 顺序图。切勿用状态图画“用户下单全流程”。

### 5. 面向对象分析设计中的 UML 用法（映射关系）

| 阶段 | 常用图 |
|------|--------|
| 需求 | 用例图（+活动图描述用例粒度） |
| 分析 | 领域类图、顺序图/状态图（关键对象） |
| 设计 | 设计类图、顺序图（类与方法级）、包图、构件图、部署图 |

## 三、常考题型与解题方法

**题型 1：补用例图**。方法：从需求动词提取用例名（动宾）；找角色（人/外系统/时钟）；发现两用例都有“登录”“身份验证”就 include；发现“短信通知可选”“支付失败重试”就 extend 作扩展用例。

**题型 2：补类图与关系**。方法：名词→类；形容词/“有/含/组成”→关联或聚合组合；“是一种/分类”→泛化；实现接口→实现；方法参数类型→依赖。重数从需求中的数量词抄（“每…可…多个”）。

**题型 3：补顺序图消息**。方法：按用例主流程逐句转消息；“A 调用 B 的 f()”→ A 到 B 同步消息 f()；“A 触发后 B 异步通知 C”→ 异步箭头；消息名写操作名，带参数时写成 m(x)。

**题型 4：判断图类型或关系符号**。方法：背牢“虚线实心？虚线空心三角？实线空心菱形？实线实心菱形？”四句符号句，见第四节例 3。

## 四、典型例题

**例 1（补充用例）** 某图书系统：“读者可检索书目并预约图书；未登录读者必须先登录才能预约；预约成功后系统可选择发送邮件提醒。”请指出用例图中应出现的用例及关系。

**解析**：用例至少有“登录”“检索书目”“预约图书”“发送邮件提醒”。关系：“预约图书” **include** “登录”（需求写“必须先登录”）；“预约图书” **extend** “发送邮件提醒”（“可选择”表可选，扩展用例指向基用例）。角色为读者；系统外邮件网关可作参与者。

**例 2（补类与关系）** “一个窗口包含多个按钮，关闭窗口则按钮一并撤销；按钮可以更换皮肤样式，皮肤样式对象可被多个按钮共享。”
**解析**：类 Window、Button、SkinStyle。Window 与 Button 为**组合**（同生命周期）；Button 与 SkinStyle 为**聚合**（样式可共享、可独立）。若 SkinStyle 只是调用工具，才考虑依赖；这里是持有共享对象，用聚合更准。

**例 3（符号辨析）** “实线 + 空心菱形”“实线 + 实心菱形”“虚线 + 空心三角”“实线 + 空心三角”分别表示什么？
**解析**：聚合、组合、实现、泛化。记法：菱形端是“整体”；实心比空心关系更强；三角+虚线=接口实现，三角+实线=类继承。

**例 4（顺序图）** 某查询流程：用户输入单号，查询服务调数据库接口取记录，若有异常则返回“查询失败”。应在顺序图中如何画？
**解析**：生命线 User、OrderService:QueryService、db:DB。User→OrderService 同步消息 query(no)；OrderService→db 同步消息 select(no)；db→OrderService 返回记录；再加返回消息给 User。异常可用组合片段 alt：一个操作分支正常返回，另一个操作返回“查询失败”。自调用处理逻辑用自关联消息。

## 五、易错点

1. **include / extend 箭头方向**：两者方向相反，考图必看箭头，写关系名时还要能自己画对方向。
2. **include“必做”、extend“可选”**：需求写“必须/首先/都”才 include；写“可选/在…时/异常”才 extend。把“登录”做成 extend 是经典错答。
3. **组合与聚合**：看是否同生命周期，不看“有没有图标感觉更强”。
4. **关联与依赖**：属性级长期引用是关联；仅方法参数/局部/静态调用是依赖。
5. **状态图 vs 活动图**：状态图主体是**一个对象**的状态迁移；活动图主体是**过程**的步骤与并行。
6. **顺序图时间方向**：时间向**下**推进，不是向上。
7. **对象图 ≠ 类图**：对象图标出对象名与链，表示瞬时快照，没有操作栏要求。
8. **通信图 = 协作图**：旧称协作图，同一类图，别当两种图数。

## 六、考点提示

- 下午题答题格式：关系名用标准术语（include/extend/聚合/组合/泛化/实现/依赖/关联），一句话点出需求依据
- 上午题背表：六种关系符号 ↔ 语义；十图归静态/动态；交互图只含顺序图与通信图
- 建议对照真题画三张手绘：用例图（带 include/extend）、类图（带重数）、顺序图（含同步/返回/自消息）
`,
    quizzes: [
      {
        id: 'sd-07-02-q1',
        type: 'choice',
        question: '在 UML 用例图中，“提交订单”用例在执行时必须先完成“身份验证”，则两者之间的关系应建模为（）',
        options: [
          '提交订单 extend 身份验证',
          '身份验证 extend 提交订单',
          '提交订单 include 身份验证',
          '身份验证 include 提交订单',
        ],
        answer: 'C',
        explanation:
          '“必须先”表示必做公共步骤，用 include，且箭头从基用例指向被包含用例，即“提交订单 include 身份验证”。extend 用于可选/扩展行为。',
      },
      {
        id: 'sd-07-02-q2',
        type: 'choice',
        question: 'UML 中“实线 + 实心菱形”表示的关系是（）',
        options: ['聚合', '组合', '关联', '依赖'],
        answer: 'B',
        explanation: '实心菱形是组合（composition），整体与部分同生命周期。空心菱形才是聚合。实线开箭头为关联，虚线开箭头为依赖。',
      },
      {
        id: 'sd-07-02-q3',
        type: 'fill',
        question: '强调对象之间链接结构、弱化时间顺序的交互图是 ______ 图（UML 标准名）。',
        answer: '通信',
        explanation: '通信图（旧称协作图）强调对象链接，顺序图强调时间顺序。二者信息等价，可相互转换。',
      },
      {
        id: 'sd-07-02-q4',
        type: 'short-answer',
        question: '阅读需求：“系统管理员启动备份任务后，任务串行备份所有数据库；备份过程中可暂停并恢复；备份结束记录日志。”应选用什么行为图？说明理由并列出关键图元。',
        answer:
          '应选活动图（若“备份任务”本身是被监控对象的状态迁移，也可画任务对象的状态图，但业务主流程以活动图为主）。理由：描述的是过程/工作流，有可暂停恢复的流程控制与并行或顺序步骤，适合活动图。关键图元：起止节点、动作（启动备份、备份数据库、写日志）、判断/合并（是否暂停）、分叉汇合（若有并行库）、泳道（管理员/系统/日志）。若画状态图，则图元为状态（就绪、运行、暂停、完成）、事件（启动、暂停、恢复、结束）与迁移。',
        explanation:
          '评分要点：选对图 1 分；理由扣住“过程/工作流”或“对象状态”1.5 分；图元合理 2.5 分。两图都写且说清分工可给满分。',
      },
    ],
    references: [],
  },
  {
    id: 'sd-07-03',
    moduleId: 'sd-oo-tech',
    title: '设计模式',
    description: '创建型结构型行为型23种模式、意图辨析与典型应用',
    content: `# 设计模式

## 考情分析

- **分值**：上午综合知识 2～5 分（常见 2～3 道选择，年年有）；下午若有“改进设计/引入模式”小问，约 3～5 分
- **题型**：给出场景选模式名；给出模式名选意图/优缺点；类图角色识别（Creator、Adapter、Decorator、Observer 等）；模式分类（创建/结构/行为）
- **考频**：★★★★ 高频且好拿分。要求“认得场景、说得出意图、分得清易混”，不要求默写全部类图

## 一、核心概念

### 1. 模式三大分类（共 23 种，GoF）

**创建型（5）**：抽象工厂、生成器（建造者）、工厂方法、原型、单例。
—— 关注“如何创建对象”，隔离创建细节，系统不依赖具体类的 new。

**结构型（7）**：适配器、桥接、组合、装饰、外观、享元、代理。
—— 关注“如何组合类/对象成更大结构”，处理接口不兼容、职责叠加、复杂子系统。

**行为型（11）**：责任链、命令、解释器、迭代器、中介者、备忘录、观察者、状态、策略、模板方法、访问者。
—— 关注“对象如何交互、如何分配职责”，封装算法、封装请求、封装状态变化。

### 2. 高频模式意图速记

| 模式 | 一句话意图 | 关键角色/信号词 |
|------|------------|----------------|
| 工厂方法 | 定义创建对象的接口，由子类决定实例化哪个类 | “多个子类延迟到子类 new” |
| 抽象工厂 | 提供创建**一系列相关对象**的接口，不必依赖具体类 | “一族产品”“换皮肤/换数据库族” |
| 建造者 | 分步骤构造复杂对象，同样的过程可产出不同表示 | “分多步组装，结果表示可变” |
| 单例 | 确保一个类仅有一个实例并提供全局访问点 | “唯一实例、全局访问、线程安全” |
| 原型 | 用原型实例指定创建种类，通过**克隆**得到新对象 | “copy 成本低/运行时指定类” |
| 适配器 | 将一个类的接口转换成客户希望的另一个接口 | “接口不兼容、旧类复用、转换头” |
| 桥接 | 将抽象部分与实现部分**分离**，使二者独立变化 | “两个维度都可扩展，避免类爆炸” |
| 组合 | 将对象组合成树形结构以表示“部分-整体”层次 | “树、递归结构、叶与容器同接口” |
| 装饰 | 动态地给对象增加职责，替代多重继承 | “层层包一层、功能叠加不改原类” |
| 外观 | 为子系统一组接口提供统一的高层接口 | “简化复杂子系统、唯一入口” |
| 代理 | 为其他对象提供代理以控制访问 | “远程/虚拟/保护/智能引用” |
| 策略 | 定义算法族并封装，使它们可互换，算法变化不影响客户 | “多算法运行时切换、if-else 堆” |
| 状态 | 允许对象在内部状态改变时改变行为 | “状态迁移图、按状态换行为” |
| 模板方法 | 在父类定义算法骨架，部分步骤延迟到子类 | “钩子方法、骨架固定步骤可变” |
| 观察者 | 定义一对多依赖，状态变化时通知所有依赖者自动更新 | “订阅/发布、事件、广播” |
| 命令 | 将请求封装为对象，支持排队、记录、撤销 | “undo、宏命令、队列请求” |
| 责任链 | 沿链传递请求，直到有处理者处理 | “审批链、过滤器链、可插拔处理者” |
| 迭代器 | 顺序访问聚合元素而不暴露内部表示 | “统一遍历接口、屏蔽容器差异” |
| 中介者 | 用中介对象封装一组对象交互，降低耦合 | “多对象互发消息、网状耦合变星型” |
| 备忘录 | 不破坏封装前提下捕获并恢复对象内部状态 | “快照、回滚、undo 状态” |
| 访问者 | 表示作用于某对象结构中各元素的操作，可在不改类的前提下定义新操作 | “对稳定对象结构频繁加新操作” |
| 解释器 | 给定语言定义文法与解释器 | “简单文法、规则引擎” |

## 二、关键原理/方法

### 1. 创建型三条主线

- **工厂方法**：一个产品等级，创建延迟到子类。新增产品 = 新增工厂子类，符合开闭。
- **抽象工厂**：**多个**产品等级（产品族）。好处是保证同一族产品一起换、一起兼容；代价是加一个**新产品种类**要改所有工厂接口，开闭上不友好。
- **建造者 vs 抽象工厂**：建造者强调**分步构建同一个复杂对象**、产出不同表示；抽象工厂强调**一步得到一族相关对象**。
- **单例实现要点**：私有构造、静态持有唯一实例、全局访问点；注意多线程下懒汉双重检查锁或饿汉直接初始化。

### 2. 结构型的“类 vs 对象”结构

- **类适配器**用多继承/继承实现转换；**对象适配器**用组合持有被适配者，更灵活（可适配子类）。
- **装饰 vs 继承**：装饰是“运行时叠加职责、可自由组合”；继承是“编译期固定扩展”。装饰器与被装饰对象实现**同一接口**，层层包装。
- **装饰 vs 代理**：装饰“加上新职责”是目的；代理“控制访问”是目的（权限、远程、延迟加载）。代码结构像，意图不同，考意图。
- **桥接 vs 适配器**：桥接是**设计之初**就分离抽象与实现两维度；适配器是**事后补救**接口不兼容。
- **外观 vs 中介者**：外观简化子系统对外接口（单向、不回传协调）；中介者协调同事对象间通信（双向、多对象交互逻辑集中）。

### 3. 行为型的“意图”对照

- **策略 vs 状态**：策略是客户**主动**指定用哪种算法，算法间通常互不感知；状态是对象**内部状态迁移**驱动行为切换，状态对象往往彼此知道迁移。场景出现“状态机、随着状态自动变”→ 状态。
- **策略 vs 模板方法**：策略用组合封装算法族，运行时可换；模板方法用继承定骨架，子类填空，编译期固定结构。
- **观察者要点**：Subject 维护观察者列表，状态变化时通知；松耦合，可随时增删订阅者。注意推送/拉取模型差异。
- **命令要点**：把“调用”变成对象，自然支持队列、日志、撤销（undo 时执行 inverse）。
- **责任链要点**：请求沿链传递，处理者可处理也可下传；客户端不知谁处理。

### 4. 选用模式的判断流程

1. 只是在纠结“对象怎么 new”？→ 创建型五选一（系列相关→抽象工厂；分步复杂→建造者；延迟到子类→工厂方法；克隆→原型；唯一→单例）。
2. 接口/结构怎么拼？→ 结构型（不兼容→适配器；两维度扩展→桥接；树形整体部分→组合；动态加职责→装饰；简化入口→外观；控制访问→代理）。
3. 交互/算法/状态怎么管？→ 行为型（可替换算法→策略；状态迁移→状态；骨架钩子→模板方法；一对多通知→观察者；请求对象化→命令；链式处理→责任链）。

## 三、常考题型与解题方法

**题型 1：场景选模式**。方法：抓信号词表格。见“需要兼容旧接口/第三方类”→适配器；见“绘制图形，简单图形与组合图形统一处理”→组合；见“在文本控件外再加边框、滚动条”→装饰；见“系统提供门面统一入口”→外观；见“支付渠道可切换、算法可插拔”→策略。

**题型 2：模式意图判断**。方法：先定分类（创建/结构/行为），再对照一句话意图。问“主要目的”时选意图句，不选“使用了多态”这类实现细节。

**题型 3：易混对（6 组）**。
工厂方法—抽象工厂：单产品 vs 产品族；
建造者—抽象工厂：分步构建一对象 vs 一次得一族对象；
装饰—代理：加职责 vs 控访问；
桥接—适配器：事前分离两维 vs 事后兼容接口；
策略—状态：外部选算法 vs 内部状态驱动；
外观—中介者：对外简化 vs 内部协调。

**题型 4：读类图识别模式**。方法：看类名/注释关键字（Adapter、Decorator、Subject/Observer、Factory、Builder）；看关系——Decorator 层层组合且同接口；Adapter 实现目标接口并持有 adaptee；Observer 是 Subject 持有观察者列表并 notify。

## 四、典型例题

**例 1（选择）** 某系统要支持多种数据库（MySQL、Oracle 等），且每种数据库都要提供“连接”“命令”“适配器”等一组对象。最适合的模式是（）。
A. 工厂方法 B. 抽象工厂 C. 生成器 D. 原型

**解析**：选 B。“一组/一族相关对象”且要整族切换，是抽象工厂的典型场景。工厂方法只适合单个产品等级；生成器是分步构建同一对象；原型靠克隆。

**例 2（选择）** 在不改变现有类代码的前提下，给某图形控件动态添加边框、阴影、滚动条等可自由组合的额外功能，应选用（）。
A. 代理 B. 适配器 C. 装饰 D. 桥接

**解析**：选 C。“动态、可组合、多层叠加职责”是装饰。代理目的是控制访问；适配器解决接口不兼容；桥接分离抽象与实现两维度。

**例 3（分析）** 某审批系统：请假单按金额依次经组长、经理、总监审批，金额越大链越往后；不同单据类型可配置不同审批链。说明适合的模式，并指出角色。

**解析**：责任链（Chain of Responsibility）。角色：Handler 定义 handle 接口与后继 next；ConcreteHandler（组长审批、经理审批、总监审批）判断权限，能批则批，否则传给 next；Client 创建链头并提交请求。好处：请求发送者与处理者解耦，链可配置。

**例 4（辨析）** “订单对象需要保存状态快照以便撤销最近一次修改，且不允许外部直接读写其内部字段。”应选备忘录还是命令？说明理由。

**解析**：选**备忘录**。需求核心是“捕获并恢复对象内部状态”且不破坏封装：原发器 Order 创建 Memento 交给负责人 Caretaker 保管，恢复时由 Order 自己从 memento 回填。命令侧重“把请求/操作对象化”以支持排队、记录、撤销**操作**；若需求是“撤销上一步操作带来的行为”才选命令。本题强调状态快照，故备忘录更准。

## 五、易错点

1. **抽象工厂与建造者混用**：见到“步骤/组件逐步构建”想建造者；见到“产品族一起创建”想抽象工厂。
2. **装饰与代理**：类图极像，答题必须写**意图**——加职责 vs 控访问。
3. **策略与状态**：只看类图几乎一样；题干出现“状态迁移、自动变换行为”选状态。
4. **模板方法与工厂方法**：模板方法是**算法骨架**层面的继承复用；工厂方法只固定“创建”这一步，不定义整个算法流程。
5. **把简单工厂当 GoF 模式**：简单工厂（静态工厂）不在 23 种之列，考“创建型模式”时不要选“简单工厂”。
6. **单例被误当成结构型**：单例是创建型。
7. **外观误用于对象间协调**：内部对象互相发消息导致耦合网，该用中介者。
8. **过度设计**：题目问“最简单做法”时别一上来就抽三层抽象，先看是否只是缺缺省参数/重载。

## 六、考点提示

- 选择题必背：三分类口诀“创单原工建抽，结适桥组装外享代，行责命解迭中备观状策模访”（自行缩写记忆亦可），以及第 1 节表格的意图列
- 下午小问模板：“建议采用 X 模式。因为需求中……（抄信号词）。该模式的意图是……。角色包括……。这样带来的好处是……。”
- 易混 6 组对比（第二节第 3 条）用表格默写三遍，正确率会明显上来
- 与 UML 联动：画出类图能反推模式；画出 Subject-Observer、Decorator 链、Adapter 三角关系是常考画图题
`,
    quizzes: [
      {
        id: 'sd-07-03-q1',
        type: 'choice',
        question: '某框架需要同时创建 Windows 和 Linux 两套 UI 风格，每套都包含 Button、TextBox、Scrollbar 等一组控件对象，要求整套切换。应优先选用（）',
        options: ['工厂方法', '抽象工厂', '生成器', '原型'],
        answer: 'B',
        explanation:
          '“一族/一组相关对象”且整族切换是抽象工厂。工厂方法针对单个产品；生成器用于分步构造同一复杂对象；原型用克隆创建。',
      },
      {
        id: 'sd-07-03-q2',
        type: 'choice',
        question: '装饰（Decorator）模式的主要意图是（）',
        options: [
          '为复杂子系统提供一个统一的高层接口',
          '将抽象部分与实现部分分离，使它们可以独立变化',
          '动态地给对象添加职责，比生成子类更灵活',
          '为其他对象提供代理以控制对它的访问',
        ],
        answer: 'C',
        explanation: 'C 是装饰的标准意图。A 是外观，B 是桥接，D 是代理。',
      },
      {
        id: 'sd-07-03-q3',
        type: 'fill',
        question: 'GoF 将设计模式分为创建型、结构型和 ______ 型三大类；观察者（Observer）属于其中的 ______ 型。',
        answer: ['行为', '行为'],
        explanation: '三分类为创建型（5）、结构型（7）、行为型（11）。观察者处理一对多通知，属于行为型。',
      },
      {
        id: 'sd-07-03-q4',
        type: 'short-answer',
        question: '比较策略（Strategy）模式与状态（State）模式的相同点与不同点，并各举一个典型应用场景。',
        answer:
          '相同点：结构相似，都是定义抽象类/接口，用多个具体子类封装可替换行为，客户与具体行为解耦，符合开闭。不同点：策略由客户或外部显式选择算法，各策略之间彼此独立、不感知切换；状态由对象内部状态迁移自动改变行为，状态对象往往参与状态流转，对象本身是 Context 且状态切换有约束。应用：策略——排序算法/支付方式/折扣算法可运行时切换；状态——订单状态（待支付、已支付、发货、完成）驱动操作行为变化。',
        explanation:
          '评分要点：相同点 1 分；不同点（触发方与耦合关系）2 分；两个例子各 1 分。只写“都是行为型”不超过 1 分。',
      },
    ],
    references: [],
  },
];
