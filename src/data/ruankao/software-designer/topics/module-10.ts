import { Topic } from '@/types';

export const topics: Topic[] = [
  {
    id: 'sd-10-01',
    moduleId: 'sd-network-security',
    title: '网络体系结构',
    description: 'OSI与TCP-IP分层模型、典型协议定位、IP地址与子网划分基础',
    content: `# 网络体系结构

## 考情分析

- **分值**：计算机网络在软件设计师上午题约2-5分，体系结构/协议分层是必考点，常1-2分
- **题型**：选择题（协议属于哪一层、OSI与TCP-IP对照、设备工作层次、IP/端口含义）
- **考频**：★★★ 逢考必有“HTTP/TCP/IP在哪一层”这类题，属于送分题但易记混

## 一、核心概念

### 1. 为什么分层

网络要完成的事很多：物理信号、成帧、编址、可靠传输、应用表达。分层把复杂问题拆开，每层只做一件事，并为上层提供服务。好处是：各层可独立改进、便于标准化、故障好定位。

两个经典模型：
- **OSI/RM 七层**（理论标准）：物理层、数据链路层、网络层、传输层、会话层、表示层、应用层
- **TCP/IP 四层**（事实标准）：网络接口层（链路层）、网际层（网络层）、传输层、应用层

对照关系（考试最爱考）：
| TCP/IP | 对应OSI |
|--------|---------|
| 网络接口层 | 物理层+数据链路层 |
| 网际层 | 网络层 |
| 传输层 | 传输层 |
| 应用层 | 会话层+表示层+应用层 |

### 2. 各层典型协议与设备（核心记忆表）

| 层次 | 作用 | 典型协议 | 互联设备 |
|------|------|----------|----------|
| 物理层 | 比特流传输 | — | 中继器、集线器 |
| 数据链路层 | 成帧、MAC寻址、差错检测 | PPP、HDLC、以太网MAC | 网桥、交换机 |
| 网络层 | 逻辑寻址、路由选择 | IP、ICMP、ARP、RARP、IGMP | 路由器、三层交换机 |
| 传输层 | 端到端可靠/不可靠传输 | TCP、UDP | 四层交换机、网关（部分） |
| 应用层 | 用户应用与表示 | HTTP、HTTPS、FTP、SMTP、POP3、DNS、TELNET、SSH、DHCP | 网关、防火墙（高层） |

补充：ARP把IP解析为MAC，教材常归入**网络层**或称为网络层与数据链路层的衔接协议，做题以题干口径为准；只要题干说“将IP解析为物理地址”即可锁定ARP。

### 3. IP地址与端口

**IPv4地址**：32位点分十进制，由“网络号+主机号”组成。按规模分：
- A类：1.0.0.0~126.255.255.255，默认掩码255.0.0.0（/8）
- B类：128.0.0.0~191.255.255.255，默认掩码255.255.0.0（/16）
- C类：192.0.0.0~223.255.255.255，默认掩码255.255.255.0（/24）
- D类组播，E类保留

私有地址（常考）：
- 10.0.0.0 ~ 10.255.255.255
- 172.16.0.0 ~ 172.31.255.255
- 192.168.0.0 ~ 192.168.255.255

**端口**：传输层用端口号标识应用进程。熟记：FTP控制20/21、SSH22、TELNET23、SMTP25、DNS53、HTTP80、POP3 110、HTTPS443。TCP是面向连接可靠传输（三次握手），UDP是无连接不可靠传输（开销小）。

### 4. 数据封装

发送时自上而下封装：应用数据 → 传输层段（加端口号）→ 网络层分组（加IP地址）→ 链路层帧（加MAC地址）→ 物理层比特。接收时反向解封装。可以形象理解为“寄信时一层层套信封”：应用层写正文，传输层写收发进程端口，网络层写源IP与目的IP，链路层写源MAC与目的MAC。每层的控制信息就是该层首部，链路层往往还有尾部校验字段。

### 5. 分层模型的实际意义

互联网实际采用的是TCP/IP体系，OSI更多是理论参考与教学框架。软件设计师考试常以OSI七层为名义考点，但具体协议按TCP/IP归层。记协议时建议双坐标：既知道它在OSI哪一层，也知道它在TCP/IP哪一层，避免题干切换模型时失分。例如HTTPS：按OSI说是应用层加上表示层的加密功能，按TCP/IP说是应用层协议建立在TLS之上。路由器永远是网络层设备，与模型名称无关。

## 二、关键原理/机制

### 1. OSI各层功能精讲

**物理层**：规定接口的机械、电气、功能、规程特性，保证比特透明传输。不关心比特含义。

**数据链路层**：把比特流组装成**帧**，负责物理地址（MAC）、成帧、差错控制（CRC）、流量控制，以及介质访问控制（CSMA/CD等）。交换机依据MAC表转发，工作在这一层。

**网络层**：负责**逻辑寻址**与**路由选择**，把分组从源送到目的，可跨异构网络。IP提供无连接不可靠服务，可靠性交给上层。路由器根据路由表转发。

**传输层**：提供**端到端**（进程到进程）服务。TCP保证可靠、面向连接、有拥塞与流量控制；UDP不保证可靠但实时性好。端口号实现复用与分用。

**会话层**：建立、管理、拆除会话，同步（检查点）。考试少考，记住“会话管理/同步”即可。

**表示层**：数据表示、加密解密、压缩解压、格式转换（如ASCII与EBCDIC）。

**应用层**：直接为用户应用提供协议。HTTP、FTP、SMTP等都放这一层。

### 2. TCP与UDP对比（高频）

| 对比项 | TCP | UDP |
|--------|-----|-----|
| 连接 | 面向连接（三次握手） | 无连接 |
| 可靠性 | 可靠、有序、不丢不重 | 尽最大努力，不保证 |
| 开销 | 首部20字节起，较大 | 首部8字节，小 |
| 模式 | 字节流 | 报文 |
| 应用 | 文件、网页、邮件 | DNS、视频、语音、DHCP |

TCP三次握手：SYN → SYN+ACK → ACK。四次挥手用于连接释放。可靠性机制：序号确认、超时重传、滑动窗口、拥塞控制（慢开始、拥塞避免、快重传、快恢复）。

### 3. 子网划分与CIDR

子网掩码与IP按位与，得到网络地址。一个C类地址/24若借2位做子网（/26），每个子网主机数 = 2^6 − 2 = 62（全0网络、全1广播不作主机地址）。

CIDR记法：192.168.1.0/26。路由聚合把连续子网合成大前缀，减少路由表规模。

划分步骤：
1. 确定需要的子网数或每子网主机数
2. 计算需要借的主机位数（子网数）或保留的主机位数
3. 写出新掩码
4. 列出子网网络地址与可用主机范围

### 4. ARP/DNS/DHCP作用链

访问网址时的典型流程：域名经**DNS**解析为IP；若目的在同一局域网，**ARP**把IP解析为MAC；本机IP若动态获取则靠**DHCP**。三层设备（路由器）按IP转发，二层设备（交换机）按MAC转发。

## 三、常考题型与解题方法

**题型1：协议/设备属于哪一层**
方法：按记忆表一一对应。最常考：HTTP、DNS在应用层；TCP/UDP在传输层；IP、ICMP、ARP在网络层；PPP、以太网、交换机在数据链路层；路由器在网络层；集线器/中继器在物理层；网关可到应用层。

**题型2：TCP与UDP场景选择**
方法：要可靠、有序、面向连接选TCP；要实时、可容忍少量丢包、开销小、查询类选UDP。DNS查询通常UDP（也可TCP），流媒体用UDP，文件传输HTTP/FTP用TCP。

**题型3：子网掩码/主机数计算**
方法：掩码/网络与运算；可用主机数 = 2^主机位 − 2；子网数 = 2^借用位。算完验证广播地址=主机位全1，网络地址=主机位全0。

**题型4：数据封装与设备转发**
方法：封装自上而下加头；交换机看MAC，路由器看IP，集线器只放大信号广播。问“交换机根据什么转发”选MAC地址；“路由器根据什么”选IP地址。

**题型5：常用端口与服务匹配**
方法：把20/21、22、23、25、53、80、110、443列成两列对照记忆。题干给出“端口25”就找邮件发送SMTP；给出“443”就是HTTPS。DNS是53，且默认可用UDP查询。

**题型6：判断协议工作层次**
方法：看该协议“直接为谁服务”。为应用进程服务的属应用层；管端到端进程通信的属传输层；管跨网络逻辑寻址的属网络层；管成帧与MAC的属数据链路层。网关一般归高层互联设备，中继器、集线器是最简单的物理层设备，不做智能转发。

## 四、典型例题

**例1（协议层次）**
HTTP、TCP、IP、以太网分别工作在TCP/IP模型的哪一层？

解：HTTP在应用层；TCP在传输层；IP在网络层（网际层）；以太网在网络接口层（对应OSI物理层+数据链路层）。
分析：HTTP是应用协议，建立在TCP之上；TCP提供端到端段传输；IP提供分组转发；以太网定义帧与MAC。若按OSI七层，则以太网主要指数据链路层（含物理层规范）。

**例2（TCP/UDP）**
下列应用中一般使用UDP的是（）：A.网页浏览 B.文件下载 C.域名解析 D.电子邮件发送。并说明理由。

解：选C。域名解析DNS要求快速、简单、无须连接建立，查询失败可重试，常用UDP；网页浏览HTTP、文件下载FTP、邮件SMTP都要求可靠传输，用TCP。
分析：判断标准——“丢不起、要排序”用TCP；“快优先、可丢”用UDP。

**例3（子网划分）**
某单位分到C类地址192.168.10.0/24，需划分4个子网，每个子网至少50台主机。请给出子网掩码、各子网网络地址，并说明每子网可用主机数。

解：
（1）每子网至少50台 → 主机位至少6位（2^6−2=62≥50，2^5−2=30<50）。
（2）需4个子网 → 借2位网络位（2^2=4）。原24位网络位+2=26位，掩码255.255.255.192（/26）。
（3）四个子网网络地址：
  - 192.168.10.0/26，可用主机 192.168.10.1~192.168.10.62
  - 192.168.10.64/26，可用主机 192.168.10.65~192.168.10.126
  - 192.168.10.128/26，可用主机 192.168.10.129~192.168.10.190
  - 192.168.10.192/26，可用主机 192.168.10.193~192.168.10.254
（4）每子网可用主机数 62。
分析：主机数先满足再借子网位，顺序不能反；掩码/26对应最后一段二进制11000000即192。

**例4（封装与设备）**
某数据包从主机发出访问外网，依次经过交换机与路由器。简述各设备依据什么地址转发，以及数据包在层次间如何变化。

解：主机侧自上而下封装：应用数据加TCP/UDP端口成为段，再加IP地址成为分组，再加MAC地址成为帧。同网段内交换机读取目的MAC查表转发；跨网段时目的MAC是网关（路由器接口）MAC，路由器收到后剥离帧头，读取目的IP查路由表，重新封装成下一跳的帧发出。
分析：交换机“看MAC、二层转发”，路由器“看IP、三层转发”。主机发往非本网段时，帧的目的MAC始终是下一跳网关的MAC，而IP目的地址始终是最终目标，这个细节是简答题得分点。

## 五、易错点

1. **把ARP当传输层协议**——ARP解析MAC地址，考试通常归网络层（或与链路层相关），不是传输层。
2. **TCP/IP四层与OSI七层对不齐**——TCP/IP应用层对应OSI上三层，网络接口层对应下两层。
3. **子网可用主机数忘记减2**——网络地址与广播地址不能配给主机。
4. **交换机与路由器层次混**——交换机二层（MAC），路由器三层（IP），集线器一层。
5. **DNS/HTTP端口记反**——DNS 53，HTTP 80，HTTPS 443，SMTP 25，FTP 21（控制）。
6. **认为IP是可靠协议**——IP不可靠无连接，可靠由TCP负责。
7. **私有地址范围背错**——172.16~172.31是B类私有，不是172整个B类。

## 六、考点提示

- 必背三张表：**层次-协议-设备**对照、**TCP vs UDP**、**常用端口**
- 子网计算套路固定：需求 → 主机位/子网位 → 掩码 → 网络地址与范围
- 上午选择题送分点多，做到“协议出现就能定位层次”即可稳拿
- 下午若考网络，多半结合防火墙、子网与IP规划，分层知识是地基
- OSI七层顺序可记：物数网传会表应（物理、数据链路、网络、传输、会话、表示、应用）
- 端口号属于传输层编址，IP属于网络层编址，MAC属于数据链路层编址，三层地址别串台
- 凡是“设备看什么转发”的题，答集线器看比特/信号，交换机看MAC，路由器看IP`,
    quizzes: [
      { id: 'sd-10-01a', type: 'choice', question: '在OSI参考模型中，负责端到端可靠传输并进行流量控制的是（）', options: ['网络层', '传输层', '会话层', '表示层'], answer: 'B', explanation: '传输层提供端到端（进程到进程）服务，TCP在此层实现可靠传输与流量/拥塞控制。网络层负责路由与逻辑寻址；会话层管理会话；表示层负责数据表示与加密压缩。' },
      { id: 'sd-10-01b', type: 'choice', question: '下列协议中，工作在应用层的是（）', options: ['IP', 'ARP', 'HTTP', 'PPP'], answer: 'C', explanation: 'HTTP是超文本传输协议，属于应用层。IP、ARP属于网络层相关协议；PPP是数据链路层协议。应用层常见：HTTP、FTP、SMTP、DNS、POP3、DHCP等。' },
      { id: 'sd-10-01c', type: 'fill', question: 'TCP/IP模型从下到上依次为网络接口层、______、传输层和应用层。', answer: '网际层', explanation: 'TCP/IP四层：网络接口层（对应OSI物理层+数据链路层）、网际层（对应网络层，核心协议IP）、传输层（TCP/UDP）、应用层（对应OSI上三层）。' },
      { id: 'sd-10-01d', type: 'short-answer', question: '简述TCP与UDP的主要区别，并各举一个典型应用场景。', answer: 'TCP：面向连接、可靠传输、有序、有确认重传与流量控制、开销较大，适合要求数据完整的场景，如网页浏览（HTTP）、文件传输（FTP）、电子邮件（SMTP）。UDP：无连接、不保证可靠与有序、首部小、速度快、可丢包，适合实时性要求高的场景，如视频点播/会议语音、DNS查询、简单请求应答。建立连接常用三次握手（TCP特有）。', explanation: '评分要点：连接方式、可靠性、开销、应用场景各占分。只写“TCP可靠UDP不可靠”过于简略，要展开机制与场景。' },
    ],
    references: [],
  },
  {
    id: 'sd-10-02',
    moduleId: 'sd-network-security',
    title: '信息安全基础',
    description: '安全属性三要素、对称与非对称加密、数字签名与PKI、防火墙与常见攻击防范',
    content: `# 信息安全基础

## 考情分析

- **分值**：信息安全约2-4分，常与网络模块一起出选择题；数字签名、加密算法几乎是每年至少一问
- **题型**：选择题（对称/非对称特点、数字签名作用、证书与CA、攻击类型辨析）、简答（说明数字签名过程）
- **考频**：★★★ “数字签名用谁的私钥加密”这类题是经典必考，性价比极高

## 一、核心概念

### 1. 信息安全三要素（CIA）

- **机密性（Confidentiality）**：信息不被未授权者获知。手段：加密、访问控制
- **完整性（Integrity）**：信息不被未授权篡改、且可验证。手段：数字签名、消息摘要、校验码
- **可用性（Availability）**：授权用户在需要时能正常使用。手段：冗余、容灾、防DDoS

扩展常提：**可控性**（对传播可控）、**不可否认性**（抗抵赖，靠数字签名）。做题先看题干描述的是“偷看、偷改还是用不了”，分别对机密、完整、可用。

### 2. 密码学基础

**对称加密**：加密解密用**同一密钥**。代表算法：DES、3DES、AES、RC4、IDEA、SM1/SM4。
优点：速度快、适合大量数据。缺点：密钥分发困难、密钥管理复杂、难以抗抵赖。

**非对称加密（公钥密码）**：加密解密用**一对密钥**（公钥公开、私钥保密）。代表：RSA（基于大数分解）、ECC（椭圆曲线）、ElGamal、SM2。
公钥加密私钥解（保密通信），或私钥加密公钥解（验签）。优点：解决密钥分发与签名问题；缺点：运算慢，常只用于加密会话密钥/签名，数据本体仍用对称加密。

**混合加密**（实战与考题都常见）：用对方公钥加密随机产生的**会话密钥**（非对称），再用会话密钥加密数据（对称）。兼顾速度与密钥安全。

### 3. 消息摘要与数字签名

**哈希函数（散列）**：把任意长消息映射为定长摘要，满足单向性、抗碰撞性。代表：MD5（128位）、SHA-1、SHA-256、SM3。用途：完整性校验。

**数字签名**（必考流程）：
1. 发送方对消息M做摘要 H = Hash(M)
2. 用**发送方自己的私钥**对H加密，得到签名 S
3. 将(M, S)发给接收方
4. 接收方用**发送方的公钥**解密S得到H'，再对M做Hash得到H''
5. 若H' = H''，则验证通过

数字签名的三大作用：**认证（证明是谁发的）**、**完整性（消息没被改）**、**不可否认性（发过不能赖）**。它**不提供机密性**——若还要保密，需再加密消息本体。

**一句话记死**：签名用**私钥**，验签用**公钥**；加密（向对方发密）用**对方公钥**，解密用**自己私钥**。

### 4. 数字证书与PKI

**数字证书**把用户身份与公钥绑定，由可信第三方**CA（认证中心）**签发。证书内容含：用户公钥、身份信息、CA签名、有效期等。X.509是常见标准。

**PKI（公钥基础设施）**组成：
- CA：签发、吊销证书
- RA：注册机构，审核身份
- 证书库：目录服务，发布证书与CRL
- 密钥管理/备份
- 终端实体：用户与应用

**信任模型**：层次结构、网状、桥接。根CA自签证书，下级由上级签发。

### 5. 网络安全设备与常见攻击

**防火墙**：在受信与非受信网络间按策略过滤。分包过滤（网络/传输层，看IP端口）、应用代理/应用网关（应用层，可深度检查）、状态检测。作用是访问控制，**防不住内部攻击**与病毒（需配合IDS/杀毒）。

**IDS/IPS**：入侵检测（旁路监听报警）/入侵防御（串接阻断）。
**VPN**：在公用网上建逻辑专用通道，常用IPSec、SSL、L2VPN，保障机密与完整。
**WAF**：Web应用防火墙，防SQL注入、XSS等。

常见攻击与防范：
| 攻击 | 类型 | 防范 |
|------|------|------|
| 木马、病毒、蠕虫 | 恶意代码 | 杀毒、补丁、权限最小化 |
| SQL注入、XSS、CSRF | Web漏洞 | 参数化查询、过滤转义、Token |
| 钓鱼、社会工程 | 欺骗 | 安全意识、证书校验 |
| DDoS | 拒绝服务 | 清洗、限流、CDN |
| 中间人 | 窃听/冒充 | HTTPS、证书校验 |
| 暴力破解、弱口令 | 认证攻击 | 强口令、锁定策略、多因素 |

## 二、关键原理/机制

### 1. 加密与签名的组合使用

完整安全通信（既保密又抗抵赖）：
1. 发送方计算消息摘要并用**自己私钥**签名
2. 用**接收方公钥**加密消息与签名
3. 接收方用**自己私钥**解密，再用**发送方公钥**验签

口诀：**签名自己私钥，验签对方公钥；加解密是一对，发信加对方公钥。**

### 2. 数字证书验证链

验证服务器证书时：检查有效期 → 用CA公钥验签 → 查CRL/OCSP是否吊销 → 核对域名。根CA公钥内置在系统或浏览器中，形成信任链（叶子证书←中间CA←根CA）。

### 3. 访问控制模型（常考）

- **自主访问控制DAC**：资源属主自己定权限，灵活但易泄露（如Linux chmod属主）
- **强制访问控制MAC**：按安全标签（密级）强约束，军方多用，严格
- **基于角色的访问控制RBAC**：用户-角色-权限，便于企业管理，降低授权成本
- **基于属性的访问控制ABAC**：按属性策略动态判定

### 4. 安全协议对号入座

| 协议 | 作用 | 层次/场景 |
|------|------|-----------|
| SSL/TLS | 传输加密、身份认证 | HTTP→HTTPS，传输层之上 |
| IPSec | 网络层VPN加密认证 | 站点到站点VPN |
| SET | 电子商务安全支付 | 在线支付（历史） |
| PGP | 邮件加密与签名 | 邮件应用 |
| Kerberos | 网络身份认证（票据） | 域内认证 |
| SSH | 安全远程登录、替代TELNET | 管理通道 |

### 5. 鉴别与密钥管理要点

身份鉴别（认证）是“证明你是谁”，手段分三类：你知道什么（口令）、你有什么（U盾、令牌）、你是什么（指纹、人脸）。多因素认证指同时具备两类以上，显著优于单一口令。密钥管理包括生成、分发、更新、吊销与备份，是非对称体系中工程量最大的一环。对称加密的核心矛盾就是密钥如何安全分发，考试若问“对称加密的最大缺点”，标准答案是密钥分发与管理困难。

## 三、常考题型与解题方法

**题型1：对称与非对称加密比较**
方法：记牢“对称快但密钥难分发；非对称解决分发与签名但慢”。看到“适合加密大量数据”选对称（AES等）；“适合密钥交换/数字签名”选非对称（RSA等）。

**题型2：数字签名用谁的密钥**
方法：自问“签名要能证明是‘我’发的，所以用只有我有的——私钥”。验签人人可用——公钥。加密给对方，用对方公开的公钥。这类题选项换着写“甲方公钥/私钥”，按人称转换即可。

**题型3：攻击类型与防范匹配**
方法：先判断是窃听（机密）、篡改（完整）、拒绝服务（可用）、冒充（认证/不可否认）哪一类，再对号入座选技术。

**题型4：证书/PKI组成**
方法：CA签发证书，RA审核身份，证书绑定公钥与身份，CRL/OCSP管吊销。问“谁签发数字证书”永远是CA。

**题型5：混合加密流程还原**
方法：按“身份与会话密钥用非对称，数据本体用对称”两段式叙述。发送方生成随机会话密钥，用接收方公钥加密该密钥，再用会话密钥加密数据；接收方先用自己私钥解出会话密钥，再解密数据。答清“哪一步用对称、哪一步用非对称”即可。

**题型6：鉴权与访问控制模型选择**
方法：属主自己授权是DAC；按密级强约束是MAC；按岗位角色授权是RBAC；按属性动态策略是ABAC。企业应用常见RBAC，军事多用MAC，个人文件系统常见DAC。

## 四、典型例题

**例1（数字签名）**
在数字签名过程中，发送方对报文摘要使用（）加密。
A.接收方公钥 B.接收方私钥 C.发送方公钥 D.发送方私钥

解：选D。
分析：发送方用**自己的私钥**对摘要加密生成签名，保证只有本人能产生该签名（认证+不可否认）；接收方用发送方公钥解密验证。若误选A，那是加密保密通信的做法，不是签名。

**例2（对称与非对称）**
下列关于对称加密与非对称加密的叙述，正确的是（）。
A.非对称加密速度比对称加密快，适合大量数据
B.对称加密使用一对公钥和私钥
C.非对称加密可解决密钥分发问题，常用于数字签名与密钥交换
D.对称加密不能用于数据加密

解：选C。
分析：对称加密加解密同钥、速度快，适合数据本体；非对称一对密钥、速度慢，擅长密钥交换和签名。混合加密是常规做法：非对称传会话密钥，对称加密数据。

**例3（防火墙与攻击）**
某网站用户登录后频繁被诱导点击不明链接，导致账号在不知情下发起转账。请指出主要攻击手段与至少两条防范措施。

解：主要属于**钓鱼/XSS/CSRF类Web欺骗攻击**（结合社会工程诱导点击），若转账动作被自动带身份发起，典型是CSRF（跨站请求伪造）；若链接仿冒登录页则为钓鱼。
防范：①关键操作二次验证（短信/令牌）；②请求加Token防CSRF；③校验Referer/Origin；④部署WAF过滤注入与跨站脚本；⑤对用户开展安全意识培训。
分析：先定性攻击（窃密/欺骗/拒绝服务），再从技术与管理两方面给措施，案例题这样答才拿满分结构。

**例4（混合加密）**
简述在电子商务中如何同时实现数据机密性与发送方不可否认性。

解：发送方先对订单报文做摘要并用自己私钥签名（保证不可否认与完整性），再把报文与签名打包，用接收方公钥加密（保证机密性）；接收方用自己私钥解密，再用发送方公钥验签并核对摘要。若报文很长，可采用混合方式：随机生成会话密钥，用接收方公钥加密会话密钥，用会话密钥加密报文，签名仍用发送方私钥对摘要处理。
分析：机密性靠“对方公钥加密”，不可否认靠“自己私钥签名”，两者叠加使用。只答加密不答签名不得分，只答签名不答加密也不得满分。

## 五、易错点

1. **数字签名用公钥加密摘要**——错误。签名是**发送方私钥**加密摘要。
2. **以为数字签名提供机密性**——签名不加密消息内容，保密需另加密。
3. **对称/非对称速度与用途记反**——对称快、非对称慢；非对称解决分发和签名。
4. **CA与RA职责混淆**——CA签发吊销证书，RA审核注册身份。
5. **认为防火墙万能**——防火墙主要做边界访问控制，防不住内部人员、病毒与0day漏洞。
6. **MD5当加密算法**——MD5是哈希/摘要算法，不是加密算法，也不可逆“解密”。
7. **HTTPS与HTTP端口**——HTTP默认80，HTTPS默认443；HTTPS=HTTP+TLS。

## 六、考点提示

- 必背对比表：**对称 vs 非对称**；数字签名四步流程原文复述
- “谁的公钥/私钥”类题每年换皮，核心口诀不变
- 攻击题按 CIA + 不可否认 四格分类，再选技术手段
- PKI抓一条链：RA审核 → CA签发 → 证书绑定 → CRL吊销
- 选择题里出现 RSA、AES、MD5、SHA、数字证书、防火墙 即可快速定位知识点板块
- 案例题答安全措施时分“技术手段+管理制度”两段写，结构分更容易拿满
- 记牢三个默认端口：HTTP 80、HTTPS 443、DNS 53，常与网络题交叉考`,
    quizzes: [
      { id: 'sd-10-02a', type: 'choice', question: '数字签名技术中，发送方对消息摘要进行加密所使用的密钥是（）', options: ['接收方公钥', '接收方私钥', '发送方公钥', '发送方私钥'], answer: 'D', explanation: '数字签名用发送方私钥对摘要加密，接收方用发送方公钥解密验证，从而实现认证、完整性与不可否认性。用对方公钥加密是为了机密性，不是签名。' },
      { id: 'sd-10-02b', type: 'choice', question: '下列属于非对称加密算法的是（）', options: ['DES', 'AES', 'RSA', 'RC4'], answer: 'C', explanation: 'RSA是公钥（非对称）加密算法，基于大数分解难题。DES、AES、RC4、IDEA都是对称加密算法，加解密使用同一密钥。' },
      { id: 'sd-10-02c', type: 'fill', question: '信息安全三要素CIA是指机密性、完整性和______。', answer: '可用性', explanation: 'CIA：Confidentiality机密性、Integrity完整性、Availability可用性。扩展还有可控性、不可否认性（抗抵赖）等，但核心三要素是CIA。' },
      { id: 'sd-10-02d', type: 'short-answer', question: '简述数字签名的完整过程，并说明它能提供哪些安全服务、不能提供什么。', answer: '过程：（1）发送方对消息M计算摘要H=Hash(M)；（2）用发送方自己的私钥对H加密得到签名S；（3）发送(M,S)给接收方；（4）接收方用发送方公钥解密S得到H1，对M计算Hash得到H2；（5）比较H1与H2，相等则验证通过。可提供：身份认证、数据完整性、不可否认性（抗抵赖）。不提供机密性——签名不隐藏消息内容，若需保密还须用接收方公钥（或会话密钥）另行加密。', explanation: '评分要点：摘要、私钥签名、公钥验签、比对；三类安全服务；明确指出不提供机密性。把“公钥加密摘要”写成签名是典型零分错误。' },
    ],
    references: [],
  },
];
