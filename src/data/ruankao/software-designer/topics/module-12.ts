import { Topic } from '@/types';

export const topics: Topic[] = [
  {
    id: 'sd-12-01',
    moduleId: 'sd-case-study',
    title: '数据流图与结构化案例',
    description: '下午案例必考：DFD建模与平衡检查。练外部实体/加工/数据存储辨识、父图子图平衡、错误识别与补全。',
    content: `# 数据流图与结构化案例

## 考情分析

数据流图（DFD）是软件设计师下午科目"系统分析与设计案例"的传统高频考点，几乎每隔一年就完整或半完整出现在案例题中，常见问法：识别图中缺失/画错的元素、指出父图与子图不平衡处、补充数据流或数据存储、说明某加工的数据字典条目。分值通常占一题大题中的2～4问。考生失分点集中在：**把数据流与控制流混淆、漏看父子图平衡、把本应是加工的地方画成外部实体**。本课题必须"看图能判、动笔能补"，光背概念不够。

## 一、核心概念

### 1. DFD 的四种基本元素

1. **外部实体（External Entity）**：系统之外与系统有数据往来的人员、部门、设备或其他系统，用方框表示。图上只出现与系统交互的数据，不描述其内部处理。判定要点：**不被本系统加工，且在图中只作为数据的源或宿**。
2. **加工（Process / Bubble）**：对数据进行的变换，用圆圈（或圆角矩形）表示，编号一般为 P1、P2…。每个加工必须**既有输入又有输出**（父图中至少一个输入、一个输出）。
3. **数据存储（Data Store）**：系统内需保存的数据集合，用双杠或开口矩形表示，编号为 D1、F1 等。文件、数据库表、日志都算数据存储。
4. **数据流（Data Flow）**：数据的流动，用带名字的箭头表示。箭头名字是**数据**而非动作，如"订单信息""成绩清单"，不要写成"提交""查询"这类动词性控制语义。

### 2. DFD 的层次与平衡

复杂系统按"自顶向下、逐步细化"分层：
- **顶层图（环境图/Context Diagram）**：只有**一个加工**（整个系统），周围是外部实体，表示系统与外界的全部数据交换。
- **0层图（顶层加工的第一次分解）**：把那唯一加工拆成若干主要加工，显示主要数据存储。
- **1层及以下子图**：对某个加工继续分解。

**平衡（Balance）** 是 DFD 的灵魂：**父图中某个加工的输入数据流，必须与该加工对应子图的全部输入数据流之和（对外部而言）一致；输出同理**。换句话说，子图对外的接口必须与父图中被分解的那个加工的接口完全一致。子图内部新增的加工之间数据流、新增的数据存储，不算"对外接口"，不影响平衡。常见不平衡：
- 父图有"退货请求"进 P3，子图却没有任何输入承载它；
- 子图对外多输出了一条父图没有的"统计报表"；
- 把内部中间数据流画到了子图边界外，造成"多出来"的接口。

### 3. 数据流图的正确画法规则

- 只能是**加工读/写数据存储**和**加工与其他元素之间**有数据流；**外部实体之间、外部实体与数据存储之间、数据存储与数据存储之间不得直接连数据流**（必须经加工）。
- 每个加工至少一条入流、一条出流（黑洞、奇迹、灰洞检查）：
  - **黑洞**：只有入没有出；
  - **奇迹（或称"无中生有"）**：只有出没有入；
  - **灰洞**：输出数据流无法由输入数据流和数据存储中的数据变换得到。
- 父图与子图平衡；数据流命名用名词短语；避免控制流（如"错误标志"不承载数据时属控制信息，经典 DFD 一般不画）。

### 4. 数据字典（DD）与结构化分析衔接

DFD 只回答"数据怎么流"，**数据字典**回答"数据是什么"。条目类型：
- **数据项**：最小数据单位，如"学号 = 10 位数字"；
- **数据结构**：由若干数据项/数据结构组成，如"学生 = 学号 + 姓名 + {课程成绩}"；
- **数据流**：描述流动数据的组成；
- **数据存储**：描述存储的数据组成及组织方式。

符号速记：\`+\` 顺序，\`|\` 选择，\`{}\` 重复（花括号内为重复部分，常注明重复次数），\`()\` 可选项。例如：订单 = 订单号 + 客户编号 + {商品编号 + 数量} + (备注)。

### 5. 结构化设计（从分析到设计）

分析阶段产出 DFD/DD 后，设计阶段用**结构图（SC）** 描述模块层次，用**变换型 / 事务型** 映射把 DFD 转成模块结构：
- **变换型 DFD**：逻辑输入 → 中心变换 → 逻辑输出，适合计算类；
- **事务型 DFD**：一条输入触发多条并行加工路径中的一条，适合"一笔业务分发到多种处理"的场景。
模块质量标准：**高内聚、低耦合**。耦合从弱到强：非直接、数据、标记、控制、外部、公共、内容；内聚从强到弱：功能、顺序、通信、过程、时间、逻辑、偶然。案例中若问"应如何改进模块划分"，标准答法是"降低控制耦合/公共耦合，提高功能内聚"。

## 二、关键法规/方法

本课题为方法论，重点掌握以下可操作检查表（案例作答时按项排查）：

**DFD 体检清单**
1. 是否有外部实体被当成加工（方框 vs 圆圈）？
2. 是否存在外部实体—外部实体、外部实体—数据存储、数据存储—数据存储的直连箭头？
3. 每个加工是否既有输入又有输出？
4. 父图加工的每条入/出数据流，是否都能在对应子图边界上找到？反之子图边界流是否都能在父图找到？
5. 数据流名字是否为名词性数据？
6. 加工命名是否为"动词+宾语"或"处理××"，避免空泛的"处理数据"？
7. 层次是否一次只分解一个复杂加工，避免在一张图上画出过多层细节？

**数据字典定义方法**
- 自顶向下逐层定义到数据项为止；
- 同一数据流在父图与子图中的定义必须一致（否则虽"图形平衡"但语义不一致）；
- 重复次数、可选项、取值范围（值域/约束）要写清。

**从 DFD 到 SC 的步骤**
1. 复核并确定 DFD 类型（变换型 / 事务型）；
2. 划分逻辑输入、中心加工、逻辑输出（或事务中心与各事务路径）；
3. 生成顶层模块与第一层控制模块；
4. 逐层分解，为每个模块写出简明功能说明；
5. 用耦合/内聚评价，必要时合并或拆分模块。

## 三、常考题型与解题方法

**题型1：图中找错（指出不当之处）。** 方法：按"体检清单"1～5 逐条对照，尤其盯紧直连箭头和父子接口。答题写清"错误现象 + 违反的规则"。

**题型2：补全元素（填外部实体、加工、数据存储、数据流）。** 方法：
- 看题干业务叙述里的**名词** → 可能是数据流或数据存储；
- 看"谁提供/谁接收" → 外部实体；
- 看"做何变换" → 加工；
- 先画出入子图边界缺的流（父图已有的），再补加工内部流。
**口诀：源宿画方框，变换画圆圈，留下来的是双杠。**

**题型3：说明父图子图是否平衡并改正。** 方法：列表对照父图加工的输入集合与子图全部输入集合，输出同理；不一致处写明"子图缺少××数据流"或"子图多出××"，并指出应删除或补上。注意：**子图内部加工之间的流不能算作子图对外接口**。

**题型4：写数据字典条目 / 判断定义是否完整。** 答题要写到数据项一层，检查 \`{}\` 与 \`()\` 是否误用，重复与顺序是否表达正确。

**题型5：DFD 转模块结构、评价耦合内聚。** 先判变换型/事务型，再套模板图；评价时点名耦合类型或内聚类型并给出改进方向。

**下午作答格式建议**：问题编号 → 一句话结论 → 规则或定义引用 → 修正后写法（补数据流写成"P1 → D2：入库单"）。

## 四、典型例题

**例1（找错）** 某"网上书店订单处理"0层图中出现：① 外部实体"客户"直接向数据存储"订单库"发"订单数据"；② 加工 P2"审核订单"只有输入"订单数据"，没有输出。请指出错误并改正。

**解析**
- ① 违反"外部实体不能与数据存储直接相连"。改正：将"订单数据"先送入加工 P1"接收订单"，再由 P1 写入"订单库"，或由 P1 校验后写入。
- ② 加工 P2 构成**黑洞**。改正：为 P2 增加输出，如"有效订单"（送 P3）或"无效反馈"（送客户），至少一条输出数据流。

**例2（平衡判断）** 父图中加工 P3"工资核算"的输入为"考勤数据"，输出为"工资明细"。其子图有输入"考勤数据""税率参数"，输出"工资明细""工资汇总表"。判断是否平衡。

**解析** 不平衡。子图多出输入"税率参数"和输出"工资汇总表"。若"税率参数"来自外部，应在父图 P3 上补画该输入；若来自本系统数据存储且仅为子图内部读取，则该流只能在子图内画到数据存储，不能作为子图对外输入。同理，"工资汇总表"若是对系统外的输出，父图 P3 应补该出边；否则应从子图对外接口中去掉（改为内部存储）。**判定原则：子图对外接口 = 父图该加工接口。**

**例3（填空）** 题干：教务员提交"成绩登记表"，系统校验后将合法成绩写入成绩文件，并给教务员"错误反馈"。DFD 中缺失数据流"合法成绩"，请写出其两端并命名。

**解析** 端点应为：加工"校验成绩"（或"登记成绩"）→ 数据存储"成绩文件"，数据流命名"合法成绩"。命名用名词短语；不要写"写入成绩"这种动词性控制语义。若问题要求指出"成绩登记表"的源，则外部实体为"教务员"。

## 五、易错点

1. **父子图"伪平衡"**：名字相同但语义/组成不同（数据字典不一致）仍算不平衡，要看题干定义。
2. **把子图内部流当成对外接口**，导致误判"多出数据流"或"缺少数据流"。
3. **数据流写成动词**："提交""查询""删除"是控制或动作，不是数据；应写"查询请求""删除确认单"等名词性数据。
4. **漏掉数据存储的读也算接口**：子图直接读全局库存文件，若父图该加工未画这条入边，在严格平衡定义下就是不平衡（依题意，若文件在子图层才定义，需在答案中说明处理方式）。
5. **外部实体双向都要画全**：只画请求不画响应，或反之。
6. **一号加工万能**：把整个业务塞进一个加工，未按事务/变换拆分，后续无法细化子图。
7. **忽略"灰色区域"**：题干说"查询不修改库"时，仍要有一条从数据存储到加工的入数据流。

## 六、考点提示

- 必背规则三条：**① 实体-实体/实体-存储/存储-存储不得直连；② 加工至少一入一出；③ 父子图对外接口一致**。
- 平衡判断口诀：**比边界，不比内部**。
- 命名纪律：数据流用名词，加工用动宾，数据存储用名词文件/表。
- 与数据库设计题的衔接：数据字典中"数据结构"常被要求映射为后续 E-R 实体属性，下午综合题可连着考。
- 复习动笔量：至少完整做 3 道补图 + 2 道平衡改正，直到能默画顶层/0层/1层关系。`,
    quizzes: [
      {
        id: 'sd-12-01-q1',
        type: 'choice',
        question: '关于数据流图，下列画法正确的是（ ）。',
        options: [
          '外部实体之间可以直接用数据流相连',
          '数据存储之间可以直接用数据流相连',
          '外部实体与数据存储之间不能直接相连，必须经过加工',
          '加工可以只有输出数据流而无输入数据流'
        ],
        answer: 'C',
        explanation: 'DFD规则规定外部实体之间、外部实体与数据存储之间、数据存储与数据存储之间均不得直接连数据流；加工至少要有一条输入和一条输出。'
      },
      {
        id: 'sd-12-01-q2',
        type: 'choice',
        question: '父图中加工 P1 的输入为“订货单”，输出为“发货单”。若其子图对外出现输入“库存查询结果”、输出“报表”，则该子图与父图（ ）。',
        options: [
          '平衡',
          '不平衡，子图缺少输入“订货单”',
          '不平衡，子图多出接口“库存查询结果”和“报表”',
          '不平衡，因为子图不应有数据存储'
        ],
        answer: 'C',
        explanation: '父子图平衡要求子图对外接口与父图被分解加工的接口完全一致。子图既缺“订货单/发货单”，又多出“库存查询结果/报表”，属不平衡。'
      },
      {
        id: 'sd-12-01-q3',
        type: 'choice',
        question: '某加工只有数据流入、没有数据流出，这种错误称为（ ）。',
        options: ['奇迹', '黑洞', '灰洞', '平衡'],
        answer: 'B',
        explanation: '只有入没有出是黑洞；只有出没有入是奇迹（无中生有）；输出无法由输入和存储数据变换得到是灰洞。'
      },
      {
        id: 'sd-12-01-q4',
        type: 'choice',
        question: '在数据字典中，符号“{}”通常表示（ ）。',
        options: ['顺序', '选择', '重复', '可选'],
        answer: 'C',
        explanation: '数据字典定义符：+表示顺序，|表示选择，{}表示重复，()表示可选。'
      },
    ],
    references: [],
  },
  {
    id: 'sd-12-02',
    moduleId: 'sd-case-study',
    title: '数据库与UML案例',
    description: '下午案例另一半：E-R/范式/SQL改错与UML补图。练实体联系识别、规范化判断、类图序列图要素填空。',
    content: `# 数据库与UML案例

## 考情分析

"系统分析与设计案例"下午题里，数据库设计与 UML 往往二选一或前后脚出现。数据库侧常见：补 E-R 图、说明联系类型（1:1 / 1:n / m:n）、判断最高范式并说明理由、给出（或修正）建表 SQL 与主外键、识别冗余与更新异常。UML 侧常见：补用例图关系（include/extend）、补类图多重度与关联类、读序列图/状态图/活动图填消息名或迁移条件。综合分值高、陷阱密，**必须练"写关键词得分"**，不要写大段白话。与上午选择题不同，下午要求写出规范术语：实体、属性、联系、外码、第一范式、泛化、关联、参与者。

## 一、核心概念

### 1. 概念结构设计：E-R 模型

- **实体（Entity）**：客观存在、可区分的事物，如"学生""课程"。在 E-R 图中用矩形表示。
- **属性（Attribute）**：实体所具有的特性，如学号、姓名。**码（Key）** 能唯一标识实体。用椭圆表示。
- **联系（Relationship）**：实体间的语义关联，如"选修""讲授"。用菱形表示。

**联系的度与类型**：
- 一对一（1:1）：班长—班级
- 一对多（1:n）：班级—学生（一个班多名学生，一名学生属一个班）
- 多对多（m:n）：学生—课程（选修）

m:n 联系转关系模式时，**必须单独转换为一张表**，其主码为两端实体主码的组合（或加上联系自身属性构成的标识）。1:1 与 1:n 可以把联系并入 n 端或任一端实体表，也可以单独成表。

属性分类：**简单属性 / 复合属性**（可再分，如姓名=姓+名）、**单值 / 多值**（一个人多个电话）、**派生属性**（可由其他属性算出，如年龄由出生日期推算，不必入库）、**码属性**。多值属性通常拆表或独立成实体。

### 2. 逻辑结构设计：关系模式与范式

- **第一范式（1NF）**：所有属性都是不可再分的原子值（无表中套表）。
- **第二范式（2NF）**：在 1NF 基础上，**不存在非主属性对候选码的部分函数依赖**（当候选码是单属性时自动满足 2NF）。
- **第三范式（3NF）**：在 2NF 基础上，**不存在非主属性对候选码的传递函数依赖**。
- **BCNF**：每个决定因素都包含候选码。

**异常**：插入异常、删除异常、更新（修改）异常、数据冗余。分解原则：**无损连接、保持函数依赖**（3NF 分解通常可同时做到；BCNF 分解可能不保持 FD）。

判断范式的三步法：
1. 写出函数依赖集，求候选码；
2. 看非主属性是否部分依赖于码 → 未达 2NF；
3. 看是否存在非主属性传递依赖 → 未达 3NF；
4. 看每个决定因素是否含码 → BCNF。

### 3. 物理设计与 SQL

必会句法骨架：
- 建表：CREATE TABLE（表名、列名、类型、PRIMARY KEY、FOREIGN KEY ... REFERENCES、UNIQUE、NOT NULL、CHECK、DEFAULT）；
- 索引：CREATE INDEX ... ON 表(列)；
- 查询：SELECT ... FROM ... WHERE ... GROUP BY ... HAVING ... ORDER BY ...
- 多表连接：JOIN ... ON / 逗号连接 + WHERE；
- 嵌套查询与 EXISTS/IN。

**完整性**：实体完整性（主码非空唯一）、参照完整性（外码取值要么空要么等于被参照主码值）、用户自定义完整性（CHECK、NOT NULL）。案例常考"删除父表记录时子表外码怎么办"——RESTRICT/NO ACTION、CASCADE、SET NULL 三种策略的选用。

**视图 vs 基表**：视图是虚表，利于安全与逻辑独立；复杂视图常不可更新。

### 4. UML 核心图（案例高频）

| 图 | 用途 | 案例常见填空 |
|----|------|-------------|
| 用例图 | 功能边界、参与者与用例 | 参与者名、用例名、include/extend/泛化箭头 |
| 类图 | 静态结构 | 类名、属性/操作、关联名、多重度（0..1, 1, 0..*, 1..*）、聚合/组合菱形、泛化空三角 |
| 对象图 | 某时刻快照 | 对象名:类名 |
| 序列图 | 按时间的消息交互 | 消息名、生命线、激活条、alt/loop 框 |
| 状态图 | 单对象生命周期 | 状态名、迁移事件[守卫]/动作 |
| 活动图 | 业务/算法流程 | 动作、判断节点、分叉汇合、泳道 |

**用例关系**：
- \`<<include>>\`：基用例**必然**执行被包含用例（箭头指向被包含者），如"下单"包含"校验库存"；
- \`<<extend>>\`：**可选**扩展，满足扩展条件才执行，如"支付"扩展"使用优惠券"；
- 用例泛化：子用例继承父用例行为并可特化。

**类图关系强度**（从强到弱）：组合（实心菱形，部分随整体生命周期）→ 聚合（空心菱形，可独立存在）→ 关联（普通箭头/连线）→ 依赖（虚线箭头，临时使用）。**泛化**是空心三角箭头，表示继承。

**多重度写法**：1、0..1、1..*、*、m..n。案例里"一个订单包含 1..* 个订单行，一个订单行只属于 1 个订单"就是 1:n 的两端多重度：订单 1 对订单行 1..*；订单行 1 对订单 1。

### 5. 从需求到模型（案例推理链）

需求陈述 → 识别参与者与用例 → 识别实体类与属性 → 定关联与多重度 → 为有状态的对象画状态图 → 为关键场景画序列图。案例题常常给出类图半成品，要求补关联/多重度；或给序列图缺消息，要求按说明补全。

## 二、关键法规/方法

（本课题方法论）**数据库案例作答规范**

1. **E-R 转关系模式的规则**
   - 每个实体转一张表，属性即列，码即主码；
   - 1:1 联系可并入任一实体表（加入对方主码作外码 + 联系属性）；
   - 1:n 联系可并入 n 端表（把 1 端主码作 n 端表的外码）；
   - m:n 联系必须新建表，主码 = 两端主码组合，并加联系属性；
   - 多值属性新建表，主码 = 实体主码 + 多值属性。
2. **命名与完整性书写模板**：\`学生(学号, 姓名, 出生日期, 班级号)\`，主码学号，外码班级号参照班级(班级号)。
3. **SQL 纠错时按四查**：关键字/类型、主外键与参照、连接条件、聚集与分组（GROUP BY 列是否被 SELECT 正确使用）。
4. **范式题的说理模板**：给出候选码 → 指出某函数依赖 → 说明它是部分/传递依赖 → 结论"最高达到 X 范式" → 建议分解方法（无损连接）。

**UML 作答规范**
1. 关系名写在连线上；多重度写在靠近目标类的一端（表示一端实例能关联到多少个目标实例）。
2. include/extend 箭头方向要写对：由基用例指向被包含/被扩展用例（教材惯例以被指向者为公共用例）。
3. 序列图消息自上而下按时间；同步实线实心三角，返回虚线。
4. 状态图迁移标注格式：事件名 [守卫条件] / 动作，三者可省略但语义要全。

## 三、常考题型与解题方法

**题型A1：补 E-R 图 / 判定联系类型。**
方法：看需求中"每个 A 可以对应几个 B"的双向计数句。出现"多对多""彼此都是多个"→ m:n；出现"必须且只能属于一个"→ 1:n 的 n 端。案例填空常漏掉联系上的属性（如"选修"上的"成绩"）——**m:n 联系的属性永远跟着联系表走**。

**题型A2：转关系模式并标主外键。**
方法：按"二、1"规则机械转换；m:n 不要并入两端。主码字段下划线、外码写 REFERENCES。若问"是否要新建表"，m:n 一定新建。

**题型A3：判断范式并分解。**
方法：三步法（码 → 部分依赖 → 传递依赖）。分解时把依赖右侧并入新表。答案结构：**结论（2NF/3NF）+ 依赖证据 + 分解后模式**。

**题型A4：SQL 填空/改错。**
方法：先读业务语句翻译成英文 FROM 表清单，再写 WHERE 连接与过滤，最后 SELECT 列。改错注意别名、HAVING 与 WHERE 分工（WHERE 过滤行、HAVING 过滤组）。

**题型B1：用例图补参与者与关系。**
方法：参与者是**系统外**的人/系统/硬件，写"客户""支付网关"；系统内模块不是参与者。必然复用 → include；条件可选 → extend；行为一般化 → 用例泛化。

**题型B2：类图补多重度/关系类型。**
方法：用"每个 A 对应几个 B"双向填 1 或 *。整体销毁是否带走部分 → 组合 vs 聚合。类对类"使用了"但生命周期不共享 → 依赖虚线。

**题型B3：序列图/状态图填空。**
方法：按题干场景句顺序对应消息；先分清对象生命线。状态图按"初态—中间状态—终态"补齐，迁移事件用题干动词（如"提交订单""支付成功"）。

**下午答题总原则**：名词用 UML/数据库规范术语；每问给结论再给依据；画图题用文字描述位置关系也可以得分（考试作答要文字明确端点与箭头方向）。

## 四、典型例题

**例1（联系与转换）** 需求：每个项目可有多名员工参与，每名员工可参与多个项目；参与时需记录“岗位”和“投入工时”。请说明员工与项目的联系类型，并写出对应关系模式。

**解析** 双向都是"多个"，联系"参与"是 m:n，联系属性为岗位、投入工时。关系模式：
- 员工(工号, 姓名, ...) 主码：工号
- 项目(项目号, 项目名, ...) 主码：项目号
- 参与(工号, 项目号, 岗位, 投入工时) 主码：(工号, 项目号)，外码：工号参照员工，项目号参照项目
必须新建"参与"表；若把岗位直接并入员工表则错误（一名员工在不同项目岗位不同）。

**例2（范式）** 关系模式 学生选课 (学号, 课程号, 姓名, 系名, 系主任, 成绩)，函数依赖：学号→姓名,学号→系名,系名→系主任,(学号,课程号)→成绩。判断最高范式并说明理由。

**解析** 候选码为 (学号, 课程号)。
- 非主属性姓名、系名**部分函数依赖**于候选码（只依赖学号），故不满足 2NF；
- 此外系名→系主任，而系名由学号决定，存在传递依赖线索。
最高达到 **1NF**。分解为：学生(学号,姓名,系名)、系(系名,系主任)、选课(学号,课程号,成绩)。分解后无损连接且消除部分/传递依赖。

**例3（UML）** 说明：客户下单时必须校验库存；仅当订单金额超过 5000 元时需要经理审批。用例图中应如何表示“校验库存”和“经理审批”与“下单”的关系？

**解析** “校验库存”是下单流程中**必然**执行的公共步骤，用 \`<<include>>\`，由用例“下单”指向“校验库存”。“经理审批”是满足条件（金额>5000）才发生的**可选**扩展，用 \`<<extend>>\`，由“经理审批”指向“下单”，并在扩展关系上注明扩展点或条件"金额>5000"。参与者：客户、（审批场景下的）经理。注意 include 是无条件复用，extend 是有条件、可选，两者方向与条件要写明。

## 五、易错点

1. **m:n 联系并入实体表**：丢失语义并产生插入/删除异常，必须独立成表。
2. **联系属性放错位置**："成绩"属于"选修"联系，不属于学生也不属于课程。
3. **范式判断不写候选码**：没有码就无法判定部分/传递依赖，直接扣分析分。
4. **把 2NF/3NF 条件背反**：2NF 反的是"部分依赖"，3NF 反的是"传递依赖"。
5. **include 与 extend 用反**：必然包含用 include，条件扩展用 extend；箭头方向也要对应。
6. **参与者误写成系统内部类**：如把"订单处理器"画成参与者——它是系统内加工/类。
7. **多重度写在错的一端**：多重度标在"被看到多少个"的那一端。
8. **组合与聚合混用**：订单—订单行是组合（订单没了行无意义）；部门—员工常为聚合（员工可调动）。
9. **SQL 中 WHERE 写聚合条件**：对 COUNT/SUM 的过滤必须放 HAVING。
10. **序列图只写消息名不写接收对象**：填空两端对象生命线同样给分点。

## 六、考点提示

- 记忆优先级：**m:n 转表规则 > 范式三步法 > include/extend > 多重度 > 组合/聚合 > 完整性与外码动作**。
- 数字与符号：多重度 1、0..1、*、1..*；关系强度组合>聚合>关联>依赖。
- 下午作答模板：结论术语 + 规则一句 + 模式/关系写出（主码用下划线或文字标明）。
- 与模块 12-01 的衔接：数据字典中的"数据结构"常成为 E-R 属性来源；一题大题可能 DFD + 数据库连出 5 问，要合理分配时间，先拿规则分。
- 动笔量建议：范式分解 3 道、E-R 转表 3 道、用例/类图补全各 2 道，对照标准答案核术语。`,
    quizzes: [
      {
        id: 'sd-12-02-q1',
        type: 'choice',
        question: '若“学生”与“课程”是多对多联系，且联系上有属性“成绩”，则转换为关系模式时正确做法是（ ）。',
        options: [
          '把成绩加入学生表',
          '把成绩加入课程表',
          '为选修联系单独建表，主码为(学号,课程号)，成绩作为该表属性',
          '取消成绩属性，改为在两张表中各存一份'
        ],
        answer: 'C',
        explanation: '多对多联系必须转换为独立关系模式，属性跟联系走；主码取两端实体主码组合。'
      },
      {
        id: 'sd-12-02-q2',
        type: 'choice',
        question: '关系模式 R(A,B,C,D)，函数依赖 AB→C, A→D，则 R 最高达到（ ）。',
        options: ['1NF', '2NF', '3NF', 'BCNF'],
        answer: 'A',
        explanation: '候选码为AB。D只依赖A，存在非主属性D对码AB的部分函数依赖，故不满足2NF，最高1NF。'
      },
      {
        id: 'sd-12-02-q3',
        type: 'choice',
        question: '“下单”用例每次执行都必须进行“校验库存”，二者应使用（ ）关系。',
        options: ['extend', 'include', '泛化', '依赖'],
        answer: 'B',
        explanation: '必然复用的公共子用例用include；满足条件才可选执行的用extend。'
      },
      {
        id: 'sd-12-02-q4',
        type: 'choice',
        question: '在类图中，表示“部分随整体销毁”的强组合关系使用（ ）。',
        options: ['空心菱形（聚合）', '实心菱形（组合）', '空心三角（泛化）', '虚线箭头（依赖）'],
        answer: 'B',
        explanation: '组合用实心菱形，部分不能脱离整体独立存在；聚合为空心菱形，部分可独立。'
      },
    ],
    references: [],
  },
];
