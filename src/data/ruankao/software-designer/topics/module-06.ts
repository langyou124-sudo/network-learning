import { Topic } from '@/types';

export const topics: Topic[] = [
  {
    id: 'sd-06-01',
    moduleId: 'sd-structured-method',
    title: '结构化分析',
    description: 'SA数据字典与DFD建模、分层细化与父图平衡、加工小说明、ER图与状态图、数据流图错误辨析',
    content: `# 结构化分析

## 考情分析

结构化分析（Structured Analysis，SA）是软件设计师考试的经典考点，几乎每年上午必有一道数据流图（DFD）相关选择题，下午案例更是常以“补全数据流图/指出图中错误”形式出现。核心考点集中在：SA 的三类模型与工具、DFD 的基本成分与分层原则、**父图与子图的平衡**、数据字典条目定义、加工小说明（决策表/决策树/结构化语言）、数据流图常见错误类型。会读图、会查平衡、会数成分，就能拿下大部分分值。

## 一、核心概念

### 1. 结构化方法概述

结构化方法由结构化分析（SA）、结构化设计（SD）、结构化程序设计（SP）组成，贯穿“分析—设计—实现”，核心思想是：**自顶向下、逐步求精、模块化**，用抽象与分解控制复杂度。其中 SA 面向数据流与数据字典，建立系统逻辑模型，回答“系统必须做什么”，不涉及“怎么做”。

SA 建立的三类模型（常称“三套图/表”）：

| 模型类型 | 核心工具 | 描述对象 |
|----------|----------|----------|
| 功能模型 | 数据流图 DFD | 数据在系统中的流动、变换与存储 |
| 数据模型 | 实体-联系图 ERD | 现实世界实体及其联系（用于数据库设计输入） |
| 行为模型 | 状态-迁移图 STD | 对象状态随事件发生的迁移过程 |

辅助说明工具：**数据字典（DD）** 定义所有数据；**小说明（加工逻辑说明）** 用结构化语言、判定表或判定树描述加工“做什么”。

### 2. 数据流图（DFD）基本成分

DFD 有四种基本图形成分（不同教材符号略有差异，含义一致）：

1. **数据流（Data Flow）**：由一组固定成分的数据组成，表示数据的流向，用箭头/有名箭线表示。必须有名字，名字应是名词或名词短语（如“学生成绩单”），且**数据流只能在加工之间、加工与数据存储之间、加工与外部实体之间流动**，不能从外部实体直接流向数据存储（外部实体与存储必须经过加工）。
2. **加工/处理（Process）**：对数据进行的变换，用圆/圆角矩形表示，命名为“动宾短语”（如“计算成绩总分”）。父图中的加工可以进一步分解为子图。
3. **数据存储/文件（Data Store）**：处于静止状态的数据集合（如“成绩表”），用双杠/开口矩形表示。
4. **外部实体/源/宿（Source/Sink）**：系统之外的数据来源或去向（如“教务处”“学生”），用方框表示。外部实体不属于系统加工内容，只能出现在最外层图中。

**分层 DFD 与平衡**：
- 顶层图（环境图）只有 1 个加工，表示整个系统与外界的数据交换。
- 中间层、底层逐层细化，任一加工都可分解为一张子图。
- **父图与子图必须平衡**：子图的输入输出数据流必须与父图中该加工的输入输出数据流**在数量与内容上一致**（子图可以使用父图未画出的内部数据流，但进出边界必须对应）。
- 分层不能出现“断层”“黑洞”“奇迹”等错误（见易错点）。

### 3. 数据字典（DD）

数据字典对 DFD 中出现的所有数据元素给出严格定义，是 DFD 的必要补充。典型定义符号（结构化方式）：

- \`=\` 表示“等价于/定义为”
- \`+\` 表示“连接”
- \`[ | | ]\` 表示“或”（从括号中多项选一项）
- \`{ }\` 表示“重复”
- \`( )\` 表示“可选”

例：\`学生成绩单 = 学号 + 姓名 + {课程号 + 成绩}\` 表示成绩单含学号、姓名及若干组课程号与成绩。
例：\`学号 = 8{数字}8\`（或写为“8 位数字”）表示学号由 8 位数字组成。

### 4. 加工小说明

对 DFD 中每个不再分解的基本加工，用小说明精确描述其逻辑，三种常用形式：

| 形式 | 适用场景 | 特点 |
|------|----------|------|
| **结构化语言** | 顺序加工、有一定分支但不复杂 | 类自然语言，用 IF-THEN-ELSE、CASE、REPEAT 等控制结构 |
| **判定表（决策表）** | 条件组合多、加工逻辑复杂、条件与动作有约束 | 条件区 + 动作区，列=规则，一目了然处理组合 |
| **判定树（决策树）** | 条件较少、分支清晰、便于用户理解 | 树状分支表示条件取值与对应动作，直观易读 |

选择口诀：**条件组合多用判定表，分支直观用判定树，顺序叙述用结构化语言**。

### 5. 实体-联系图与状态-迁移图

- **ERD**：实体（矩形）、属性（椭圆）、联系（菱形）。联系基数有 1:1、1:N、M:N。是概念数据模型，为数据库设计做准备。
- **STD**：状态（圆角框）+ 迁移（箭头）+ 事件/条件。适合描述有复杂状态变化的对象（如订单、进程）。

## 二、关键原理/方法

### DFD 构造方法与步骤

1. 确定系统边界与外部实体（源与宿）。
2. 画顶层图：一个加工“××系统”，标出与实体间的主要输入输出数据流。
3. 逐层分解加工：对复杂加工继续拆分为子加工，细化到“基本加工”（能用小说明清晰描述、不必再分解）。
4. 每细化一层都检查父子平衡。
5. 为所有数据流、文件命名，并在数据字典中定义。
6. 为基本加工写小说明。

抽象与分解是控制复杂度的两把刀：分解降低宽度，抽象控制深度。

### 父子平衡判定法（下午题必备）

比较父图中某加工 P 的输入输出数据流集合，与 P 展开后的子图边界数据流集合：
- 子图缺了父图有的一条入边/出边 → **不完整/不平衡**（可能漏画）。
- 子图多出一条与父图无关的边界流 → **不平衡**。
- 内部加工之间的数据流不必出现在父图。
- 同名数据流内容必须一致（数据字典约束）。

### 数据流图的“三错一漏”检查清单

1. **黑洞**：加工只有输入没有输出（数据进去后“消失”），违背数据守恒。
2. **奇迹**：加工只有输出没有输入（凭空产生数据），违背数据守恒。
3. **断层/灰洞**（不完备）：输入不足以产生输出，或输出中的部分数据未在输入/存储中出现（定义不完整）。
4. **不守恒/不平衡**：父图与子图的边界数据流不一致。
5. 其他常见错：数据流无名字；两个加工之间无数据存储却直接共享数据而未画数据流；外部实体之间直接连数据流；数据存储未被任何加工读写；数据流命名用动词等。

### SA 建模与需求规格的关系

SA 产出的逻辑模型应写入 SRS 的功能与数据描述部分。SA 只建“做什么”的逻辑模型，不决定模块结构（那是结构化设计 SD 的事）。从 DFD 到模块结构的转换属于设计阶段（变换型/事务型映射，见《结构化设计》）。

### 与面向对象分析的对比

| 维度 | 结构化分析 SA | 面向对象分析 OOA |
|------|---------------|------------------|
| 出发点 | 数据流（功能分解） | 对象与类 |
| 核心图 | DFD、DD、ER、STD | 用例图、类图、顺序图、状态图 |
| 分解方式 | 功能自顶向下分解 | 对象封装与职责分配 |
| 对变化适应 | 功能变化易牵动多图 | 对象封装，局部修改影响小 |

## 三、常考题型与解题方法

1. **成分识别题**：给出符号或描述，问是数据流/加工/存储/外部实体。技巧：看“是否变换数据”“是否系统外”“是否静止数据”。
2. **父子平衡题**：数边界数据流。方法：先列表父图 P 的入/出流，再列表子图边界入/出流，作集合对比。
3. **错误指认题**（下午）：用检查清单扫描——黑洞？奇迹？无名流？实体-存储直连？父子不守恒？数据字典未定义？
4. **数据字典展开题**：根据定义式写出某数据项的组成，或判断某具体值是否合法。
5. **小说明形式选择题**：条件组合多→判定表；直观分支→判定树；顺序加工→结构化语言。
6. **图类型匹配题**：描述状态迁移→STD；描述实体联系→ERD；描述数据加工→DFD。

解题流程：先读题干确定边界 → 画/查顶层 → 核对分层与平衡 → 查守恒 → 查命名与字典 → 再答具体小问。

## 四、典型例题

**例1** 在分层数据流图中，父图与子图的平衡是指（ ）。
A. 子图中加工的个数等于父图中该加工的编号数
B. 子图的输入输出数据流与父图中该加工的输入输出数据流一致
C. 子图中数据存储的个数与父图相同
D. 子图中外部实体与父图中外部实体完全相同

**解析**：平衡原则约束的是**边界数据流**：展开后的子图其输入输出数据流，必须与父图中对应加工的输入输出数据流在数量和内容上保持一致。子图内部的加工、存储可以比父图更丰富，外部实体通常只出现在顶层/环境图。**答案 B**。

**例2** 某加工“计算奖学金”需要根据学生综合成绩、出勤率、是否贫困生等多个条件的组合来确定奖学金等级与金额。描述该加工逻辑最合适的工具是（ ）。
A. 结构化语言
B. 判定树
C. 判定表
D. 实体-联系图

**解析**：多个条件存在组合取值、对应不同动作，用判定表（决策表）能把条件组合与动作一一列出，避免遗漏。判定树适合条件少、分支直观的情形；结构化语言适合以顺序为主的加工；ERD 是数据建模工具，不描述加工逻辑。**答案 C**。

**例3** 在数据流图中，一个加工只有数据输入而没有任何数据输出，这种错误被称为（ ）。
A. 奇迹
B. 黑洞
C. 断层
D. 不平衡

**解析**：只有输入没有输出是“黑洞”（数据被吞掉）；只有输出没有输入是“奇迹”（数据凭空产生）。两者都违背数据守恒。不平衡专指父子图边界数据流不一致。**答案 B**。

## 五、易错点

1. **把数据流当成控制流**：DFD 只表达数据及其变换，不表达控制时序。箭头上应是数据名词，不是“开始计算”这类控制命令。
2. **外部实体之间直接相连**：源到宿若要传递数据，必须经过加工；实体与数据存储之间也必须经过加工。
3. **父子图不平衡却以为“内部细化无所谓”**：内部细化确实自由，但**边界流必须一致**，这是硬约束。
4. **混淆黑洞与奇迹**：无输出=黑洞；无输入=奇迹。口诀：**黑吞光（无出），奇虚张（无入）**。
5. **数据字典定义式符号用错**：\`+\` 是顺序连接，\`[ | | ]\` 是多选一，\`{ }\` 是重复，\`( )\` 是可选。把“或”写成“+”是常见错误。
6. **在 SA 阶段设计模块结构与算法**：SA 只到逻辑模型；模块划分、调用关系、算法是 SD/DD 的内容。
7. **加工命名不当**：加工应为动宾短语（“审核订单”）；数据流/存储应为名词（“订单信息”）。把加工命名成名词会使图无法理解其变换含义。
8. **基本加工无小说明**：DFD 到底后必须配小说明，否则加工含义不精确，属于文档不完备。

## 六、考点提示

- 必背：DFD 四成分含义；父子平衡定义；黑洞/奇迹定义；数据字典符号（= + [ ] { }）；三种小说明适用场景。
- 下午 DFD 题答题套路：①按题干补齐缺画的数据流（注意起止点必须落在正确成分上）；②指出并改正错误（写明错误类型+改正）；③若考字典，按定义式逐项展开。
- 检查顺序建议：先查外部实体与边界 → 再查父子平衡 → 再查每个加工数据守恒（黑/奇）→ 最后查命名与字典完整性。
- SA 三件套口诀：**功能 DFD、数据 ER、行为 STD，字典小说明来兜底**。`,
    quizzes: [
      {
        id: 'sd-06-01-q1',
        type: 'choice',
        question: '数据流图中，外部实体之间的数据传递（ ）。',
        options: ['可以直接用数据流连接', '必须经过加工进行处理或转换', '必须通过数据存储中转', '可以用控制流连接'],
        answer: 'B',
        explanation: '外部实体是系统外的源或宿，二者若交换数据，必须经过系统内加工（表示系统确实参与了处理），不能直接相连。数据存储是系统内静止数据，也不允许与外部实体直接交换数据。'
      },
      {
        id: 'sd-06-01-q2',
        type: 'choice',
        question: '数据字典定义式 “订货单 = 订单号 + 客户名 + {产品号 + 数量}” 中，花括号的含义是（ ）。',
        options: ['括号内项目可选', '括号内项目重复多次', '括号内项目必选其一', '括号内项目同时出现'],
        answer: 'B',
        explanation: '数据字典中 { } 表示重复（可出现 0 次或多次）；( ) 表示可选；[ | | ] 表示从多项中选一项；+ 表示顺序连接。该定义表示一张订货单包含订单号、客户名及若干组产品号与数量。'
      },
      {
        id: 'sd-06-01-q3',
        type: 'choice',
        question: '某加工的条件组合情况较多，且条件与执行动作之间约束复杂，描述其加工逻辑宜采用（ ）。',
        options: ['判定树', '判定表', '结构化语言', '实体-联系图'],
        answer: 'B',
        explanation: '判定表适合条件多、组合复杂、条件与动作有约束的加工逻辑，能避免遗漏；判定树适合条件少、分支直观；结构化语言适合以顺序叙述为主的加工；ER 图是数据建模工具。'
      },
    ],
    references: [],
  },
  {
    id: 'sd-06-02',
    moduleId: 'sd-structured-method',
    title: '结构化设计',
    description: 'SD总体/详细设计、模块结构图MSC、耦合与内聚强弱排序、变换型与事务型映射、模块独立性度量',
    content: `# 结构化设计

## 考情分析

结构化设计（Structured Design，SD）是软件设计师考试中“模块独立性”相关考点的唯一主要出处，上午题几乎年年考**耦合与内聚的强弱排序或实例辨析**，偶尔考变换型/事务型数据流图到模块结构图的映射、模块作用域与控制域、扇入扇出。下午案例则可能要求“根据 DFD 给出软件结构图”或“指出模块耦合过高的原因并改进”。把耦合 7 级、内聚 7 级这两条链背准、能对号入座，本课大半分值可得。

## 一、核心概念

### 1. 结构化设计的任务划分

- **总体设计（概要设计）**：将软件需求转化为**软件体系结构**，确定模块划分、模块间调用与数据传递关系，产出模块结构图（MSC）与概要设计说明书。重点是“如何组织模块”。
- **详细设计**：对每个模块给出其算法与数据结构的精确描述（程序流程图、N-S 图、PAD、PDL 伪码等），产出详细设计说明书。重点是“模块内部怎么做”。

总体设计常用的启发式规则：改进软件结构提高模块独立性；模块规模适中（如 50～100 行）；深度、宽度、扇出、扇入适中；模块作用域应在控制域内；降低模块接口复杂度；设计单入口单出口模块；模块功能可预测。

### 2. 模块结构图（MSC）

模块结构图用五种成分表示软件结构：
1. **模块**：具有一定功能的矩形框，内写模块名。
2. **调用关系**：上层模块调用下层模块的箭头/连线。
3. **数据传递**：模块间传递的数据（带名字的箭头，常画成空心圆圈连出的短线）。
4. **控制信息传递**：传递标志、开关等控制信息（实心圆）。
5. **转接符号**：结构图过大时的转接，保证图面清晰。

常见结构形态：
- **变换型**：数据流呈“输入—变换中心—输出”，映射为“输入模块—变换模块—输出模块”的三层或多层控制结构。
- **事务型**：数据流经过“事务中心”分发到多条平行加工路径，映射为“事务调度模块 + 各动作模块”的辐射结构。
- **混合型**：变换型与事务型混合，先按事务分解，再对各分支按变换分解（或相反）。

### 3. 耦合与内聚（核心必背）

**模块独立性**由两个定性指标衡量：**内聚**（模块内部各元素彼此结合的紧密程度）与**耦合**（模块之间相互连接的紧密程度）。目标：**高内聚、低耦合**。

**耦合由弱（好）到强（坏）七级**：

| 顺序 | 耦合类型 | 含义 | 独立性 |
|------|----------|------|--------|
| 1 | 非直接耦合 | 两模块无直接信息传递，各自独立 | 最好 |
| 2 | 数据耦合 | 通过参数传递简单数据项 | 好 |
| 3 | 标记耦合 | 通过参数传递记录/数据结构（标记） | 较好 |
| 4 | 控制耦合 | 传递控制信息（如标志位、开关量），影响对方内部逻辑 | 较差 |
| 5 | 外部耦合 | 一组模块都访问同一全局数据/外部格式 | 差 |
| 6 | 公共耦合 | 一组模块共享同一公共数据区（全局变量区） | 很差 |
| 7 | 内容耦合 | 直接访问对方内部（转入对方内部、修改对方数据、多入口重叠代码） | 最坏 |

口诀：**非数标控外公内**（由弱到强）。

**内聚由弱（坏）到强（好）七级**：

| 顺序 | 内聚类型 | 含义 | 例子 |
|------|----------|------|------|
| 1 | 偶然内聚 | 模块内各部分无实质联系，仅因巧合放在一起 | 把几个模块都用到的 5 行代码抽成“公共模块” |
| 2 | 逻辑内聚 | 把若干逻辑上相似的功能放在一起，由调用者传参决定做哪一种 | “输入输出模块”混写多种设备操作 |
| 3 | 时间内聚 | 在同一时间段执行的各部分放在一起 | 初始化模块：清寄存器、开中断、置默认值 |
| 4 | 过程内聚 | 处理成分相关，按特定次序执行 | 某流程按固定步骤依次处理 |
| 5 | 通信内聚 | 各部分都使用**同一输入数据或产生同一输出数据** | 同一数据先计算再打印 |
| 6 | 顺序内聚 | 各成分有顺序关系，前一成分输出是后一成分输入 | 先语法分析后语义分析 |
| 7 | 功能内聚 | 模块内各成分共同完成一个**单一、完整**的功能 | “计算平方根”模块 |

口诀：**偶逻时过通顺功**（由弱到强）。

### 4. 模块的扇入、扇出、作用域与控制域

- **扇出**：一个模块直接调用的下级模块数。过大说明模块任务过杂（上帝模块），过小说明深度过深、划分过细。一般 3～4 为宜，上限 5～9。
- **扇入**：直接调用该模块的上级模块数。扇入大说明模块复用好，但过高可能功能过于宽泛。
- **作用域**：受该模块内判定影响的所有模块集合。
- **控制域**：该模块及其全部下级模块的集合。
- **规则**：**作用域应落在控制域内**；否则判定影响跨层传递，必然引入控制耦合或公共耦合。

### 5. 详细设计表达工具

| 工具 | 特点 |
|------|------|
| 程序流程图 | 直观，但箭头随意，易写出非结构化流程 |
| 盒图（N-S 图） | 强制结构化，无箭头，三种基本控制结构 |
| PAD 图 | 二维树形，支持自顶向下逐步求精，易转程序 |
| 判定表/判定树 | 复杂条件组合逻辑 |
| PDL（伪码） | 近自然语言的过程设计语言，便于注释与自动转换 |

## 二、关键原理/方法

### 从数据流图导出模块结构图

**变换型映射步骤**：
1. 确定输入、变换中心、输出三部分（找主加工）；
2. 设计顶层与第一层模块（输入、变换、输出）；
3. 自顶向下细化各分支，直到基本模块；
4. 用启发式规则优化（提高内聚、降低耦合、控制扇出等）。

**事务型映射步骤**：
1. 找出事务中心与各动作路径；
2. 设计事务调度模块（顶层）；
3. 为每条路径设计动作模块并细化；
4. 优化。

**混合数据流**：通常先区分事务型，把每条事务路径再按变换型分解。

### 提高模块独立性的改写手法

- 控制耦合 → 改成数据耦合：把“做 A 还是做 B”的标志下放到调用者，由调用者分别调用“做 A”或“做 B”模块，或拆分逻辑内聚模块为功能内聚模块。
- 公共耦合/外部耦合 → 尽量改为参数化数据传递，限制全局数据范围。
- 内容耦合 → 必须消除，禁止跨模块修改、跳入。
- 时间/逻辑内聚 → 按功能或按数据联系重组为顺序/通信/功能内聚。
- 作用域不在控制域 → 上移判定到能覆盖所有受影响模块的上层，或重组结构。

### 启发式规则与结构优化评价

好的软件结构通常满足：
1. 模块独立性高（高内聚低耦合）；
2. 规模适中，单入口单出口；
3. 顶层扇出较高，中层扇出较少，底层高扇入；
4. 作用域在控制域内；
5. 接口简单清晰，数据传递尽量少且类型简单；
6. 深度宽度适中，功能可预测（对相同输入必产生相同结果，不含隐藏状态副作用）。

## 三、常考题型与解题方法

1. **耦合/内聚排序题**：直接默写两条七级链，注意题目问的是“由强到弱”还是“由弱到强”，以及是问耦合还是内聚。最弱耦合=非直接，最坏耦合=内容；最弱内聚=偶然，最强内聚=功能。
2. **实例归属题**：读一小段模块描述，判断耦合或内聚类型。技巧：
   - 看“模块之间怎么传” → 耦合（简单数据=数据耦合；记录结构=标记；开关标志=控制；全局区=公共/外部；闯入内部=内容）。
   - 看“模块内部为何合在一起” → 内聚（巧合=偶然；同类操作=逻辑；同时执行=时间；次序过程=过程；同数据=通信；前后产出=顺序；单一完整功能=功能）。
3. **结构图映射题**：先判数据流是变换型还是事务型，再按映射步骤给结构。
4. **作用域/控制域题**：画调用树，标出判定所在模块与受影响模块，检查是否都在判定模块的子树内。
5. **优化改写题**（下午）：指出当前是何种不良耦合/内聚 → 说明为何不好 → 给出改造后的结构（拆模块、改参数、上移判定）。

解题口诀：**弱到强“非数标控外公内”“偶逻时过通顺功”；见到标志传控制，见到全局是公共，见到跳转是内容**。

## 四、典型例题

**例1** 模块间通过传递开关量（控制信息）影响对方内部执行逻辑，这种耦合称为（ ）。
A. 数据耦合
B. 标记耦合
C. 控制耦合
D. 公共耦合

**解析**：数据耦合传递简单数据项；标记耦合传递记录等复合数据结构；控制耦合传递开关量、标志位等**控制信息**，接收方根据该信息决定内部逻辑分支，模块独立性较差；公共耦合是共享全局数据区。**答案 C**。

**例2** 下列几种内聚类型中，内聚程度最强（最好）的是（ ）。
A. 逻辑内聚
B. 通信内聚
C. 顺序内聚
D. 功能内聚

**解析**：内聚由弱到强为：偶然、逻辑、时间、过程、通信、顺序、功能。功能内聚指模块内各成分协同完成一个单一完整的功能，是内聚性最好的一类。**答案 D**。

**例3** 某模块 A 的内部判定会影响非其直接或间接下属的模块 B 的执行，这违反了（ ）。
A. 作用域应是控制域的子集
B. 模块应单入口单出口
C. 应尽量使用数据耦合
D. 模块扇入应尽可能大

**解析**：模块的作用域指受其判定影响的模块集合，控制域指该模块及其全部下属。设计规则要求作用域落在控制域之内。若判定影响到控制域之外的模块 B，说明控制信息跨层传播，必然引入较差的耦合，应通过上移判定或重组结构来修正。**答案 A**。

## 五、易错点

1. **耦合与内聚强弱方向记反**：考试问“由强到弱”时，答案顺序应是内容→…→非直接（耦合）、功能→…→偶然（内聚）。先看清楚题干方向再排序。
2. **把逻辑内聚当成高内聚**：逻辑内聚只是“功能相似被塞在一起”，调用者还要传参挑选，属于弱内聚，仅次于偶然内聚。
3. **把数据耦合与标记耦合弄混**：传简单数据项（整数、字符串）是数据耦合；传数组、记录、结构体是标记耦合。标记耦合略差于数据耦合。
4. **把控制耦合说成数据耦合**：只要参数里含“标志位/开关/类型码”用来影响对方内部流程，就是控制耦合。
5. **公共耦合与外部耦合**：共享**同一块公共数据区**（全局变量区）是公共耦合；共享外部格式、协议、I/O 设备等外部约束是外部耦合。公共耦合通常更差。
6. **认为扇入越大越好**：过高扇入可能是“上帝模块”或功能宽泛的杂烩，应结合内聚判断。
7. **详细设计工具张冠李戴**：N-S 图强调无 GOTO 的结构化；PAD 图便于逐步求精；PDL 是伪码。程序流程图可以表达任意跳转，结构化最弱。
8. **在 SD 阶段画 DFD**：DFD 是分析（SA）产物；设计产物是模块结构图 MSC 与接口/算法说明。

## 六、考点提示

- 上午题两条七级链必须滚瓜烂熟，并能给出实例归属（本课表格中的例子就是命题素材）。
- 常考最优组合：**数据耦合 + 功能内聚**（或非直接耦合 + 功能内聚）为模块独立性最理想状态。
- 启发式规则中“作用域在控制域内”“顶层高扇出、底层高扇入、中层少扇出”是结构性高频判断点。
- 下午题若考结构图，答题三步走：识别数据流类型（变换/事务）→ 映射模块层次 → 按高内聚低耦合优化并说明理由。
- 记忆钩子：**耦合七级“非数标控外公内”，内聚七级“偶逻时过通顺功”，目标“高内聚、低耦合、作用域在控制域”**。`,
    quizzes: [
      {
        id: 'sd-06-02-q1',
        type: 'choice',
        question: '下列耦合类型中，耦合程度最弱、模块独立性最好的是（ ）。',
        options: ['数据耦合', '非直接耦合', '标记耦合', '控制耦合'],
        answer: 'B',
        explanation: '耦合由弱到强依次为：非直接耦合、数据耦合、标记耦合、控制耦合、外部耦合、公共耦合、内容耦合。非直接耦合指两个模块间没有直接信息传递，独立性最好；数据耦合次之。'
      },
      {
        id: 'sd-06-02-q2',
        type: 'choice',
        question: '一个模块内各组成部分都使用同一输入数据或产生同一输出数据，该模块的内聚类型属于（ ）。',
        options: ['逻辑内聚', '过程内聚', '通信内聚', '顺序内聚'],
        answer: 'C',
        explanation: '通信内聚的特点是模块内各处理成分都对**同一数据**进行操作（或都为同一输出服务）。顺序内聚强调前一成分输出作为后一成分输入的顺序关系；过程内聚强调按特定次序执行；逻辑内聚是把逻辑上相似的功能放在一起。'
      },
      {
        id: 'sd-06-02-q3',
        type: 'choice',
        question: '在软件结构设计中，关于模块作用域与控制域的关系，正确的是（ ）。',
        options: ['作用域应落在控制域之外，以扩大影响范围', '控制域应落在作用域之内', '作用域应是控制域的子集（落在控制域内）', '两者没有必然联系，可以任意'],
        answer: 'C',
        explanation: '控制域是模块及其全部下属模块；作用域是受该模块内判定影响的所有模块。设计规则要求作用域应落在控制域之内，否则判定影响会跨层传播，被迫引入控制耦合或公共耦合，损害模块独立性。'
      },
      {
        id: 'sd-06-02-q4',
        type: 'choice',
        question: '数据流图呈现“输入—变换中心—输出”形态，导出模块结构图时一般先划分出（ ）。',
        options: ['输入模块、变换模块、输出模块', '事务调度模块与各动作模块', '主程序模块与设备驱动模块', '类与对象模块'],
        answer: 'A',
        explanation: '变换型数据流图映射为典型的三层（或多层）结构：输入模块接收数据、变换模块完成核心加工、输出模块输出结果。事务型数据流图才映射为事务调度模块加各动作模块的辐射结构。'
      },
    ],
    references: [],
  },
];
