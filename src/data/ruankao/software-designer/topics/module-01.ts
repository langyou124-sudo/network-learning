import { Topic } from '@/types';

export const topics: Topic[] = [
  {
    id: 'sd-01-01',
    moduleId: 'sd-computer-system',
    title: '计算机组成与CPU',
    description: '冯·诺依曼体系、CPU内部结构、指令系统与寻址方式、流水线与性能计算，覆盖软件设计师上午题计算机组成高频考点。',
    content: `# 计算机组成与CPU

## 考情分析

「计算机组成与CPU」是软件设计师上午题中计算机系统模块的必考内容，一般出现在试卷开头的基础题区，每年稳定出现 1～2 分。常见题型为单选，考点集中在三块：冯·诺依曼体系结构的基本特征、CPU 内部部件的分工（控制器与运算器各管什么）、指令寻址方式与流水线性能计算。近年也出现过把 CPI、主频、指令数揉进一道小计算的综合题。

复习优先级：寻址方式（几乎每年都有一道）大于 CPU 部件归属（控制器 vs 运算器）大于流水线加速比计算大于 RISC/CISC 特征辨析。计算类题目公式固定，属于“送分题”，务必拿下。

## 一、核心概念

### 1. 冯·诺依曼体系结构

现代计算机的主流架构由冯·诺依曼提出，核心要点有五条，考试喜欢从否定项挖坑：

第一，计算机由运算器、控制器、存储器、输入设备、输出设备五大部分组成。注意：CPU 只包含运算器和控制器，不包含存储器。

第二，采用存储程序原理：程序和数据以同等地位存放在存储器中，可按地址寻访。这是冯·诺依曼结构区别于早期“外插式”计算机的根本特征。

第三，指令和数据用二进制表示。

第四，指令由操作码和地址码组成。操作码说明“做什么”，地址码说明“对谁做”。

第五，指令在存储器中按顺序存放，一般按顺序执行（但跳转、分支指令可改变顺序）。

### 2. CPU 的内部组成

CPU 由运算器、控制器和寄存器组三部分构成，部件归属是常考点。

运算器：负责算术运算和逻辑运算。核心部件包括算术逻辑单元 ALU（真正做加减与逻辑判断的电路）、累加器 AC（存放操作数或运算中间结果）、数据缓冲寄存器 DR、状态条件寄存器 PSW（存放标志位，如进位、溢出、零、符号）。

控制器：负责取指令、分析指令、发出控制信号。核心部件包括程序计数器 PC（存放下一条要执行指令的地址，能自动加 1）、指令寄存器 IR（存放当前正在执行的指令）、指令译码器 ID、时序控制电路、微操作信号发生器。

寄存器组：通用寄存器、地址寄存器、数据寄存器等，速度最快，位于 CPU 芯片内部。

高频归属口诀：“PC 跟着指令走，归控制器；PSW 存标志，归运算器；ALU 干活，归运算器；译码发命令，归控制器。”

### 3. 指令系统与寻址方式

指令格式：操作码字段 + 地址码字段。地址码可以是零地址（如停机指令）、一地址、二地址、三地址。RISC 常用定长指令，CISC 常用变长指令。

常见寻址方式（务必能举例识别）：

立即寻址：操作数直接写在指令地址码中，例如 MOV R0, #5。速度最快，但操作数不能修改。

直接寻址：地址码是操作数在内存中的有效地址，执行时再访存取数。

间接寻址：地址码指向的内存单元里存的才是有效地址，需要二次访存，灵活但慢。

寄存器寻址：操作数在 CPU 寄存器中，速度快。

寄存器间接寻址：寄存器中存的是内存地址，相当于“寄存器版的间接寻址”。

相对寻址：有效地址 = 程序计数器 PC 的当前值 + 地址码偏移量，常用于转移指令、程序浮动。

基址寻址：有效地址 = 基址寄存器内容 + 形式地址，适合多道程序的重定位。

变址寻址：有效地址 = 变址寄存器内容 + 形式地址，适合数组、循环访问。

## 二、关键原理/公式/机制

### 1. CPU 执行时间公式

CPU 执行时间 = CPU 时钟周期数 x 时钟周期 = 指令数 x CPI x 时钟周期 = 指令数 x CPI / 主频。

其中 CPI 是 Cycles Per Instruction，即执行一条指令平均需要的时钟周期数。主频越高、CPI 越小、指令数越少，执行越快。

注意：提高主频不一定降低执行时间，若主频提升的同时 CPI 变大，总时间可能反而变长。Amdahl 定律也提醒我们：只优化部分模块时，整体加速受限于该模块占比。

### 2. 流水线

流水线把指令执行分成若干阶段（如取指 IF、译码 ID、执行 EX、访存 MEM、写回 WB），多条指令在不同阶段并行推进。

流水线时钟周期 = 各段中最长的一段执行时间 + 锁存器延迟（考试常忽略锁存延迟，取 max 段时间）。

n 条指令的总时间 = 第一条指令完成所需时间 + (n - 1) x 流水线时钟周期。若分 k 段且每段耗时相等为 t，则首条需 k x t，后续每条 t，总时间 = (k + n - 1) x t。

流水线吞吐率 = 指令数 / 总时间。加速比 = 不用流水线的总时间 / 用流水线的总时间。

流水线冲突三类：结构冲突（资源不够，如两条指令都要访存）、数据冲突（后一条要读前一条还没写回的结果）、控制冲突（分支指令导致取指方向不确定）。解决手段包括相关指令插入气泡、数据旁路、分支预测、延迟分支。

超标量流水线：一个时钟周期内可发射多条指令，吞吐率进一步提高，常见于 RISC。

### 3. RISC 与 CISC 对比

RISC（精简指令集）：指令条数少、定长指令、寻址方式少、以寄存器-寄存器操作为主、硬布线控制、便于流水线实现、寄存器数量多。典型代表：ARM、MIPS、PowerPC、RISC-V。

CISC（复杂指令集）：指令数目多、变长、寻址方式丰富、内存-内存操作多、微程序控制、不利于流水线。典型代表：x86 系列。

软件设计师考题常以“下列关于 RISC 的叙述正确的是”形式出现，注意 RISC 不是没有微程序，而是主要用硬布线；RISC 的目标是通过简化指令提高执行效率。

## 三、常考题型与解题方法

题型一：CPU 部件归属题。问“下列属于控制器的是/运算器的是”。方法：死记归属表——PC、IR、指令译码器、时序电路归控制器；ALU、AC、PSW 归运算器。看到“存放指令地址/指令本身/译码”选控制器，看到“做运算/存标志/存中间结果”选运算器。

题型二：寻址方式识别题。给出一条指令或有效地址公式，问属于哪种寻址。方法：抓住“有效地址怎么来”。立即寻址数在指令里；直接寻址地址即 EA；间接寻址再拐一道弯；相对寻址一定加 PC；基址加基址寄存器；变址加变址寄存器。

题型三：性能计算题。给指令数、CPI、主频，算执行时间或加速比。方法：套公式，统一单位（MHz 与 ns、GHz 与 s 的换算），中间步骤写在草稿上。

题型四：流水线题。问流水线周期、总耗时、加速比。方法：周期取最长段；总时间 = 首条各段之和 + (n-1) x 周期。

## 四、典型例题

例 1：在 CPU 中用于存放当前正在执行指令的寄存器是（）。
A. 程序计数器 PC
B. 指令寄存器 IR
C. 状态条件寄存器 PSW
D. 地址寄存器

解析：IR（Instruction Register）存放的是当前正在执行的指令本身；PC 存放下一条指令地址；PSW 存运算结果的状态标志。答案选 B。

例 2：指令“MOV R0, [1000H]”中，若地址码 1000H 直接给出操作数在内存中的有效地址，则该指令采用（）。
A. 立即寻址
B. 直接寻址
C. 间接寻址
D. 寄存器寻址

解析：地址码本身就是有效地址，执行时用 1000H 去内存取操作数，属于直接寻址。间接寻址时 1000H 存的是另一个地址。答案选 B。

例 3：某指令系统中，指令数为 1 亿条，平均 CPI 为 2.5，CPU 主频为 2 GHz，则程序执行时间约为（）。
A. 0.1 s
B. 0.125 s
C. 0.25 s
D. 0.5 s

解析：主频 2 GHz 则时钟周期 = 1 / (2 x 10^9) s。总时间 = 1 x 10^8 x 2.5 / (2 x 10^9) = 2.5 x 10^8 / 2 x 10^9 = 0.125 s。答案选 B。

例 4：五段流水线，各段执行时间分别为 90 ns、100 ns、80 ns、90 ns、70 ns，忽略锁存延迟，则流水线时钟周期至少为（）。
A. 70 ns
B. 82 ns
C. 90 ns
D. 100 ns

解析：流水线时钟周期由最慢一段决定，取 max(90, 100, 80, 90, 70) = 100 ns。答案选 D。

## 五、易错点

易错点 1：把存储器算进 CPU。CPU = 运算器 + 控制器 + 寄存器组，存储器在 CPU 之外（缓存 Cache 在 CPU 内但不属于冯·诺依曼五部件中的存储器主体表述）。

易错点 2：PC 与 IR 混淆。PC 是地址（下一条），IR 是指令（当前条）。

易错点 3：相对寻址 vs 基址寻址。相对寻址的基准是 PC，基址寻址的基准是基址寄存器 BR，两者都能实现程序浮动，但基准不同。

易错点 4：流水线加速比误以为等于段数。只有在指令数趋于无穷、各段时间相等、无冲突的理想情况下，加速比才趋近于段数 k；有限条指令时加速比小于 k。

易错点 5：CPI 与主频的关系。CPI 是平均时钟周期数，与主频无直接换算关系；主频升高时钟周期变短，但 CPI 可能因微结构不同而变。

## 六、考点提示

必背清单：五部件名称；CPU 三大部分及部件归属表；六种以上寻址方式的定义与有效地址计算式；CPU 执行时间 = 指令数 x CPI / 主频；流水线时钟周期 = 最长段；流水线总时间公式；RISC 与 CISC 的五条核心差异。

计算题动笔：遇到性能题不要口算心算，单位换算错误是最大失分点。GHz 转 Hz 要乘 10 的 9 次方，MIPS 是每秒百万条指令，不要和 CPI 混用。

读题关键词：“正在执行的指令”对 IR；“下一条指令地址”对 PC；“有效地址 = PC + 位移”对相对寻址；“各段中最慢”对流水线周期。

冲刺自查：能不能在十秒内默写 CPU 执行时间公式并代入数字？能不能看到“PC + 位移”立刻说出相对寻址？能不能分清 ALU 与指令译码器各属哪一半 CPU？若任一题犹豫，回到第一节部件表和第二节公式再过一遍，再做课后四道自测题巩固。软考上午题这类基础分丢掉非常可惜，正确率应稳定在九成以上。`,
    quizzes: [
      {
        id: 'sd-q-01-01a',
        type: 'choice',
        question: '在 CPU 中，用于存放下一条要执行的指令地址，并能自动增量的寄存器是（）。',
        options: ['指令寄存器 IR', '程序计数器 PC', '状态条件寄存器 PSW', '累加器 AC'],
        answer: 'B',
        explanation: '程序计数器 PC 保存下一条指令的地址，每取完一条指令后一般会自动 +1 形成下一条地址。IR 存当前指令，PSW 存标志位，AC 存操作数或中间结果。',
      },
      {
        id: 'sd-q-01-01b',
        type: 'choice',
        question: '下列部件中，属于运算器组成部分的是（）。',
        options: ['指令译码器', '程序计数器 PC', '算术逻辑单元 ALU', '微操作信号发生器'],
        answer: 'C',
        explanation: '运算器包括 ALU、累加器 AC、数据缓冲寄存器 DR、状态条件寄存器 PSW 等。指令译码器、PC、微操作信号发生器均属于控制器。',
      },
      {
        id: 'sd-q-01-01c',
        type: 'choice',
        question: '某计算机主频为 2.5 GHz，某程序共执行 5 x 10^8 条指令，平均 CPI 为 2，则该程序的 CPU 执行时间约为（）。',
        options: ['0.2 s', '0.4 s', '0.5 s', '1.25 s'],
        answer: 'B',
        explanation: '执行时间 = 指令数 x CPI / 主频 = 5 x 10^8 x 2 / (2.5 x 10^9) = 10^9 / 2.5 x 10^9 = 0.4 s。',
      },
      {
        id: 'sd-q-01-01d',
        type: 'choice',
        question: '某指令采用“有效地址 = PC 的内容 + 地址码中的位移量”形成操作数地址，该寻址方式是（）。',
        options: ['基址寻址', '变址寻址', '相对寻址', '寄存器间接寻址'],
        answer: 'C',
        explanation: '以程序计数器 PC 为基准加上形式地址（位移量）得到有效地址，属于相对寻址。基址寻址用基址寄存器，变址寻址用变址寄存器。',
      },
    ],
    references: [],
  },
  {
    id: 'sd-01-02',
    moduleId: 'sd-computer-system',
    title: '存储系统',
    description: '存储器层次结构、Cache 映射与替换、主存编址计算、虚拟存储与相联存储，软件设计师上午题计算与概念双重点。',
    content: `# 存储系统

## 考情分析

「存储系统」是软件设计师计算机系统知识中出题密度最高的小节之一，历年上午题约 2～3 分，且概念题与计算题并重。高频考点排序大致为：Cache 地址映射（直接映射、全相联、组相联）与 Cache 访问命中计算、主存按字节/按字编址的容量计算、存储层次结构的速度与价格定位、虚拟存储器的作用、相联存储器的用途。

计算题集中在两处：一是主存芯片数量与地址范围题（要会算地址线根数、芯片位数扩展与字数扩展）；二是 Cache 平均访问时间题（命中率、主存访问时间、Cache 访问时间的组合）。这两类必须动手算熟。

## 一、核心概念

### 1. 存储器层次结构

典型层次：寄存器 → Cache（高速缓存） → 主存（内存） → 辅存（外存，如硬盘、U 盘、光盘、SSD）。

从上到下：容量越来越大，每位价格越来越低，速度越来越慢。Cache 到主存这一级由硬件（MMU/Cache 控制器）实现透明调度；主存到辅存这一级由操作系统与硬件配合，以页或段为单位调度，形成虚拟存储器。

为什么要分层：速度接近寄存器、容量接近辅存、价格接近辅存——用小容量高速存储存放当前活跃数据，大容量低速存储存放全量数据。考试若给出速度或价格排序，方向永远是：寄存器最快最贵，辅存最慢最便宜。

### 2. 主存储器分类

按存取方式：随机存取存储器 RAM（可读可写，断电丢失，如 DRAM、SRAM）、只读存储器 ROM（断电不丢，如 BIOS 芯片）、顺序存取（磁带）、直接存取（磁盘，定位后随机）。

DRAM 用电容存信息，需定期刷新，集成度高、便宜，做主存；SRAM 用触发器，不用刷新，快而贵，做 Cache。按性能与用途还可记忆：闪存 Flash 介于主存与辅存之间，可做 U 盘与 SSD。

ROM 家族常考几个变种：掩膜 ROM 出厂固定；PROM 一次可编程；EPROM 紫外线可擦除重写；EEPROM 电可擦除；它们共同点是断电不丢。PC 的 BIOS/UEFI 芯片属于这一类。

### 3. 主存编址

编址方式有两种典型考法：

按字节编址：1 个地址单元 = 1 字节。若地址总线 n 根，可编 2^n 个单元，容量 = 2^n 字节。

按字编址：1 个地址单元 = 1 个字。字长 W 位时，容量 = 2^n x W/8 字节。

地址范围题：从地址 A 到地址 B，单元数 = B - A + 1，再乘每单元字节数。十六进制地址计算时先换成二进制或统一进制再相减。

存储器芯片扩展：位扩展（字长不够，多片并联扩位）与字扩展（字数不够，多片串联扩字，需译码器选片）。所需芯片数 = (总容量) / (单片容量)。若既不够位又不够字，先做位扩展满足字长，再做字扩展满足容量。

例：用 16K x 1 位芯片构成 64K x 8 位存储器。字长 8 位需 8 片并联做位扩展，字数从 16K 到 64K 需 4 组做字扩展，共 32 片。这类片数计算是上午题经典小计算。

### 4. Cache（高速缓冲存储器）

Cache 位于 CPU 与主存之间，容量小、速度接近 CPU。其理论依据是程序的局部性原理：时间局部性（刚访问过的可能马上再用）与空间局部性（附近单元可能马上被用）。

Cache 以块（行）为单位与主存交换数据。主存块调入 Cache 的映射方式是必考点：

直接映射：主存第 i 块只能放到 Cache 的第 (i mod C) 槽，C 为 Cache 块数。地址划分 = 标记 Tag + 行号（索引） + 块内偏移。优点是硬件简单，缺点是冲突多、命中率低。

全相联映射：主存块可放任意 Cache 行。地址划分 = 标记 + 块内偏移。灵活、命中率高，但比较器多、成本高。

组相联映射：折中方案。Cache 分成若干组，组间直接映射、组内全相联。地址划分 = 标记 + 组号 + 块内偏移。常见 2 路、4 路组相联。

替换算法：先进先出 FIFO、最近最少使用 LRU、随机替换 RAND。全相联与组相联需要替换算法，直接映射不需要。LRU 在“经常用的留下”这一点上通常命中率最好，是教材默认推荐。

写策略：写直达（写 Cache 同时写主存，一致但总线忙）、写回法（只写 Cache，行被替换时写回主存，效率高但管理复杂）。注意还有“写分配”与“非写分配”与之组合，考试一般只需区分写直达与写回。

Cache 命中与缺失：若要访问的主存块已在 Cache 中则命中，速度接近 Cache；否则缺失，需从主存调块，可能触发替换。

## 二、关键原理/公式/机制

### 1. Cache 平均访问时间

设 Cache 访问时间 Tc，主存访问时间 Tm，命中率 H：

命中率 H = Cache 命中次数 / 总访问次数，一般要求 H 大于 90%，理想达 95% 以上。

先访问 Cache、未命中再访问主存：
平均访问时间 Ta = H x Tc + (1 - H) x (Tc + Tm)

同时访问 Cache 与主存（节省一次切换）：
Ta = H x Tc + (1 - H) x Tm

读题时看清是“同时访问”还是“分步访问”，两种公式结果不同，是常见命题陷阱。

### 2. Cache 地址格式

直接映射：主存地址 = 标记 + 行号 + 块内偏移。
块内偏移位数 = log2(块大小)。
行号位数 = log2(Cache 块数)。
标记位数 = 主存地址总位数 - 行号位数 - 块内偏移位数。

组相联：组号位数 = log2(Cache 组数)。组数 = Cache 块数 / 路数。

例：主存 4 GB，块大小 64 B，Cache 共 1024 行直接映射。主存地址 32 位；块内偏移 6 位；行号 10 位；标记 = 32 - 10 - 6 = 16 位。

### 3. 相联存储器

按内容检索而不是按地址检索，用比较器并行匹配关键字。典型用途：Cache 的标记查找、页表快表 TLB、虚拟存储器查表。考试常问“按内容寻址的是”，答案选相联存储器。

### 4. 虚拟存储器

在主存与辅存之间建立逻辑地址空间，使程序可使用大于实际主存的地址空间。页式虚拟存储（页表管理）、段式、段页式。对程序员透明。页面淘汰算法有 LRU、FIFO、OPT（最优，理论用）等。快表 TLB 用于加速页表查询。

虚拟存储器与 Cache 的共同点是都基于局部性原理、都用小而快的存储缓冲大而慢的存储；差别是 Cache 由硬件对程序员和系统程序员都透明，虚拟存储由软硬件结合、对应用程序员透明但操作系统感知。页面大小固定为页框大小，段式则按逻辑信息段划分，更符合程序结构但会产生外部碎片。

### 5. 校验码常识（附带考点）

存储与传输还常考校验码：奇偶校验只能检 1 位错、不能纠错；海明码既可检错也可纠错，校验位数 r 需满足 2^r 大于等于 m + r + 1（m 为数据位）；循环冗余校验 CRC 用模 2 除法，检错能力强，适合通信与磁盘。若考题给出数据位与要求纠错能力，可反推海明码校验位位数。

## 三、常考题型与解题方法

题型一：存储层次与器件辨析。问速度/价格排序、DRAM 与 SRAML 的用途。方法：画一条“快而小贵到慢而大便宜”的箭头，对号入座。

题型二：主存编址与芯片计算。给地址范围与字长，算容量、算芯片片数。方法：统一换算成字节或位，再除单片容量；按字节编址别漏字长换算。

题型三：Cache 映射与地址字段。给块大小、Cache 行数、主存容量，问地址如何划分或某主存块应放哪一行。方法：先算三段位数，再看题目是直接映射还是组相联。

题型四：平均访问时间计算。方法：先判写法是“先 Cache 后主存”还是“并行”，再代公式，注意未命中时间是否已含 Tc。

## 四、典型例题

例 1：按字节编址，地址从 4000H 到 4FFFH，该存储区域的容量是（）。
A. 2 KB
B. 4 KB
C. 8 KB
D. 16 KB

解析：单元数 = 4FFFH - 4000H + 1 = 1000H = 4096。按字节编址，容量 = 4096 B = 4 KB。答案选 B。

例 2：某 Cache 的块大小为 64 B，采用直接映射，Cache 共有 2048 行，主存地址 32 位，则标记字段的位数是（）。
A. 12
B. 13
C. 14
D. 15

解析：块内偏移 = log2(64) = 6 位；行号 = log2(2048) = 11 位；标记 = 32 - 11 - 6 = 15 位。答案选 D。

例 3：Cache 访问时间 20 ns，主存访问时间 200 ns，命中率 95%，采用“先访问 Cache，未命中则访问主存”的方式，平均访问时间为（）。
A. 28 ns
B. 29 ns
C. 30 ns
D. 38 ns

解析：Ta = 0.95 x 20 + 0.05 x (20 + 200) = 19 + 0.05 x 220 = 19 + 11 = 30 ns。答案选 C。

例 4：用于 Cache 的标记查找、按内容寻址的存储器是（）。
A. 只读存储器 ROM
B. 相联存储器
C. 随机存取存储器 RAM
D. 直接存取磁盘

解析：相联存储器按内容并行比较、按内容寻址，常用于 Cache 标记与 TLB。答案选 B。

## 五、易错点

易错点 1：按字节编址与按字编址混用。按字编址时 1 地址单元等于 1 字，不等于 1 字节，算容量必须乘字长。

易错点 2：直接映射的块内偏移与行号算反。偏移看块大小，行号看 Cache 行数，顺序是“标记 | 行号 | 偏移”。

易错点 3：平均访问时间公式的两种语义。未命中开销是否包含一次 Cache 查询时间，必须按题干描述选公式，不能见数字就套。

易错点 4：把 Cache 当成主存层次之外的外存。Cache 在 CPU 与主存之间，属于高速小容量缓冲，不是辅存。

易错点 5：DRAM 与 SRAM 用反。DRAM 要刷新、主存用；SRAM 不刷新、Cache 用。

## 六、考点提示

必背：存储层次速度与价格的方向；DRAM/SRAM 区别；直接映射、全相联、组相联的定义与地址字段；两个平均访问时间公式；局部性原理；相联存储器用途。

主存计算模板：地址差加一得单元数，再看编址单位；芯片数 = 总位容量 / 单片位容量。

Cache 计算模板：先列三段位数（偏移、索引、标记），再写数字；平均访问时间先写公式再代数。

命题信号词：“按内容寻址”指相联存储器；“需要刷新”指 DRAM；“标记、行号、块内偏移”指 Cache 地址格式；“程序员感知不到的扩容”指虚拟存储器。

最后提醒：存储系统计算题务必分清“位”和“字节”，位扩展与字扩展的乘除关系要自己推一遍公式再上考场。概念题优先消灭“地址线决定容量”“DRAM 做 Cache”“直接映射要 LRU”这三类颠倒说法。`,
    quizzes: [
      {
        id: 'sd-q-01-02a',
        type: 'choice',
        question: '按字节编址时，地址范围为 8000H 到 8FFFH 的存储器容量是（）。',
        options: ['2 KB', '4 KB', '8 KB', '16 KB'],
        answer: 'B',
        explanation: '8FFFH - 8000H + 1 = 1000H = 4096 个单元，按字节编址即 4096 字节 = 4 KB。',
      },
      {
        id: 'sd-q-01-02b',
        type: 'choice',
        question: '关于 Cache 的叙述中，正确的是（）。',
        options: [
          'Cache 位于主存与辅存之间，由操作系统负责调度',
          'Cache 的理论依据是程序的局部性原理',
          '直接映射需要复杂的替换算法',
          'SRAM 集成度高、需要刷新，常用来构成 Cache'
        ],
        answer: 'B',
        explanation: 'Cache 依据局部性原理在 CPU 与主存间做缓冲。Cache 在 CPU 与主存之间而非主存与辅存之间；直接映射无替换算法；SRAM 不需刷新，DRAM 才需刷新。主存多用 DRAM，Cache 多用 SRAM。',
      },
      {
        id: 'sd-q-01-02c',
        type: 'choice',
        question: 'Cache 访问时间为 10 ns，主存访问时间为 100 ns，命中率为 90%，采用“先访问 Cache，未命中再访问主存”方式，则平均访问时间为（）。',
        options: ['19 ns', '20 ns', '28 ns', '190 ns'],
        answer: 'B',
        explanation: '该方式下未命中路径要先后访问 Cache 和主存。Ta = H x Tc + (1 - H) x (Tc + Tm) = 0.9 x 10 + 0.1 x (10 + 100) = 9 + 11 = 20 ns。',
      },
      {
        id: 'sd-q-01-02d',
        type: 'choice',
        question: '某存储系统中，可按内容进行并行查找、常用于 Cache 标记查询的是（）。',
        options: ['随机存取存储器 RAM', '只读存储器 ROM', '相联存储器', '顺序存取磁带'],
        answer: 'C',
        explanation: '相联存储器按内容寻址，用比较器并行匹配关键字，典型应用是 Cache 标记查找和页表快表 TLB。',
      },
    ],
    references: [],
  },
  {
    id: 'sd-01-03',
    moduleId: 'sd-computer-system',
    title: '输入输出与总线',
    description: 'I/O 控制方式、中断与 DMA、总线分类与性能计算、接口标准，软件设计师上午题基础识记与小计算混合考点。',
    content: `# 输入输出与总线

## 考情分析

「输入输出与总线」一般占 1～2 分，题型以概念辨析为主，偶尔出现总线带宽小计算。高频考点：四种 I/O 控制方式（程序查询、中断、DMA、通道）的对比与适用场景、DMA 的“窃取总线/周期挪用”特征、中断响应过程、总线分类（数据总线、地址总线、控制总线）及其位数含义、总线带宽计算、常见接口标准（USB、SATA、PCIe、RS-232 等）速度与串并属性。

本部分记忆量适中，性价比高：把控制方式对比表和总线三部件记牢，再掌握带宽一个公式，分数基本稳拿。

## 一、核心概念

### 1. I/O 系统基本组成

外部设备（键盘、鼠标、显示器、磁盘等）通过 I/O 接口与主机相连。接口的作用：数据缓冲、信号格式转换、设备选择、时序匹配、电平转换。I/O 接口中常有数据寄存器、状态寄存器、控制寄存器。

主机与外设交换数据的核心矛盾：CPU 速度远高于外设，不能让 CPU 干等。因此产生了多种控制方式。外设按速度大致分低速（键盘、鼠标）、中速（打印机）、高速（磁盘、网卡），控制方式的选择与速度、是否成批传输密切相关。

### 2. 四种 I/O 控制方式

程序查询方式：CPU 不断查询设备状态寄存器，就绪则传送。优点是简单、硬件少；缺点是 CPU 效率极低，适用于极慢的简单外设。

中断方式：外设准备好后发中断请求，CPU 保存现场、执行中断服务程序完成传送，再恢复现场。CPU 不必忙等，可与外设并行工作。但每次传送仍以字节/字为单位，中断响应与现场保护有开销，适合键盘、鼠标等低速设备。

DMA（直接存储器访问）方式：由 DMA 控制器接管总线，在主存与外设之间直接成块传送，不需要 CPU 介入每个数据单元。CPU 只在开始时给出内存起始地址、传送长度，并在结束时响应中断。传送单位是“块”。适合磁盘等高速成批设备。DMA 与主存交换数据有三种方式：停止 CPU 访存（CPU 闲置）、周期挪用（外设要传时挪用若干存取周期，用完归还）、DMA 与 CPU 交替访存。

通道与 I/O 处理机：通道是专用处理器，有自己的指令系统（通道指令），可执行通道程序，独立管理多个外设，CPU 只需启动通道，宏观上完全并行。常见通道字节多路、选择通道、数组多路通道。I/O 处理机则功能更强，可带本地存储器，接近一台小计算机。

一句话对比：查询是“CPU 打电话一直等”；中断是“外设打电话叫 CPU”；DMA 是“外设直接去仓库（主存）搬货，保安（CPU）只看一次”；通道是“外包给专门的物流经理”。

### 3. 中断基础

中断源分类：内中断（异常、陷入）与外中断。又可分可屏蔽中断与不可屏蔽中断（NMI，如电源掉电，必须响应）。

中断响应过程：关中断、保存断点（PC、PSW）和现场、判中断优先级、转入中断服务程序、开中断、执行服务程序、关中断恢复现场、开中断返回。

中断向量：中断服务程序的入口地址表，由中断类型号索引。向量中断响应快。

中断优先级与嵌套：高优先级可打断低优先级；同级或低级不可。多级中断可提高系统实时性。

DMA 与中断的区别是常考：中断靠程序传送数据，DMA 靠硬件；中断以字为单位，DMA 以块为单位；DMA 优先级通常高于普通中断；中断响应发生在指令结束时刻，DMA 申请可在指令周期内被响应。另外，DMA 在成批高速场景下单位数据开销远小于中断。

### 4. 总线

总线是多个部件分时共享的传输介质。按层次分：片内总线（芯片内部）、系统总线（连接 CPU、主存、I/O）、通信总线（外部，如 RS-232、USB）。按数据传送方式分：串行总线（一次一位，适合远距离）与并行总线（一次多位，近距离带宽高但线间干扰与时钟同步难）。

系统总线分三部分：

数据总线 DB：传输数据，位数决定一次能传的位宽（与字长相关），双向。

地址总线 AB：传输地址，单向，由 CPU 发出；位数决定可寻址空间大小（n 位对应 2^n 个单元）。

控制总线 CB：传输读写、中断、复位、时钟等控制信号，每根方向固定但整体有入有出。

总线性能指标：总线带宽 = 总线宽度 / 传输周期，或 = 总线宽度 x 总线频率 / 每传输占用时钟数。注意单位（MB/s、Gb/s）。此外还有总线复用（同一组线分时传地址和数据）、总线仲裁（决定谁获得总线控制权）等常识点。

总线标准常识：ISA（老）、EISA、PCI（并行）、PCIe（串行，分 x1/x4/x8/x16 通道）、USB（通用串行，USB3.x 显著提速）、SATA（串行 ATA 接硬盘）、RS-232（串行，低速老接口）。串行总线近年来胜出，原因是时钟同步与布线成本更优。

## 二、关键原理/公式/机制

### 1. 总线带宽计算

带宽（数据传输率）= 总线宽度（字节）x 总线时钟频率 / 完成一次传输所需的时钟周期数。

例：总线宽度 32 位 = 4 字节，工作频率 66 MHz，每 2 个时钟周期传一次，则带宽 = 4 B x 66 MHz / 2 = 132 MB/s。

若一次传输需多个时钟周期（如传输周期包括地址期与数据期），必须在分母体现，否则会算高。

### 2. 中断与 DMA 的时间开销观

中断方式下，设每次传送 n 字节，每字节都需一次中断，总时间约 = 数据时间 + 中断处理开销 x 传送次数。
DMA 下总时间约 = 设置 DMA 寄存器时间 + 块传输时间 + 中断次数（通常仅首尾）。
因此数据量越大、速度越高，DMA 优势越明显。

### 3. 地址总线与容量

n 根地址总线可寻址 2^n 字节（按字节编址时）。数据总线宽度不改变寻址空间，只影响一次传输位数。做题时两者不可混。

## 三、常考题型与解题方法

题型一：控制方式选择题。问“磁盘适于哪种方式”“键盘适于哪种”。方法：低速简单外设用查询或中断；成块高速外设用 DMA；大型机多通道用 I/O 通道。口诀“盘带用 DMA，键鼠用中断，老古董用查询，大机器用通道”。若题干强调“CPU 与外设并行、按块传、几乎不占 CPU”，直接指向 DMA；强调“响应事件、保存现场”，指向中断。

题型二：控制方式叙述正确/错误题。方法：抓 DMA 不用 CPU 参与数据、以块为单位；中断在指令结束后响应；查询时 CPU 始终参与。

题型三：总线分类与位数含义。方法：数据总线看宽度，地址总线看寻址，控制总线看信号。单向/双向判断：地址单向、数据双向。

题型四：总线带宽计算。方法：宽度先换成字节，再乘频率，再除每传输周期数，检查单位是否 MB/s。

## 四、典型例题

例 1：在磁盘和主存之间建立直接数据通路、成块传送数据的方式是（）。
A. 程序查询方式
B. 程序中断方式
C. DMA 方式
D. 通道方式

解析：DMA 由 DMA 控制器在磁盘与主存间直接传送数据块，CPU 不参与每个数据。答案选 C。（通道也能成批，但题干强调“直接数据通路、成块”是 DMA 的经典定义表述。）

例 2：某系统总线宽度为 64 位，时钟频率为 133 MHz，每传输一个数据需要 2 个时钟周期，则其最大数据传输率约为（）。
A. 266 MB/s
B. 532 MB/s
C. 1064 MB/s
D. 2128 MB/s

解析：64 位 = 8 字节。带宽 = 8 B x 133 MHz / 2 = 8 x 133 / 2 MB/s = 532 MB/s。答案选 B。

例 3：下列关于总线的叙述中，正确的是（）。
A. 数据总线的位数决定了主存空间的大小
B. 地址总线是双向传输的
C. 地址总线的位数决定了可寻址范围
D. 控制总线只能从 CPU 向外传

解析：数据总线位数决定一次传送位宽；地址总线单向，由 CPU 发出；地址总线位数决定寻址空间；控制总线部分信号由外设回传（如中断请求、就绪）。答案选 C。

例 4：DMA 与主存交换数据时，CPU 继续执行、仅在 DMA 需要时挪用若干存取周期的方式是（）。
A. 停止 CPU 访存
B. 周期挪用
C. DMA 与 CPU 交替访存
D. 程序中断

解析：周期挪用（cycle stealing）指外设需要时临时占用若干内存周期，用完归还，CPU 几乎不闲置。答案选 B。

## 五、易错点

易错点 1：地址总线与数据总线职责互换。寻址看地址总线，传数据宽度看数据总线。

易错点 2：DMA 仍需 CPU 完全不管。DMA 前 CPU 要初始化地址与计数，结束后常以中断通知 CPU。

易错点 3：中断响应时机。一般在一条指令执行结束时响应中断，而不是任意时刻（DMA 请求可在更细粒度被允许）。

易错点 4：总线带宽漏除时钟周期数。有的总线一个时钟传一次，有的多个时钟传一次，题干有“每 N 个周期”必须除 N。

易错点 5：把 USB、SATA 当成并行总线。它们都是串行；传统 PCI 是并行，PCIe 是串行。

## 六、考点提示

必背对比表：查询 / 中断 / DMA / 通道在“CPU 参与度、传送单位、适合设备、软硬分工”四列上的差异。

必背：数据、地址、控制总线的方向与位数含义；总线带宽公式；周期挪用的含义；不可屏蔽中断 NMI 的典型例子。

计算题规范步骤：宽度换字节 → 乘频率 → 除每传输周期 → 写单位。选择题用排除法时优先排“地址双向”“DMA 完全不需要 CPU”“数据总线决定寻址空间”这三类错误说法。`,
    quizzes: [
      {
        id: 'sd-q-01-03a',
        type: 'choice',
        question: '在磁盘与主存之间成批交换数据、由专门硬件控制、CPU 基本不参与数据传送的方式是（）。',
        options: ['程序查询方式', '程序中断方式', 'DMA 方式', '无条件传送方式'],
        answer: 'C',
        explanation: 'DMA 方式由 DMA 控制器在 I/O 设备与主存之间建立直接数据通路，以数据块为单位传送，CPU 只做初始化和收尾中断，适合磁盘等高速外设。',
      },
      {
        id: 'sd-q-01-03b',
        type: 'choice',
        question: '某总线宽度为 32 位，时钟频率为 50 MHz，每传输一次数据占用 2 个时钟周期，其总线带宽是（）。',
        options: ['50 MB/s', '100 MB/s', '200 MB/s', '400 MB/s'],
        answer: 'B',
        explanation: '32 位 = 4 字节。带宽 = 4 B x 50 MHz / 2 = 100 MB/s。',
      },
      {
        id: 'sd-q-01-03c',
        type: 'choice',
        question: '关于计算机总线的叙述中，错误的是（）。',
        options: [
          '地址总线是单向的，用于传送 CPU 发出的地址',
          '数据总线的宽度一般与字长有关',
          '地址总线的位数决定了可寻址的存储空间大小',
          '数据总线的位数决定了主存的最大容量'
        ],
        answer: 'D',
        explanation: '主存容量由地址总线位数决定，不是数据总线。数据总线决定一次并行传输的位数。',
      },
      {
        id: 'sd-q-01-03d',
        type: 'choice',
        question: 'DMA 与主存交换数据时，若外设仅在需要时临时占用若干存取周期、用完归还，这种方式称为（）。',
        options: ['停止 CPU 访存', '周期挪用', 'DMA 与 CPU 交替访存', '程序查询'],
        answer: 'B',
        explanation: '周期挪用是 DMA 三种访存方式之一：外设需要时挪用一个或几个存取周期，用完归还 CPU，兼顾了 CPU 效率与传输需求。',
      },
    ],
    references: [],
  },
];
