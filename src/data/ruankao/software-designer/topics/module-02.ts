import { Topic } from '@/types';

export const topics: Topic[] = [
  {
    id: 'sd-02-01',
    moduleId: 'sd-programming-language',
    title: '编译过程与文法',
    description: '编译程序六阶段、解释与编译的差别、文法四元组与四型文法、句型句子与语法树，软件设计师上午题必考基础。',
    content: `# 编译过程与文法

## 考情分析

「编译过程与文法」属于软件设计师上午题程序设计语言部分的基础必考块，每年约 1～2 分，多出现在前半卷。命题集中在三处：编译程序各阶段任务与产物的对应关系（尤其词法分析、语义分析、代码生成分别做什么）、编译与解释方式的对比、形式文法的定义与四型文法（0～3 型）识别、文法推导与句型/句子/语言的辨析。近年还出现过给出一个简单文法，问某个串是否是句子、属于几型文法的题。

复习策略：编译阶段表必须能按顺序默写并匹配产物；文法四元组定义记牢；Chomsky 四型文法按产生式受限程度反向记忆（3 型最严、0 型最宽）；句子、句型、语言三个概念能举例区分。

## 一、核心概念

### 1. 翻译程序的两种方式

把高级语言翻译成机器可执行代码（或中间代码）有两种路径：

编译方式：先由编译器把整个源程序翻译成目标程序（或汇编），再由机器执行。执行效率高，但源程序改动后必须重新编译。典型如 C、C++、Go、Rust。

解释方式：解释器逐句读源程序，立即分析、立即执行，不产生独立的目标程序。便于调试、可移植性好，但运行效率低。典型如早期 BASIC、JavaScript 的某些运行方式、Python 的字节码虚拟机执行（严格说是“先编译到字节码再解释”）。

一句话对比：编译是“整本翻译完再看”，解释是“看一句翻一句”。编译方式有目标代码生成阶段的完整产物，解释方式通常不生成完整目标程序。

编译方式与解释方式也可以混合：先编译为中间代码，再由解释器执行中间代码，兼得移植性与一定效率。

### 2. 编译程序的工作阶段

经典编译过程分六个逻辑阶段，外加表格管理与出错处理两个贯穿全程的部分：

词法分析（扫描）：输入源程序字符流，输出单词符号序列（记号 Token）。任务是识别关键字、标识符、常数、运算符、界符，滤掉空格与注释。依据是语言的词法规则（正则文法/正规式）。错误如标识符不合法、符号无法识别。

语法分析：以单词为输入，按语法规则（上下文无关文法）判断是否构成语法上正确的语法单位（表达式、语句、程序），并构造语法分析树（语法树）。错误如括号不配、表达式结构非法。

语义分析：审查语法正确的结构是否有意义。典型工作：类型检查（能否相乘？）、类型转换（整数与实数运算）、作用域与名字合法性检查、控制流检查（break 是否在循环内）。

中间代码生成：把语法树翻译为与具体机器无关的中间表示，如三地址码、四元式、逆波兰式、抽象语法树。便于优化与移植。

代码优化：对中间代码进行等价变换，提高时空效率。如常量合并、公共子表达式消除、死代码消除、循环优化、强度削减。优化不改变程序语义。

目标代码生成：把优化后的中间代码变为目标机器的机器码或汇编。涉及寄存器分配、指令选择、内存布局。

符号表管理：贯穿各阶段，记录标识符的名字、类型、作用域、地址等属性。
出错处理：各阶段都可能出错，应能报错并尽量恢复，继续查错。

注意：教材常考“每个阶段的输入输出是什么”。词法的输入是字符流、输出是单词序列；语法的输入是单词、输出是语法树；语义分析与语法分析树上的语义动作紧密相关。

### 3. 形式语言与文法

文法（语法）是描述语言结构的形式化规则，用四元组 G = (S, Vn, Vt, P) 或写作 G = (Vn, Vt, S, P) 表示，四个成分是：

Vn：非终结符集合（变量集合），表示语法单位。
Vt：终结符集合，语言的基本符号，出现在最终句子中。
S：开始符号（识别符），属于 Vn，推导的起点。
P：产生式（规则）集合，形如 α -> β，表示 α 可以改写为 β。

V = Vn 并 Vt 称为文法的字母表（词汇表）。

### 4. Chomsky 四型文法

按产生式 α -> β 中 α、β 的受限程度分型，注意：型别数字越小，限制越严、能力越强，能描述的语言越多。

3 型文法（正规文法、右线性文法）：产生式形如 A -> aB 或 A -> a，A、B 为非终结符，a 为终结符或空串。描述词法，等价于有限自动机 FA、正规式。词法分析的理论基础。

2 型文法（上下文无关文法 CFG）：产生式形如 A -> β，A 为单个非终结符，β 为任意符号串。大多数高级语言的语法用 2 型文法描述，等价于下推自动机 PDA。语法分析的理论基础。

1 型文法（上下文有关文法）：产生式 α1Aα2 -> α1βα2，β 至少与 A 一样“不更短”（允许空串例外的特殊情况除外），即符号串长度基本不减少。也称非收缩的上下文有关文法，等价于线性有界自动机。

0 型文法（短语结构文法、无限制文法）：产生式 α -> β 无任何限制，只要求 β 中至少有一个符号（或特例 S -> 空串）。能力相当于图灵机。

包含关系：3 型属于 2 型属于 1 型属于 0 型，反之不成立。

### 5. 推导、句型、句子、语言

直接推导：用一条产生式替换一个非终结符。
推导：经若干步直接推导。从开始符号 S 出发的推导记作 S =>* α。
句型：从 S 开始可推导出的任意符号串（可含终结符和非终结符）。
句子：仅由终结符组成的句型。
语言 L(G)：文法 G 产生的所有句子的集合。

语法树：推导过程的树形表示。同一句型可能有多棵不同语法树或不同推导序列，此时称文法是二义的。

## 二、关键原理/公式/机制

### 1. 编译阶段对应关系（记忆骨架）

字符流 --词法分析--> 记号序列 --语法分析--> 语法树 --语义分析--> 带语义的语法树 --中间代码生成--> 中间代码 --优化--> 优化后中间代码 --目标代码生成--> 目标程序。

错误类型口诀：词法“认不得符号”，语法“排错队形”，语义“类型不对/名字没定义”，优化与目标代码一般不报源程序逻辑错。

### 2. 文法识别判别法

判断 3 型：每个产生式左部是单个非终结符，右部是“一个终结符 + 至多一个非终结符”且非终结符在右端（右线性）或左端（左线性），左右不混。

判断 2 型：左部必须是单个非终结符，右部任意。若右部被替换依赖左边上下文（左部含多个符号），则不是 2 型。

判断 1 型：在非“含空串”特殊情况下，产生式右部长度不小于左部（不收缩），且左部含至少一个非终结符。

其余归入 0 型。

### 3. 句子判定方法

给定符号串 w 是否是句子：能否从开始符号 S 经若干步推导出 w（或 w 能否被归约到 S）。对简单文法可用穷举推导；对表达式文法可用语法树画法验证。

### 4. 二义性判据

若文法中存在某个句子，可以画出两棵不同的语法树（或存在两种不同的最左推导 / 最右推导），则该文法是二义的。二义文法会给编译器带来“不知按哪种结合性计算”的问题，通常要靠优先级与结合性规则消解。

## 三、常考题型与解题方法

题型一：编译阶段与任务匹配。方法：背“字符—词法—单词；结构—语法—语法树；含义—语义—类型；机器—目标—指令”四句对应。

题型二：编译 vs 解释叙述题。方法：有无目标程序是分水岭；执行效率通常编译高；移植与调试解释方便。

题型三：文法分型题。方法：先看产生式左部是否为单非终结符（否则直接不是 2 型），再看右部形状是否右线性/左线性（判 3 型），最后看是否收缩（判 1 型）。

题型四：句型/句子/语言辨析。方法：能从 S 推出来的是句型；全部是终结符的句型才是句子；所有句子构成语言。

## 四、典型例题

例 1：编译程序中，对源程序进行词法分析时，所依据的是（）。
A. 语言的语义规则
B. 语言的语法规则
C. 语言的词法规则
D. 语言的操作规则

解析：词法分析依据词法规则（正规式/正规文法）识别单词；语法分析依据语法规则（上下文无关文法）；语义分析依据语义规则。答案选 C。

例 2：给出文法 G：E -> E + T | T，T -> T * F | F，F -> (E) | id。则下列叙述正确的是（）。
A. G 是 3 型文法
B. G 是 2 型文法
C. G 是 1 型文法
D. G 不是形式文法

解析：各产生式左部都是单个非终结符（E、T、F），右部形式不受限制（可含多个符号和括号），属于上下文无关文法，即 2 型。因右部不是“终结符后跟单非终结符”的右线性形式，不是 3 型。答案选 B。

例 3：设文法 G 的开始符号为 S。符号串 x 称为 G 的句子，当且仅当（）。
A. S 可以推导出 x
B. x 中既有终结符又有非终结符
C. x 是仅由终结符组成的句型
D. x 是仅由非终结符组成的串

解析：句型是 S 可推导出的任意串；句子是只含终结符的句型。答案选 C。

例 4：若某句子在文法 G 中存在两棵不同的语法树，则说明（）。
A. G 一定是 0 型文法
B. G 是二义文法
C. G 描述的语言为空
D. G 的开始符号不唯一

解析：同一句子存在两棵不同语法树（或两种不同最左/最右推导）是文法二义的定义。答案选 B。

## 五、易错点

易错点 1：把句型与句子混为一谈。句型可含非终结符，句子必须全部由终结符组成。

易错点 2：文法四元组中 Vn 与 Vt 写反，或把开始符号当成终结符。S 属于 Vn。

易错点 3：四型文法强弱关系记反。数字越小能力越强，限制越少；3 型是最受限的正规文法。

易错点 4：认为“词法分析用上下文无关文法”。词法用 3 型（正规文法/正规式），语法用 2 型（CFG）。

易错点 5：把语义分析的类型检查说成语法分析的事。括号配对、表达式结构归语法；能否执行、类型是否匹配归语义。

易错点 6：混淆编译与解释——解释通常不生成完整目标程序，但可能生成中间代码或字节码。

## 六、考点提示

必背链条：词法规则→词法分析→单词；语法规则→语法分析→语法树；语义规则→语义分析→类型与含义检查。

必背四元组：G = (Vn, Vt, S, P)。必背四型：3 正规、2 上下文无关、1 上下文有关、0 无限制。

计算/推导题习惯：遇到“是否句子”就从 S 画推导；遇到“几型”就逐个产生式看形状；遇到“二义”就找两种最左推导。

命题信号词：“识别单词”是词法；“语法树”是语法；“类型不匹配”是语义；“优化”只改效率不改语义；“右线性”是 3 型。`,
    quizzes: [
      {
        id: 'sd-q-02-01a',
        type: 'choice',
        question: '编译程序对源程序进行词法分析所依据的语言规则是（）。',
        options: ['语义规则', '语法规则', '词法规则', '操作规则'],
        answer: 'C',
        explanation: '词法分析依据词法规则把字符流识别为单词符号；语法分析依据语法规则（上下文无关文法）构造语法树；语义分析依据语义规则检查含义与类型。',
      },
      {
        id: 'sd-q-02-01b',
        type: 'choice',
        question: '设有文法 G：S -> AB，A -> a，B -> b。则下列说法正确的是（）。',
        options: [
          'G 是 3 型文法',
          'G 是 0 型文法而不是 1 型文法',
          'ab 是 G 的句子',
          'AB 是 G 的句子'
        ],
        answer: 'C',
        explanation: 'S => AB => aB => ab，ab 只含终结符且可由 S 推出，是句子；AB 是句型不是句子。该文法各产生式左部为单非终结符、右部不收缩，属于 2 型上下文无关文法；S -> AB 右部是两个非终结符，不满足右线性，故不是 3 型。',
      },
      {
        id: 'sd-q-02-01c',
        type: 'choice',
        question: '关于编译与解释的比较，正确的是（）。',
        options: [
          '解释方式会生成完整的目标程序文件',
          '编译方式每次运行都要重新编译源程序',
          '解释方式一般不生成可脱离源程序独立运行的目标程序',
          '编译方式和解释方式都不能进行优化'
        ],
        answer: 'C',
        explanation: '解释方式通常边翻译边执行，不产生可独立保存的目标程序。编译方式一次编译可多次运行目标程序；编译各阶段（尤其代码优化阶段）可以做优化，解释器内部也可有优化但题干说“都不能”显然错误。',
      },
      {
        id: 'sd-q-02-01d',
        type: 'choice',
        question: '按 Chomsky 分型，产生式形如 A -> aB 或 A -> a（A、B 为非终结符，a 为终结符或空串）的文法属于（）。',
        options: ['0 型文法', '1 型文法', '2 型文法', '3 型文法'],
        answer: 'D',
        explanation: '右线性（或左线性）产生式构成的文法是 3 型正规文法，描述词法，等价于有限自动机。',
      },
    ],
    references: [],
  },
  {
    id: 'sd-02-02',
    moduleId: 'sd-programming-language',
    title: '词法与语法分析',
    description: '正规式与有限自动机、NFA/DFA、词法分析器设计、自上而下与自下而上语法分析、LR 与 LL、递归下降、算符优先，上午题高频。',
    content: `# 词法与语法分析

## 考情分析

「词法与语法分析」是软件设计师程序设计语言模块中技术含量最高、也最稳定出题的小节，上午题约 1～2 分，偶尔在下午题编译原理相关描述题中间接出现。高频考点：正规式与有限自动机的对应与简单判定、NFA 与 DFA 的区别及等价转换概念、词法分析器的功能与单词类别、语法分析的两大类方法（自上而下、自下而上）、LL(1) 文法条件、递归下降分析、算符优先与 LR 分析器的基本概念、简单文法的最左/最右推导与归约步骤。

复习优先级：分析方法分类与代表 > 正规式识别 > 简单推导/归约 > LL(1) 与 LR 的特点对比 > 二义性与冲突（移进-归约、归约-归约）。

## 一、核心概念

### 1. 词法分析的地位与任务

词法分析是编译第一阶段，由词法分析器（扫描器）完成，从左到右读入源程序字符流，按词法规则组合成记号（Token）。Token 一般用二元组表示：（单词类别，属性值）。例如标识符算子 foo，类别是 id，属性指向符号表项；常数 123，类别是 number，属性是数值。

单词类别通常分五类：关键字、标识符、常数、运算符、界符（分号、括号等）。

实现词法分析器的方法有三种常考线索：用状态转换图手工编写；用正规式描述词法；用词法分析器生成器（如 lex/flex）根据正规式自动产生。

### 2. 正规式与有限自动机

正规式（正则表达式）描述单词的词法规则。基本运算：连接、选择（或运算，常用加号或竖线表示）、闭包（星号，零次或多次）。例如标识符可写为 letter (letter | digit)*。

有限自动机 FA 分为：

确定有限自动机 DFA：每个状态对每个输入符号至多一个转移，输入唯一确定下一状态；可有可无的终态；模拟执行时状态路径唯一。

非确定有限自动机 NFA：一个状态对同一输入可有多个转移，还可以有空转移；有多条可能路径。

重要结论（考概念即可）：DFA 与 NFA 能识别的语言类相同，都是正规集，都与正规式等价；任何 NFA 都可等价转换为 DFA（子集构造法）；正规式可转成 NFA（Thompson 构造）；DFA 也可化简。

词法分析用 FA 判断“是否接受”：读完整个单词符号后，若停在终止状态（终态），则接受该串。

### 3. 语法分析两大策略

语法分析输入是记号序列，任务是按上下文无关文法检查结构并构造语法树。两大类方法：

（1）自上而下分析（从开始符号出发向下推导，试图生成输入串）

基本做法：从开始符号 S 开始，不断用产生式右部替换当前句型中的非终结符，看能否匹配输入。

递归下降分析法：为每个非终结符编一个递归过程，在过程中按产生式展开。直观、好写、易调试，适合较小文法。

预测分析法（LL 分析）：显式维护分析栈，查 LL(1) 分析表决定用哪条产生式。LL(1) 表示从左到右扫描、最左推导、向前看 1 个符号。

自上而下的致命问题是回溯与左递归：若文法有左递归（A -> Ab | ...），直接递归下降会无限循环，必须改写成右递归或用循环；若同一非终结符的多条产生式左部公共前缀相同且仅凭 1 个前看符号无法区分，则需要提取公共左因子或增加前看。

LL(1) 文法的基本条件（概念理解即可）：无左递归；对每个非终结符的各产生式，其可推出的首符号集互不相交，且与能跟在该非终结符后的符号（FOLLOW 集）配合满足选择条件。FIRST 集与 FOLLOW 集是构造 LL(1) 表的基础。

（2）自下而上分析（从输入串出发向上归约，最终归到开始符号）

基本做法：移进输入符号，当栈顶出现某产生式右部时归约为左部非终结符。是“最右推导的逆过程”，对应最左归约。

算符优先分析法：按运算符之间的优先关系决定移进或归约，适合表达式分析。

LR 分析法：规范的自下而上方法，比算符优先更一般。LR 分析器 = 驱动程序 + 分析表（ACTION/GOTO）+ 分析栈。LR(0)、SLR(1)、LALR(1)、LR(1) 是能力递增（表也更复杂）的系列。LALR(1) 在实践中最常用（如 YACC/Bison）。

LR 分析的冲突：移进-归约冲突、归约-归约冲突。出现冲突通常说明文法有二义，或该文法不属于当前 LR 级别。

### 4. 两大类方法对比

自上而下：面向产生式、从抽象到具体、可能回溯、忌左递归，代表 LL、递归下降。
自下而上：面向输入、从具体到抽象、移进与归约、忌移进-归约冲突，代表 LR、算符优先。
一般认为，能被自上而下分析的文法集合是自下而上能分析文法集合的子集（在同一文法类前提下的通俗说法），LR 比 LL 能力更强，很多文法适合 LR 不适合 LL(1)。

## 二、关键原理/公式/机制

### 1. 正规式与 FA 的对应

正规式三种基本构造思想：选择用“分叉转移”，连接用“串接转移”，闭包用“回边与空转移”。复杂正规式可分解为子表达式拼装。

### 2. 推导与归约的方向

最左推导：每次替换当前句型最左边的非终结符。
最右推导（规范推导）：每次替换最右边的非终结符。
规范句型：由最右推导得到的句型。
自下而上的规范归约是最右推导的逆序。

### 3. FIRST 与 FOLLOW（概念级）

FIRST(α)：可从 α 推出的所有串的首终结符集合。
FOLLOW(A)：可能紧跟在非终结符 A 后面的终结符集合，常在构造 LL(1) 表时用到。
LL(1) 选择条件：对产生式 A -> α | β，需 FIRST(α) 与 FIRST(β) 不相交；若 α 可推空，则还需 β 的 FIRST 与 FOLLOW(A) 满足不冲突条件。

### 4. LR 项目与状态

LR 分析的核心是“项目”（产生式加点），点表示已识别到右部的哪个位置。项目集闭包与转移构成状态机，再生成 ACTION/GOTO 表。考题只需记住：点表示进度，移进对应点后是终结符，归约对应点已在右部末尾。

## 三、常考题型与解题方法

题型一：分析方法归类题。问“递归下降属于哪类”“LR 属于哪类”。方法：自上而下记“递归下降、预测、LL”；自下而上记“算符优先、LR”。

题型二：正规式与自动机识别题。给一个正规式或 FA，问能否接受某串。方法：用正规式展开匹配，或在 FA 上走一遍状态转移，看是否到终态。

题型三：推导/归约序列题。给文法与句子，写最左推导或归约步数。方法：最左推导永远展开最左非终结符；归约则反过来找句柄（最左素短语/规范句型中的待归约子串）。

题型四：LL/LR 特征辨析。方法：见“左递归、公共前缀、回溯”选自上而下困难点；见“移进-归约冲突”选 LR。

## 四、典型例题

例 1：递归下降分析方法属于（）。
A. 自下而上分析
B. 自上而下分析
C. 算符优先分析
D. LR 分析

解析：递归下降为每个非终结符写一个递归过程，从开始符号出发做推导匹配输入，属于自上而下分析。答案选 B。

例 2：若文法存在左递归，则（）。
A. 适合用递归下降分析法直接实现
B. 不适合用自上而下分析法直接实现
C. 一定不是上下文无关文法
D. 一定不能用 LR 分析

解析：左递归会让自上而下（尤其递归下降）陷入无限递归，必须先改写；但左递归文法完全可以是上下文无关文法，也完全可以被 LR 分析。答案选 B。

例 3：关于 DFA 与 NFA 的叙述，正确的是（）。
A. NFA 能识别的语言类比 DFA 更大
B. 任何 DFA 都不能转换为 NFA
C. NFA 与 DFA 识别的语言类相同
D. DFA 必须有多个终止状态

解析：NFA 可通过子集构造等价转换为 DFA，二者都恰好识别正规集，语言类相同。DFA 终止状态可有 0 个或多个。答案选 C。

例 4：LR 分析过程中，若分析表在某状态同时要求“移进”和“归约”，则发生（）。
A. 归约-归约冲突
B. 移进-归约冲突
C. 左递归
D. 二义文法的必然结果，但不能称冲突

解析：同一状态同一符号下既可移进又可归约，是移进-归约冲突，常因文法二义或超出当前 LR 级别引起。答案选 B。

## 五、易错点

易错点 1：把递归下降当成自下而上。递归下降是标准的自上而下分析。

易错点 2：以为左递归文法不是上下文无关文法。左递归与文法类型无关，2 型文法完全可以左递归。

易错点 3：混淆推导方向与归约方向。自上而下用推导（S 往下），自下而上用归约（输入往上到 S）；规范归约对应最右推导的逆过程。

易错点 4：认为 DFA 比 NFA 能力更强。能力等价，DFA 只是无不确定性、状态数可能更多。

易错点 5：把算符优先与 LR 混为一谈。算符优先按运算符优先级处理，属自下而上但不是完整 LR；LR 是更一般的规范方法。

易错点 6：LL(1) 的“1”被误解为“一个产生式”。它指向前看一个终结符。

## 六、考点提示

必背分类表：自上而下——递归下降、预测分析（LL）；自下而上——算符优先、LR(0)/SLR/LALR/LR(1)。

必背：正规式与 FA 等价；NFA 可确定化；左递归阻碍自上而下；移进-归约冲突与 LR 相关；最右推导的逆是最左归约/规范归约。

做推导题先标非终结符与终结符，一步一步替换，不要跳步；做 FA 题画状态转移箭头，空转移要单独标。

命题信号词：“扫描器/扫描程序”是词法；“移进、归约”是自下而上；“递归过程”是递归下降；“ACTION/GOTO 表”是 LR。`,
    quizzes: [
      {
        id: 'sd-q-02-02a',
        type: 'choice',
        question: '递归下降分析法属于语法分析方法中的（）。',
        options: ['自下而上分析', '自上而下分析', '算符优先分析', 'LR 分析'],
        answer: 'B',
        explanation: '递归下降为每个非终结符编写递归过程，从开始符号出发进行推导以匹配输入，是典型的自上而下分析方法。',
      },
      {
        id: 'sd-q-02-02b',
        type: 'choice',
        question: '若文法含有左递归，则（）。',
        options: [
          '不适合直接采用自上而下的分析方法',
          '该文法一定不是上下文无关文法',
          '该文法不能用任何 LR 分析器分析',
          '该文法不能描述任何语言'
        ],
        answer: 'A',
        explanation: '左递归会使自上而下分析（尤其是递归下降）陷入无限递归，必须改写为右递归或引入循环。左递归文法仍然是上下文无关文法，并且通常很适合 LR 分析。',
      },
      {
        id: 'sd-q-02-02c',
        type: 'choice',
        question: '下列关于 DFA 与 NFA 的叙述中，正确的是（）。',
        options: [
          'NFA 能识别的语言集合比 DFA 更大',
          '只有 NFA 可以识别正规集，DFA 不行',
          'NFA 与 DFA 所识别的语言类相同，都是正规集',
          'DFA 对同一输入符号一定有多个转移'
        ],
        answer: 'C',
        explanation: 'DFA 与 NFA 描述能力相同，都识别正规集，且任何 NFA 都可等价转换为 DFA。DFA 的特征是同一输入至多一个转移，而不是多个。',
      },
      {
        id: 'sd-q-02-02d',
        type: 'choice',
        question: 'LR 分析表的同一状态、同一输入符号下若同时存在移进项目和归约项目，则产生的冲突是（）。',
        options: ['左递归冲突', '移进-归约冲突', '归约-归约冲突', 'FIRST/FOLLOW 冲突'],
        answer: 'B',
        explanation: '同一状态下既可移进又可归约，称为移进-归约冲突，常见于二义文法或当前 LR 级别无法处理的文法。',
      },
    ],
    references: [],
  },
  {
    id: 'sd-02-03',
    moduleId: 'sd-programming-language',
    title: '语义分析与中间代码',
    description: '语义分析与类型检查、中间代码形式（三地址码、四元式、逆波兰式）、优化技术、目标代码生成与运行时存储，软件设计师综合考点。',
    content: `# 语义分析与中间代码

## 考情分析

「语义分析与中间代码」在软件设计师上午题中约 1 分，常与前一课的语法分析或后继的“程序设计语言概述”结合出题。高频考点：语义分析要做什么（类型检查、作用域、类型转换）、中间代码的常见形式及其表达（特别是逆波兰式与三地址码/四元式）、优化类型的识别（常量合并、公共子表达式、强度削弱、代码外提）、静态存储分配与动态存储分配（栈式分配）的适用范围、目标代码生成的基本职责。

复习优先级：中间代码形式（尤其是表达式转逆波兰式）大于优化类型辨析大于语义分析任务清单大于存储分配策略。逆波兰式和四元式若出题几乎就是送分计算，必须会手算。

## 一、核心概念

### 1. 语义分析的任务

语义分析处于语法分析之后，作用在语法结构上，检查“含义是否合法”，并可为后续阶段收集属性信息。主要任务：

静态语义检查：类型检查（运算对象类型是否匹配，如布尔变量能否参与乘法）、类型转换与类型推导（整型提升为实型）、作用域与可见性检查（名字是否先声明后使用）、控制流合法性（return、break 是否出现在允许位置）、数组下标与维数检查、过程调用的实参与形参个数与类型一致性。

属性文法：在上下文无关文法的产生式上附着语义规则/语义动作，把语法与语义结合。综合属性自下而上传递信息，继承属性自上而下或自兄弟传递。语法制导翻译即按语法分析过程执行语义动作，边分析边生成中间代码。

符号表支撑：语义分析频繁查填符号表，获得标识符的类型、地址、作用域嵌套层次。

注意：动态的运行时错误（如除零、越界）不属于编译期语义分析，而是运行期检查；语义分析管的是编译期能确定的正确性。

### 2. 中间代码及其形式

中间代码是与具体机器低层细节无关、又便于优化与翻译的表示。作用：使编译程序结构清晰、便于优化、便于移植、便于跨语言共享后端。

常见形式：

后缀式（逆波兰式）：运算符跟在操作数之后，表达式不需要括号，用栈求值非常方便。例如 a + b * c 的逆波兰式是 abc*+。

三地址码：形如 x = y op z，每条指令至多一个运算符，地址是名字、常数或编译器生成的临时变量。清晰规范。

四元式：四元组（运算符, 操作数1, 操作数2, 结果）。例如 ( +, a, b, t ) 表示 t = a + b。常考“把表达式写成四元式序列”。

三元式：（运算符, 操作数1位置, 操作数2位置），用位置引用结果，可节省临时变量记录，但优化改写较麻烦。

抽象语法树 AST：略去部分纯语法装饰后的语法树，现代编译器核心表示之一。

树、有向无环图 DAG：DAG 可表达公共子表达式共享，有利于优化。

### 3. 代码优化

优化在不改变程序语义（可观察行为）的前提下改进时空效率。按范围分局部优化（基本块内）与全局优化（跨基本块、循环优化）。

常见优化技术（考试几乎只考识别名字与含义）：

常量合并与常量传播：编译时把常量运算算掉，把常数往使用点传播。
公共子表达式消除：同一表达式已计算过则复用结果。
复写传播：尽量把 x = y 中的 y 传到后续 x 的使用处，减少无用赋值。
死代码消除：永远执行不到或结果从未被使用的代码删除。
强度削减：把昂贵运算变廉价，如乘 2 改为移位，循环中的归纳变量用加法递推代替乘法。
代码外提：把循环不变量运算提到循环外，避免重复计算。
合并已知量、变换控制流、削减目标代码长度等。

优化级别（如 -O0、-O2）体现优化程度，不是考题重点，但要知道优化可多轮反复。

### 4. 目标代码生成与运行时存储

目标代码生成：把中间代码映射为目标指令，需做指令选择、寄存器分配（减少访存）、内存分配与重定位、对齐与调用约定。生成形式可以是绝对机器代码、可重定位目标模块、或汇编指令流。

运行时存储分配策略：

静态分配：在编译期确定所有数据对象的地址，适合全程量、定长、无递归的场景。简单但不支持递归与动态数据结构。

栈式分配：把过程的活动记录（局部变量、参数、返回地址、临时变量）压入运行栈，调用时入栈、返回时出栈。支持递归，C/Pascal 等过程式语言主要用栈与堆配合。

堆式分配：用于动态创建的、生命周期不确定的数据对象（如 malloc/new 出来的对象），需要垃圾回收或显式释放。

过程调用的一般活动记录包含：返回地址、动态链、静态链、参数区、局部变量区、临时单元。动态链指向调用者活动记录，用于返回；静态链用于访问外层非局部变量。

## 二、关键原理/公式/机制

### 1. 表达式转逆波兰式（后缀式）

规则：按运算优先级与结合性去掉括号后写成“操作数序列 + 运算符后置”。括号内先处理。

例：把 (a + b) * (c - d) 写成逆波兰式。
步骤：先处理 a + b 得 ab+，再处理 c - d 得 cd-，最后乘法置后，结果是 ab+cd-*。

例：a + b * c，乘法优先，先 b*c 再加 a，结果 abc*+。

求值用栈：读到操作数入栈，读到运算符弹出两个操作数运算，结果再入栈。

### 2. 表达式转四元式

规则：自底向上为每个子表达式分配临时变量 t1、t2 …，每条四元式至多一个运算符。

例：x = a + b * c。
先乘：( *, b, c, t1 )
再加：( +, a, t1, t2 )
再赋值：( =, t2, -, x ) 或写作 ( :=, t2, x, - ) 形式，教材写法略有差异，核心是“运算符 + 两操作数 + 结果”四元组顺序。

### 3. 基本块与 DAG（概念）

基本块：程序中只能从其入口进入、从其出口离开的顺序指令序列，无分支跳入跳出中间。局部优化多在基本块内用 DAG 表示，便于发现公共子表达式与死代码。

## 三、常考题型与解题方法

题型一：语义分析职责题。方法：排除“只关心符号怎么拼”的词法项和“只关心结构怎么排”的语法项，剩下的类型、作用域、声明、转换归语义。

题型二：表达式转逆波兰式/四元式计算题。方法：加括号显式表示优先级，逐步生成，临时变量从 t1 开始顺序分配。逆波兰式做完可用栈求值验算。

题型三：优化技术识别题。方法：看关键词——“编译时算出常数”是常量合并/传播；“同一表达式再算”是公共子表达式；“乘改移位”是强度削减；“不变运算提到循环外”是代码外提；“删掉永假分支”是死代码消除。

题型四：存储分配策略题。方法：编译期定址是静态；递归过程活动记录进出是栈式；new/malloc 与 GC 是堆式。

## 四、典型例题

例 1：表达式 (a + b) * (c - d) 的逆波兰式是（）。
A. ab+cd-*
B. abcd+ -*
C. +ab*-cd
D. ab+*cd-

解析：先按括号算子表达式，a + b 的逆波兰为 ab+，c - d 为 cd-，乘号放在两操作对象之后，整体 ab+cd-*。答案选 A。

例 2：赋值语句 x = a + b * c 对应的四元式序列是（）。
A. ( +, a, b, t1 ); ( *, t1, c, x )
B. ( *, b, c, t1 ); ( +, a, t1, x )
C. ( +, a, b, t1 ); ( *, t1, c, t2 ); ( =, t2, -, x )
D. ( *, a, b, t1 ); ( +, t1, c, x )

解析：先乘后加，且乘法操作数是 b 与 c。所以 ( *, b, c, t1 )，再 ( +, a, t1, x ) 可直接给出赋值目标 x（有的教材多一步赋值四元式，但运算顺序必须是先 * 后 +，且 * 的操作数是 b、c）。选项 B 运算顺序与操作数均正确。答案选 B。

例 3：把循环体内部不变的表达式运算提到循环之外，这种优化属于（）。
A. 常量合并
B. 死代码消除
C. 代码外提
D. 强度削减

解析：代码外提（loop-invariant code motion）就是把循环不变量的计算移到循环前置头或循环外。答案选 C。

例 4：在编译程序中，完成类型检查与类型转换的阶段是（）。
A. 词法分析
B. 语法分析
C. 语义分析
D. 目标代码生成

解析：类型是否合法、如何转换是语义分析的静态语义检查内容。答案选 C。

## 五、易错点

易错点 1：逆波兰式弄错运算顺序。必须尊重原表达式优先级与结合性，不能简单“把符号都挪后面”。(a+b)*(c-d) 不等于 ab+cd*- 以外的顺序。

易错点 2：四元式里操作数顺序写反。a + b * c 若写成 ( +, a, b, t1 ) 再乘 c，就改变了语义。

易错点 3：把动态越界、除零当成语义分析职责。这些是运行期检查。

易错点 4：优化改变语义。优化必须保持可观察行为不变，这是优化的定义性约束。

易错点 5：把静态分配与栈式分配混用。有递归过程时局部变量必须用栈或堆动态分配，静态分配无法保存多个活跃的递归副本。

易错点 6：逆波兰式求值栈序。二元运算应先弹右操作数再弹左操作数，减法与除法尤其容易顺序反。

## 六、考点提示

必背定义：语义分析 = 类型检查 + 作用域 + 类型转换 + 控制流合法性；中间代码形式 = 逆波兰式、三地址码、四元式、三元式、AST/DAG；优化类型五连 = 常量合并、公共子表达式消除、死代码消除、强度削减、代码外提。

手算模板：表达式转逆波兰式——加括号，递归处理子表达式，操作数保持相对顺序，运算符后置；转四元式——从最内层子表达式开始，临时变量递增编号。

存储分配口诀：“全局长用静态，递归局部用栈，动态对象用堆。”

命题信号词：“类型不匹配”是语义；“t1、t2 临时变量”是四元式/三地址码；“把运算符放到后面”是逆波兰式；“循环不变量”是代码外提；“乘法改移位”是强度削减。`,
    quizzes: [
      {
        id: 'sd-q-02-03a',
        type: 'choice',
        question: '表达式 (a + b) * (c - d) 的逆波兰式（后缀式）是（）。',
        options: ['ab+cd-*', 'abcd+ -*', '+ab*-cd', 'ab+*cd-'],
        answer: 'A',
        explanation: 'a + b 的逆波兰式是 ab+，c - d 是 cd-，最后做乘法，运算符后置得到 ab+cd-*。',
      },
      {
        id: 'sd-q-02-03b',
        type: 'choice',
        question: '语义分析阶段所完成的工作是（）。',
        options: [
          '识别单词并输出记号序列',
          '根据上下文无关文法构造语法分析树',
          '进行类型检查、类型转换与作用域分析',
          '把优化后的中间代码变成目标指令'
        ],
        answer: 'C',
        explanation: '类型检查、类型转换、名字作用域与控制流合法性属于语义分析。A 是词法分析，B 是语法分析，D 是目标代码生成。',
      },
      {
        id: 'sd-q-02-03c',
        type: 'choice',
        question: '在编译优化中，把循环体内不随循环变化的运算移到循环体外，这种技术称为（）。',
        options: ['常量合并', '死代码消除', '强度削减', '代码外提'],
        answer: 'D',
        explanation: '代码外提针对循环不变量，将其计算放到循环之前或之后，避免每轮重复计算。强度削减是把高价运算改为低价运算（如乘改移位）。',
      },
      {
        id: 'sd-q-02-03d',
        type: 'choice',
        question: '赋值语句 x = a + b * c 用四元式表示时，正确的序列是（）。',
        options: [
          '( +, a, b, t1 ); ( *, t1, c, x )',
          '( *, b, c, t1 ); ( +, a, t1, x )',
          '( +, a, b, t1 ); ( *, t1, c, t2 )',
          '( *, a, c, t1 ); ( +, b, t1, x )'
        ],
        answer: 'B',
        explanation: '乘法优先于加法，先算 b * c 得临时变量 t1，再算 a + t1 赋给 x，对应四元式 ( *, b, c, t1 ) 与 ( +, a, t1, x )。',
      },
    ],
    references: [],
  },
];
