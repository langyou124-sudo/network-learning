import { Topic } from '@/types';

export const topics: Topic[] = [
  {
    id: 'sd-08-01',
    moduleId: 'sd-algorithms',
    title: '算法分析基础',
    description: '时间空间复杂度、渐进记号、递推式与主定理',
    content: `# 算法分析基础

## 考情分析

- **分值**：上午综合知识 2～4 分（复杂度判断、递推式求解、排序查找复杂度对比）；下午算法题第一问常要求“分析你的算法复杂度”，约 2～3 分。近年趋势是把复杂度与具体代码阅读结合，不直接考定义默写
- **题型**：选择（给代码问复杂度、给增长量级排序）、填空（递推式解、复杂度上界）、简答（分析算法时间/空间）
- **考频**：★★★★ 基础必考。送分点多，但主定理与递推式易错

## 一、核心概念

### 1. 什么是算法，五特性

算法是解决特定问题的有限指令序列，具有：**有穷性、确定性、可行性、输入、输出**。设计目标除了正确，还要**高效**（时间/空间少）与**可读**。

### 2. 时间复杂度与空间复杂度

**时间复杂度** T(n)：算法执行时间随问题规模 n 的增长量级，用渐进记号表示，不看具体机器快慢，只数**基本操作**次数（比较、赋值、加减乘除、读写等）的量级。

**空间复杂度** S(n)：除输入数据外，算法额外占用存储随 n 的增长量级，含辅助变量、递归栈、动态分配。注意：**输入规模本身不计入**“额外空间”，但若题目问“总空间”则要加上输入。

**最坏、最好、平均**：
- 最坏时间复杂度 W(n)：任何输入下的上界（最重要，常用于保证性能）
- 最好时间复杂度 B(n)：最优输入下的下界
- 平均时间复杂度 A(n)：输入概率分布下的期望，需知分布才可谈

例如顺序查找：最好 O(1)（第一个就中），最坏 O(n)（最后或不中），平均约 n/2 次比较即 O(n)。

### 3. 渐进记号

设 f(n)、g(n) 为正函数：
- **O（大 O）**：f(n) = O(g(n)) 表示存在 c、n0，当 n≥n0 时 f(n) ≤ c·g(n)。**上界**。
- **Ω（大 Ω）**：下界。
- **Θ（大 Θ）**：紧确界，既是 O 又是 Ω。
- **o、ω**：非紧的严格上/下界，软考较少深究，但要知 o(g) 是比 g 增长严格更慢。

常数量级：O(1) < O(log n) < O(n) < O(n log n) < O(n^2) < O(n^3) < O(2^n) < O(n!)

**运算规则速记**：两段顺序执行取高阶（加法时大 O 吸收小 O）；嵌套相乘；若 f=O(g) 则 f+g=O(g)；常数因子与低次项可忽略，故 3n^2+5n+1 = O(n^2)。比较两个阶时先取增长最快项；只有最高次相同时才看系数或对数幂次。

### 4. 分析方法套路

1. 循环：乘法法则——多层嵌套循环次数相乘；循环内是 O(1) 则总时间 = 次数。
2. 顺序语句：加法法则——取最大量级。
3. 分支：最坏情况取代价最大分支。
4. 递归：写递推式 T(n) = aT(n/b) + f(n)，再用主定理或展开求解。
5. 记住常见数列：1+2+…+n = n(n+1)/2 = Θ(n^2)；1+2+2^2+…+2^k = 2^{k+1}-1。

**循环次数的三种典型形态**：
- 循环变量 i 从 1 到 n 步长 1：n 次。
- 循环变量 i 从 1 到 n，每次翻倍或减半：约 log n 次。
- 内层 j 到 i 或 n-i：需要求和，结果常是 Θ(n^2)。
下午题写分析时，先数最内层基本操作，写成关于 n 的表达式，再化到渐进阶，比直接猜阶更稳。

### 5. 算法设计目标与评价

除了时间、空间，还要考虑正确性、可读性、健壮性（非法输入的处理）。工程上有时用**摊还分析**看一系列操作的平均代价，例如动态数组扩容每次 O(n)，但 n 次插入摊还 O(1)。软考只要求知道：单看某次操作最坏 O(n) 不代表总代价就是 O(n^2)。

## 二、关键原理/方法

### 0. 常见算法复杂度对照（务必眼熟）

顺序查找 O(n)；有序折半查找 O(log n)；二叉搜索树查找平均 O(log n)、最坏 O(n)（退化成链）；平衡二叉树查找 O(log n)；散列表查找平均 O(1)、最坏 O(n)。
直接插入、冒泡、简单选择排序 O(n^2)；希尔排序约 O(n^1.3) 量级；快速、归并、堆排序平均 O(n log n)，其中快排最坏 O(n^2)，归并与堆排最坏仍是 O(n log n)；基数排序 O(d(n+k))。
空间：冒泡、插入、选择、快排（不计栈）、堆排序原地 O(1) 或 O(log n) 栈；归并 O(n)；基数 O(n+k)。
稳定性：稳定的是插入、冒泡、归并、基数；不稳定的是选择、希尔、快排、堆排。选择题爱考“下列哪项是不稳定排序”。

### 1. 主定理（分治递推 T(n)=aT(n/b)+f(n)，a≥1，b>1）

令 c_crit = log_b(a)（即 n 的临界指数）：
1. 若 f(n) = O(n^{c_crit - ε})，ε>0，则 T(n) = Θ(n^{c_crit})。即“合并很轻，根为主”。
2. 若 f(n) = Θ(n^{c_crit} · (log n)^k) 且 k≥0，则 T(n) = Θ(n^{c_crit} · (log n)^{k+1})。即“平衡”，多一个 log。
3. 若 f(n) = Ω(n^{c_crit + ε})，ε>0，且正则条件 a·f(n/b) ≤ c·f(n)（c<1）成立，则 T(n) = Θ(f(n))。即“合并很重，叶可忽略”。

**典型套用**：
- 归并/二分递归：T(n)=2T(n/2)+Θ(n) → a=2,b=2,c_crit=1，f=n^1，落入第 2 类（k=0）→ Θ(n log n)。
- 二分查找：T(n)=T(n/2)+Θ(1) → a=1,c_crit=0，f=Θ(1) → 第 2 类 k=0 → Θ(log n)。
- 最大子数组分治（教科书）：T(n)=2T(n/2)+Θ(n) → Θ(n log n)。
- 矩阵相乘朴素：T(n)=8T(n/2)+Θ(n^2) → c_crit=3，f=n^2 为轻 → Θ(n^3)；Strassen：a=7 → Θ(n^{log_2 7}) ≈ Θ(n^2.81)。

**主定理不适用时**：用递归树展开或迭代法。例如 T(n)=2T(n/2)+Θ(n log n)，f 带 log 落在边界情形，解为 Θ(n (log n)^2)。

### 2. 递归树直观

从根开始每层代价相加。高度约 log_b n，叶子数 a^{log_b n} = n^{log_b a}。总代价 = 各层代价之和：
- 各层等比增 → 叶子主导；
- 各层近似相等 → 各层均摊，总为 n 的幂乘 log；
- 各层递减 → 根主导。

### 3. 空间复杂度的两个要点

- **递归栈**：递归深度 × 每层局部变量。二分查找深度 O(log n)；朴素快速排序最坏栈深 O(n)，平均 O(log n)；归并若原地合并不用额外数组则为 O(n) 的临时区，整体 O(n) 或 O(log n) 取决于实现。
- **原地 in-place**：额外空间 O(1) 称原地。堆排序是原地 O(1)；归并排序非原地 O(n)；计数排序 O(n+k) 额外空间。

### 4. 如何估算自己写的伪代码（下午题）

- 数最内层基本操作的执行次数，写成 n 的表达式再化阶。
- 双重循环且内层上界与外层变量相关：求和 1+2+…+n → n^2 阶。
- 递归：先写递推式，若形如减治 T(n)=T(n-1)+O(n) 则 T(n)=Θ(n^2)；若 T(n)=T(n/2)+O(1) 则 Θ(log n)。
- 答案格式：“时间复杂度 O(…)，空间复杂度 O(… )（含/不含递归栈）”。

## 三、常考题型与解题方法

**题型 1：给代码问时间复杂度**。方法：把代码翻译成操作次数求和式。看循环变量增量（i=i*2 则 log n；i=i+1 则 n），嵌套相乘，相加取 max。

**题型 2：递推式求解**。方法：优先套主定理三情形；边界/对数幂次卡在分界就用递归树展开求和。

**题型 3：复杂度排序/选择最优算法**。方法：写出各选项复杂度再按增长量级排序；n 很大时 n log n 优于 n^2。

**题型 4：空间复杂度**。方法：找最大同时存活的数据结构大小 + 最大递归深度。若问“辅助空间”，不含输入与输出。

**题型 5：比较算法优劣 / 选择排序**。方法：按题目约束过滤——稳定吗？最坏是否 O(n log n)？是否原地？数据规模大时平均复杂度优先，数据基本有序时插入排序反而更好。把约束写成勾选表再匹配算法名。

## 四、典型例题

**例 1（选择）** 以下程序段的时间复杂度是（）。
for (i = 1; i <= n; i = i * 2)
  for (j = 0; j < i; j++)
    s++;

A. O(n) B. O(n log n) C. O(n) 的平方级即 O(n^2) D. O(log n)

**解析**：选 A（O(n)）。外层 i 依次 1,2,4,…,2^k ≤ n，约 log n 轮，但**内层次数依赖 i**：总次数 = 1+2+4+…+2^{⌊log n⌋} = 2^{⌊log n⌋+1}-1 = Θ(n)。错误选项 C 是把内层当 n；B 是把外层 log n 与内层 n 相乘；D 只数了外层。

**例 2（主定理）** T(n) = 4T(n/2) + n，T(1)=Θ(1)。求 Θ 解。

**解析**：a=4，b=2，c_crit = log_2 4 = 2，f(n)=n = O(n^{2-1})，落入主定理第 1 类 → T(n)=Θ(n^2)。

**例 3（填空/分析）** 写出二分查找（迭代实现）在最坏情况下的比较次数与时间复杂度；若改为递归实现，空间复杂度是多少？

**解析**：迭代二分最坏比较次数 ⌈log_2(n+1)⌉ 次（或 ⌊log_2 n⌋+1 量级），时间复杂度 O(log n)。递归实现时间同为 O(log n)，空间复杂度为递归栈 O(log n)；迭代实现若只用 lo、hi、mid 三个变量则辅助空间 O(1)。

**例 4（求和）** 某算法核心为：
for (i = 1; i <= n; i++)
  for (j = 1; j <= i; j++)
    k++;
求时间复杂度。

**解析**：基本操作次数 S = 1+2+…+n = n(n+1)/2 = Θ(n^2)，时间复杂度 O(n^2)。

**例 5（递推展开）** T(n) = T(n-1) + n，T(1)=1。求阶。

**解析**：迭代展开 T(n)=n+(n-1)+…+1=n(n+1)/2，故 Θ(n^2)。这类“减一治”递推很常见，例如插入排序最坏、打印从 n 到 1 的递归。对比 T(n)=T(n/2)+1 则每次砍半，深度 log n，Θ(log n)。考试中看到 +n 的合并代价且子问题规模只减 1，就要立刻反应是平方级。

**例 6（排序稳定性与复杂度）** 若要求“最坏时间 O(n log n) 且原地、不稳定”，应选哪种排序？若额外要求稳定呢？

**解析**：原地且最坏 O(n log n)、不稳定：**堆排序**。若还要求稳定：**归并排序**（但辅助空间 O(n)，不是原地）；或牺牲原地用归并。快排平均虽好但最坏 O(n^2) 且不稳定，不满足“最坏 O(n log n)”。这类题按复杂度、空间、稳定三张牌同时打。

**例 7（综合判断）** 对 n 个元素作升序排列，要求辅助空间尽量少且算法稳定，时间平均较好。指出可选算法与不选快排的理由。

**解析**：稳定且辅助空间相对可控的首选是**归并排序**（稳定、最坏 O(n log n)，但需 O(n) 辅助）；若更看重原地且能接受 O(n^2) 平均，可用**插入排序**（稳定、原地、小规模很快）。不选快排的理由：快排不稳定，最坏 O(n^2)，且需要递归栈空间，虽平均 O(n log n) 常数小，但不满足“稳定”硬约束。考试答题时把“稳定/原地/最坏”当成筛选条件逐个过滤。

## 五、易错点

1. **内层上界是变量 i 时，不能直接外层次数×n**：必须求和，否则例 1 会错选 n^2 或 n log n。
2. **主定理第 2 类要求 f 带 (log n)^k 形式**：f(n)=Θ(n^{c_crit} log n) 时 k=1，结果多 log^2；别漏看指数。
3. **log 以 2 为底的常数在 Θ 中忽略**：O(log_2 n) 与 O(log_{10} n) 同阶，不影响渐进记号。
4. **时间复杂度不是运行时间**：是量级模型，与硬件无关。
5. **空间复杂度漏算递归栈**：递归二分、递归快排必须写栈深。
6. **平均 ≠ (最好+最坏)/2**：平均是概率加权期望，两者不可随意平均。
7. **f(n)=O(1) 时别把递归次数当总复杂度**：T(n)=T(n/2)+O(1) 的解是 O(log n)，因为递归深度 log n，不是 O(1)。
8. **n^2 与 n log n 的排序**：增长量级 n log n 更优，但小 n 时常数因子可能反杀——题目问复杂度时按阶比大小。
9. **最坏与平均混用**：问“最坏”必须答 O(n^2) 的快排最坏，不能答平均。
10. **基本操作选取不当**：把 I/O 或与规模无关的初始化算进主项，会算错阶；只数随 n 增长的操作。

## 六、考点提示

- 必背三例递推解：二分 Θ(log n)、归并 Θ(n log n)、减治递归 T(n)=T(n-1)+Θ(n) → Θ(n^2)
- 主定理三情形口诀：合并轻看叶、合并重看根、平衡各层均摊
- 下午题答法：先写操作次数表达式或递推式，再给最终 Θ/O 结论，别只写答案
- 与下一课题联动：分治的复杂度就用主定理；DP 的复杂度 = 状态数 × 转移代价
`,
    quizzes: [
      {
        id: 'sd-08-01-q1',
        type: 'choice',
        question: '递推式 T(n) = 2T(n/2) + n，T(1)=Θ(1)，则 T(n) = （）',
        options: ['Θ(n)', 'Θ(n log n)', 'Θ(n^2)', 'Θ(log n)'],
        answer: 'B',
        explanation:
          'a=2, b=2, c_crit=1，f(n)=Θ(n^1 · (log n)^0)，主定理第 2 类 k=0，故 T(n)=Θ(n log n)。归并排序即此式。',
      },
      {
        id: 'sd-08-01-q2',
        type: 'choice',
        question: '下列程序段的时间复杂度为（）：for (i = 1; i <= n; i = i * 2) s++',
        options: ['O(n)', 'O(n log n)', 'O(log n)', 'O(1)'],
        answer: 'C',
        explanation: '外层 i 以 2 倍增长，循环次数约 log_2 n，体内 O(1)，故总时间 O(log n)。',
      },
      {
        id: 'sd-08-01-q3',
        type: 'fill',
        question: '算法复杂度分析中，描述最坏情况执行时间上界的渐进记号是 ______；递归二分查找（递归版）的空间复杂度为 O(______)。',
        answer: ['O', 'log n'],
        explanation: '大 O 表示渐进上界（常与 Θ 混用时写 Θ 更紧）。递归二分深度为 log n，栈空间 O(log n)；迭代版辅助空间才是 O(1)。',
      },
      {
        id: 'sd-08-01-q4',
        type: 'short-answer',
        question: '某算法主干为双重循环：外层 i 从 1 到 n，内层 j 从 1 到 i 执行一次基本操作。求操作次数表达式并给出时间复杂度。',
        answer:
          '操作次数 S(n) = Σ_{i=1 to n} i = n(n+1)/2 = (n^2+n)/2。最高次项为 n^2，故时间复杂度为 Θ(n^2)（或 O(n^2)）。分析要点：内层上界与外层变量相关，必须求和后再定阶，不能直接乘出 n×n 或 n×log n。',
        explanation: '评分要点：求和式 2 分，公式 1.5 分，结论 1.5 分。',
      },
    ],
    references: [],
  },
  {
    id: 'sd-08-02',
    moduleId: 'sd-algorithms',
    title: '分治与动态规划',
    description: '分治步骤与经典算法、DP状态设计与背包LCS矩阵连乘',
    content: `# 分治与动态规划

## 考情分析

- **分值**：上午 2～4 分（问算法思想、时间复杂度、状态转移）；下午算法题（填伪代码、写递推式、算最优值）5～15 分，是高分题。背包与序列类问题常与程序填空结合
- **题型**：选择（分治/DP 归属判断、复杂度）、填空（递推公式、最优值）、程序填空（补 DP 转移或分治合并）、简答（说明状态含义）
- **考频**：★★★★ 下午必练。背包、LCS、矩阵连乘、归并、快排几乎轮流考

## 一、核心概念

### 1. 分治法（Divide and Conquer）

**三步**：① **分解**（Divide）原问题为若干规模更小、结构相同的子问题；② **求解**（Conquer）递归求解子问题，规模足够小时直接解；③ **合并**（Combine）把子问题的解合成原解。

**适用条件**：原问题可分解；子问题与原问题同构且相互**独立**（无重叠）；合并代价可控。

**经典**：二分查找、快速排序、归并排序、大整数乘法（Karatsuba）、Strassen 矩阵乘、棋盘覆盖、循环赛日程表。

复杂度通式（主定理）：T(n) = aT(n/b) + f(n)，a 为子问题数，n/b 为子问题规模，f 为分解合并代价。

### 2. 动态规划（Dynamic Programming，DP）

**两个核心特征**：
1. **最优子结构**：问题的最优解包含子问题的最优解。可写成“最优解 = 若干子问题最优解的组合”。
2. **重叠子问题**：递归展开会反复求解相同的子问题。DP 靠**表格记录**避免重复计算。

**两个实现要点**：
- **状态定义**：明确 dp[i] 或 dp[i][j] 的含义（最优值？方案数？可行性？）。
- **状态转移方程**：dp[i] 由哪些更小状态推出；边界（初值）；计算顺序（自底向上填表 / 自顶向下记忆化）。

**经典**：0-1 背包、完全背包、最长公共子序列 LCS、最长递增子序列 LIS、矩阵连乘、最优二叉搜索树、硬币找零、编辑距离。

### 3. 分治与 DP 的本质区别（必考）

| 对比 | 分治 | DP |
|------|------|----|
| 子问题关系 | 相互**独立**、不重叠 | **重叠** |
| 是否存表 | 通常不存（或仅缓存） | 一定填表/记忆化 |
| 典型 | 归并排序、二分 | 背包、LCS |
| 递归树 | 子问题不共享节点 | 大量子问题共享 |

注意：归并排序是分治**不是** DP，因为左右两半问题不重叠；矩阵连乘是 DP，因为区间子问题大量重叠。

**记忆化搜索与递推的等价性**：自顶向下“递归+备忘录”与自底向上“填表”时间复杂度相同，都是状态数乘以单状态转移代价。写伪代码时选递推更直观，但要保证循环顺序满足依赖：背包外层物品内层容量（或相反，看方程），LCS 按行或按列逐格推，矩阵连乘按区间长度从小到大。

### 4. 常见 DP 问题类型

- **计数型**：数方案数，如走方格、硬币拼法数，转移常用加法。
- **最值型**：求最大价值或最小代价，如背包、编辑距离，转移用 max/min。
- **存在型**：问能否达到，如子集和、能否凑出金额，布尔数组即可。
考试审题先看问的是哪一类，它决定转移是 sum 还是 max/min，以及答案取 dp[n] 还是 max(dp)。

## 二、关键原理/方法

### 1. 0-1 背包（必须默写）

问题：n 件物品，重量 w[i]，价值 v[i]，背包容量 W，每件至多取 1 件，求最大价值。
状态：dp[i][j] = 前 i 件物品、容量 j 下的最大价值。
转移：
dp[i][j] = max( dp[i-1][j]， dp[i-1][j-w[i]] + v[i] )，当 j ≥ w[i]
dp[i][j] = dp[i-1][j]，当 j < w[i]
边界：dp[0][j]=0，dp[i][0]=0。
答案：dp[n][W]。复杂度 O(nW)，空间可滚动数组压成一维 dp[j]，**倒序**遍历容量保证每件只用一次。

**完全背包**：每件无限件。转移同式但一维滚动时容量**正序**遍历。

### 2. 最长公共子序列 LCS

序列 X[1..m]，Y[1..n]。状态 dp[i][j] = X 前缀 i 与 Y 前缀 j 的 LCS 长度。
转移：
- 若 X[i]=Y[j]：dp[i][j] = dp[i-1][j-1] + 1
- 否则：dp[i][j] = max( dp[i-1][j], dp[i][j-1] )
边界：dp[0][j]=dp[i][0]=0。答案 dp[m][n]。复杂度 O(mn)。
回溯可得 LCS 串本身（自 dp[m][n] 往回走）。

### 3. 矩阵连乘

连乘 A1A2…An，给定维度 p0,p1,…,pn（Ai 为 p_{i-1}×p_i）。括号化方案决定乘法次数。
状态 dp[i][j] = 矩阵 Ai…Aj 连乘的最小乘法次数。
转移：
dp[i][j] = min_{i≤k<j} { dp[i][k] + dp[k+1][j] + p_{i-1}·p_k·p_j }
边界 dp[i][i]=0。答案 dp[1][n]。复杂度 O(n^3)。k 枚举的 split 位置可用 s[i][j] 记录以便打印括号方案。

### 4. 分治算法的合并要点

- **归并**：两有序段合并，双指针扫描，代价 Θ(n)；T(n)=2T(n/2)+Θ(n)=Θ(n log n)。稳定。
- **快排**：partition 划分，最坏 T(n)=T(n-1)+Θ(n)=Θ(n^2)（已序），平均 Θ(n log n)；不稳定；原地。
- **二分查找**：T(n)=T(n/2)+Θ(1)=Θ(log n)；要求有序。
- **大整数乘法（朴素分治）**：把 n 位数拆成两个 n/2 位数，做 4 次乘法再加减合并，T(n)=4T(n/2)+Θ(n)=Θ(n^2)；Karatsuba 用 3 次乘法，T(n)=3T(n/2)+Θ(n)=Θ(n^{log_2 3})≈Θ(n^1.59)。这是“减少子问题数 a 换取更高合并代价 f(n)”的典型，主定理第 1 类。
- **循环赛日程表 / 棋盘覆盖**：标准的分治教学题，T(n)=4T(n/2)+Θ(1) 或类似，重点在分解与合并的对称构造。

### 5. 设计 DP 的答题步骤（下午题模板）

1. 用一句话定义状态 dp[…] 含义（必须写进答案）。
2. 写转移方程与取 min/max 或求和的对象。
3. 写边界/初值。
4. 写计算顺序（外层 for 谁、内层 for 谁；0-1 背包容量倒序等）。
5. 复杂度 = 状态数 × 转移代价。

### 6. 两个高频补充模型

**最长递增子序列 LIS**：状态 dp[i] = 以 a[i] 结尾的 LIS 长度。转移：dp[i] = max_{j<i 且 a[j]≤a[i]} (dp[j]) + 1，边界 dp[i]=1。答案 max_i dp[i]。朴素 O(n^2)。思路是“以谁结尾”强制包含最后一项，从而定义可递推的子问题。

**硬币找零（凑成金额 n 的最少枚数）**：币值 c[1..m]。状态 dp[j] = 凑出 j 元的最少枚数。转移：dp[j] = min_k { dp[j-c[k]] + 1 }（j≥c[k]）。边界 dp[0]=0，其余初始化为无穷大。答案 dp[n]。这是完全背包的 min 版；求方案数则把 min 换成 sum，并保证组合不重复（外层循环币、内层循环金额）。

### 7. 滚动数组与路径还原

0-1 背包二维 O(nW) 空间可优化为一维 dp[W+1]，因为第 i 行只依赖第 i-1 行；倒序遍历容量即得。但滚动后若还要求“装了哪些物品”，一般保留二维表或在转移时记录 choose[i][j]，最后从 dp[n][W] 回溯路径。下午题问“哪些物品被选中”时，必须补一句如何还原。

## 三、常考题型与解题方法

**题型 1：判断分治还是 DP**。方法：看子问题是否重叠。排序、二分、平面最近点对 → 分治；最优化+重叠子问题 → DP。快速排序不是 DP。

**题型 2：补状态转移方程**。方法：先自问“最后一步怎么决策/最后哪个元素一定被用”，写出选择结构；背包写“拿/不拿”，LCS 写“末位相等/不等”，矩阵连乘写“最后乘法的断开点”。

**题型 3：手工填表**。方法：严格按转移方程逐格算，别跳步；0-1 背包每行只依赖上一行，填错一行全盘皆错。

**题型 4：复杂度**。方法：状态维数决定数量级——一维 O(n)，两维 O(nm) 或 O(n^2)，矩阵连乘三重枚举 O(n^3)；空间可优化要会说滚动数组。

**题型 5：说算法思想**。方法：先答属于分治还是 DP，再写三步（分解求解合并）或四件套（状态、转移、边界、顺序）。只写“递归”或“最优解”不给满分。

## 四、典型例题

**例 1（0-1 背包手工）** n=3，W=5，w=(2,3,4)，v=(3,4,5)。求最大价值。

**解析**：画 dp[i][j] 表（i 从 0..3，j 从 0..5）。
- 只考虑物品 1（w=2,v=3）：j≥2 的格为 3。
- 加物品 2（w=3,v=4）：dp[2][j]=max(上行, 上行[j-3]+4)。dp[2][5]=max(3, 0+4)=4；dp[2][4]=max(3,1+4?上行[1]=0+4)=4？仔细：
dp[2][3]=max(0,0+4)=4；dp[2][4]=max(3,1? wait 上行 dp[1][1]=0 → 4)=4；dp[2][5]=max(3, dp[1][2]+4=3+4=7)=7。
- 加物品 3（w=4,v=5）：dp[3][5]=max(dp[2][5]=7, dp[2][1]+5=0+5=5)=7。
**答案 7**（取物品 1 和 2）。复杂度 O(nW)=O(15)。

**例 2（LCS）** X=ABCBDAB，Y=BDCABA。求 LCS 长度。

**解析**：按转移填表得 LCS 长度 **4**，一条 LCS 为 BCBA（或 BDAB 等，可能不唯一）。末位相等则加 1，否则取左/上最大。回溯构造序列时从 dp[m][n] 按相等/不等规则走。

**例 3（矩阵连乘）** 三个矩阵维度 (10×100)(100×5)(5×50)。比较 (A1A2)A3 与 A1(A2A3)。

**解析**：
- (A1A2)A3：A1A2 需 10×100×5=5000，得 10×5；再乘 A3：10×5×50=2500；共 7500。
- A1(A2A3)：A2A3 需 100×5×50=25000，得 100×50；再乘 A1：10×100×50=50000；共 75000。
**最优为 (A1A2)A3，7500**。说明括号化对代价影响巨大，故用 DP 求最优加括号顺序。

**例 4（分治）** 用分治策略说明归并排序算法步骤并给出复杂度。

**解析**：分解：把待排序列对半分成两段；求解：递归排序两段；合并：双指针把两个有序段合并成一个有序段。复杂度：T(n)=2T(n/2)+Θ(n)，由主定理 Θ(n log n)。空间：合并需要辅助数组 Θ(n)，递归栈 Θ(log n)。稳定排序。

**例 5（LIS）** 序列 1,5,2,4,3。求 LIS 长度。

**解析**：dp[1..5] = 1,2,2,3,2。过程：dp[2] 从 a1 转移得 2；dp[3] 只能从 a1 得 2；dp[4] 从 a1 或 a3 得 3；dp[5] 从 a1 或 a3 得 2。答案 max=3，一条 LIS 为 1,2,4 或 1,2,3 或 1,4 之外的长度 3 序列如 1,2,4。要点：状态是“以 a[i] 结尾”，不是“前 i 个的最长”，后者不能简单递推。

**例 6（最少硬币）** 币值 1,3,4，凑 6 元，最少几枚？与贪心比较。

**解析**：dp[0]=0；dp[1]=1；dp[2]=2；dp[3]=1；dp[4]=1；dp[5]=2（4+1 或 3+1+1 取 2）；dp[6]=min(dp[5]+1, dp[3]+1, dp[2]+1)=min(3,2,3)=2。最少 2 枚（3+3）。贪心取 4+1+1 为 3 枚，非最优。说明最优化硬币问题一般用 DP，只有特殊币值体系才可贪心。

**例 7（背包路径还原）** 在 0-1 背包例 1（n=3，W=5，w=2,3,4，v=3,4,5）中，说明如何输出所选物品编号。

**解析**：填表时额外维护 choice[i][j]∈{0,1}，表示第 i 件在容量 j 的最优解中是否被选。从 i=n、j=W 回溯：若 choice[i][j]=1，则记录 i，j←j-w[i]，i←i-1；否则 i←i-1，直到 i=0。本例 choice 路径给出选中物品 1 与 2。若空间已滚动成一维，则无法直接回溯，需再跑一遍二维或转移时记录前驱。下午题常把“输出选中的物品序号”作为第二问，这一步必须写清回溯规则。

## 五、易错点

1. **把归并当 DP**：子问题不重叠，没有填表最优子结构需求，归并是分治。
2. **0-1 背包一维数组遍历顺序**：必须容量**倒序**；正序会变完全背包。完全背包才正序。
3. **LCS 与 LIS 混淆**：LCS 是两序列公共子序列，LIS 是一个序列的递增子序列。子序列**不要求连续**，子串/子段要求连续。
4. **状态含义不写清**：下午题不写 dp[i][j] 含义直接扣分。
5. **矩阵连乘下标**：维度是 p0..pn 共 n+1 个数，Ai 的维度是 p_{i-1}×p_i，常写错。
6. **最优子结构被忽略**：若子问题最优并不导出原问题最优（如带约束的最长路径），不能直接 DP，需改状态。
7. **快排最坏当平均**：最坏 Θ(n^2)，平均 Θ(n log n)，问最坏别填 n log n。
8. **记忆化 vs 自底向上**：二者时间同阶；填表题写清循环顺序即可，但边界必须初始化。
9. **状态维数不够**：若“前 i 个”的状态无法决策第 i 个的归属，就要像 LIS 那样改成“以 i 结尾”。
10. **把计数写成 max**：问方案数时转移是加法求和，不是取最大。

## 六、考点提示

- 下午题三件套默写：0-1 背包转移、LCS 转移、矩阵连乘 min 转移
- 分治背复杂度三式：二分 Θ(log n)、归并 Θ(n log n)、快排平均 Θ(n log n)/最坏 Θ(n^2)
- 答题固定结构：状态定义 → 转移方程 → 边界 → 计算顺序/复杂度
- 与贪心对比：能贪心往往也能 DP，但 DP 求全局最优更稳；下一课题辨析三者
`,
    quizzes: [
      {
        id: 'sd-08-02-q1',
        type: 'choice',
        question: '下列算法中，使用动态规划设计思想的是（）',
        options: ['快速排序', '二分查找', '矩阵连乘问题', '归并排序'],
        answer: 'C',
        explanation:
          '矩阵连乘具有最优子结构与重叠子问题，用区间 DP。快排、归并是分治，二分查找是减治/分治思想，子问题均不重叠。',
      },
      {
        id: 'sd-08-02-q2',
        type: 'choice',
        question: '0-1 背包问题的状态方程（dp[i][j] 表示前 i 件物品容量 j 的最大价值）正确的是（）',
        options: [
          'dp[i][j] = max(dp[i-1][j], dp[i][j-w[i]] + v[i])',
          'dp[i][j] = max(dp[i-1][j], dp[i-1][j-w[i]] + v[i])（j≥w[i]）',
          'dp[i][j] = dp[i-1][j] + v[i]',
          'dp[i][j] = max(dp[i-1][j], dp[i+1][j] + v[i])',
        ],
        answer: 'B',
        explanation:
          '0-1 背包“不拿”与“拿”分别对应 dp[i-1][j] 与 dp[i-1][j-w[i]]+v[i]。A 是完全背包的一维形式误写进二维；C 变成必拿；D 引用了非法的后继状态。',
      },
      {
        id: 'sd-08-02-q3',
        type: 'fill',
        question: '分治法的三个步骤是分解、______ 和合并；判断问题能否用动态规划的两个特征是最优子结构和 ______。',
        answer: ['求解', '重叠子问题'],
        explanation: '分治：分解-求解（递归解决子问题）-合并。DP 两特征：最优子结构、重叠子问题（缺一不易用 DP）。',
      },
      {
        id: 'sd-08-02-q4',
        type: 'short-answer',
        question: '给出两序列 X=ABC 与 Y=ACB 的最长公共子序列（一条即可）及其长度，并写出 LCS 的状态定义与转移方程。',
        answer:
          'LCS 长度为 2，一条 LCS 为 "AB" 或 "AC"。状态定义：dp[i][j] 表示 X 前 i 个字符与 Y 前 j 个字符的最长公共子序列长度。转移：若 X[i]=Y[j]，dp[i][j]=dp[i-1][j-1]+1；否则 dp[i][j]=max(dp[i-1][j], dp[i][j-1])。边界 dp[0][j]=dp[i][0]=0。答案为 dp[m][n]。',
        explanation: '评分要点：长度与一条序列 2 分；状态定义 1.5 分；转移与边界 2.5 分。',
      },
    ],
    references: [],
  },
  {
    id: 'sd-08-03',
    moduleId: 'sd-algorithms',
    title: '贪心与回溯',
    description: '贪心选择性质与经典问题、回溯剪枝与N皇后、四种策略辨析',
    content: `# 贪心与回溯

## 考情分析

- **分值**：上午 2～4 分（贪心策略选择、回溯基本概念、与 DP 区别）；下午算法题中回溯/贪心填空 5～15 分
- **题型**：选择（求最优解用哪种策略、某算法属于贪心还是 DP、复杂度）、填空（贪心选择量、剪枝条件）、程序填空（回溯递归框架、贪心排序关键字）
- **考频**：★★★☆ 贪心几乎每次必有一问；回溯在下午题中常与“全部解/排列组合”绑定考查

## 一、核心概念

### 1. 贪心法（Greedy）

**基本思想**：每一步都做出当前看起来**局部最优**的选择，希望通过局部最优堆出全局最优，**不回退**。

**两个关键性质**：
1. **贪心选择性质**：全局最优解可以通过一系列局部（贪心）选择达到，即存在最优解包含当前的贪心选择。需要证明“贪心选择后，剩余问题仍是同构的最优化问题”。
2. **最优子结构**：问题的最优解包含子问题的最优解（与 DP 共有）。

**经典问题**：
- **活动选择 / 会议安排**：按结束时间升序贪心，能选则选。可证最优。
- **部分背包**：按单位价值 v/w 降序，能拿满则拿满。可证最优。（0-1 背包**不能**纯贪心，要 DP 或贪心+回溯。）
- **哈夫曼编码**：每次取权值最小的两棵树合并。可证最优，得前缀码。
- **Prim / Kruskal 最小生成树**：Prim 贪“最近顶点”，Kruskal 贪“最短边（不成环）”。
- **Dijkstra 单源最短路**（非负权）：贪当前最短距离的顶点确定。
- **找零问题**：若币值是标准的 1,5,10,25 之类，每次取不超过余额的最大面额可得最优；币值任意时贪心**不一定最优**（例如 1,3,4 换 6 元：贪心 4+1+1=3 张，DP 3+3=2 张）。

**不能贪心的例子**：一般 0-1 背包、任意币值找零、一般图的最短路（有负权）、TSP。

### 2. 回溯法（Backtracking）

**基本思想**：在**解空间树**上按深度优先方式搜索，发现当前部分解不可能导出可行解/更优解时，**剪枝**回溯到父节点换分支。本质是带剪枝的 DFS，属于**系统性枚举**，保证找到全部解或最优解。

**两类解空间树**：
- **子集树**：每个元素“选或不选”，满二叉树约 2^n 个叶节点（0-1 背包、子集和、装载问题）。
- **排列树**：n 个元素的全排列，n! 个叶节点（TSP、n 皇后按列放也可看排列思想、批处理作业调度）。

**剪枝函数**：约束函数（剪去不可行）与限界函数（剪去得不到更优）。剪枝是回溯与朴素枚举的本质差别。

**经典**：n 皇后、图着色、哈密顿回路、0-1 背包（求最优或全部装法）、装载问题、批处理作业调度、连续邮资问题。

**递归回溯框架（必须熟）**：
if 到达叶节点：记录解；return
for 每个可选的候选值 x：
  if x 满足约束（可行）：
    做出选择；递归下一层；撤销选择（回溯）；
（0-1 型：不选走一支，选走一支，再撤销。）

### 3. 分支限界（略提，防止混淆）

分支限界常以广度优先/优先队列方式搜索解空间，用限界函数求**一个**最优解（而不强调全部解）；回溯重在找出**所有**解或求最优但走 DFS。软考下午常只考回溯框架，但概念辨析题会出现分支限界。

## 4. 四种策略对比（必背）

| 策略 | 子问题 | 是否回退 | 解的质量 | 典型 |
|------|--------|----------|----------|------|
| 分治 | 独立不重叠 | 无 | 精确 | 归并、快排 |
| 动态规划 | 重叠子问题 | 无（填表） | 精确最优 | 背包、LCS |
| 贪心 | 规模缩小的子问题 | 无 | 不一定全局最优 | 活动选择、哈夫曼 |
| 回溯 | 解空间树节点 | **有** | 可求全部解/最优 | n 皇后、TSP |

判断口诀：能走一步定死不留后路且可证最优 → 贪心；要枚举所有选择并可能后悔 → 回溯；问题有递推最优结构 → DP；二分拆成独立小问题 → 分治。

## 二、关键原理/方法

### 1. 贪心选择的证明套路（下午简答）

1. 证明贪心选择性质：构造最优解，可改造成包含贪心选择而不变差（交换论证）。
2. 证明最优子结构：去掉贪心选择的部分后，剩余子问题的最优解与整体一致。
3. 活动选择经典结论：按**结束时间最早**优先，可选出最大兼容活动集。（若按开始时间或时长则错误。）

### 2. 哈夫曼算法与前缀码

步骤：叶子权值排序（或用堆）→ 取最小两权值合并为新父节点（权值相加）→ 直到只剩一棵树。
带权路径长 WPL = Σ 权值×深度。哈夫曼树使 WPL 最小，编码无歧义（前缀码），且频率高的字符深度浅。
例：权 2,3,4,7 → 合并 2+3=5，合并 4+5=9，合并 7+9=16，得 WPL = 2×3+3×3+4×2+7×1 = 6+9+8+7 = 30（注意深度以实际树形为准，此例可按画图核对；考题多给定树求 WPL）。

### 3. n 皇后回溯要点

按行（或列）逐个放皇后，对每一行枚举各列，**剪枝**：与已放皇后不同列、不在同一正/反对角线。约束：col[r] 不同；r-c 差相同在同一主对角；r+c 和相同在同一次对角。
复杂度约 O(n!) 量级（有效剪枝后实际远小于此），空间 O(n) 存放每行皇后列号。

### 4. 0-1 背包的回溯/贪心混合（常考）

- 纯贪心（按 v/w）不一定对 0-1 背包最优。
- 可用**贪心+回溯**：以单位价值排序后深度优先搜索，并用“上界 = 当前价值 + 剩余容量×单位价值（按分数背包估值）”作限界剪枝；只维护当前最优 best。
- 这是分支限界/回溯求最优的典型下午题。

### 5. 活动选择算法

按结束时间 f 升序排；last = -∞；for 每活动 a_i：若 s_i ≥ last 则选取，并 last = f_i。
时间复杂度 O(n log n)（排序主导）。证明用“最早结束不占用过多资源”的交换论证。

### 6. 回溯复杂度与剪枝效果

解空间大小是指数级：子集树满树 2^n，排列树 n!。剪枝前做完全枚举在 n 稍大时不可行。约束函数砍掉“不可行”的分支（如皇后攻击、重量超限），限界函数砍掉“不如当前最优”的分支（如上界小于 best）。答题时务必写“最坏时间复杂度仍为 O(2^n) 或 O(n!)，但剪枝后实际访问节点数大幅下降，平均快很多”。空间复杂度主要看递归深度与当前解数组，一般 O(n)。

## 三、常考题型与解题方法

**题型 1：给场景选策略**。方法：出现“每步取当前最优、不回退”→ 贪心；出现“所有方案/解空间/N 皇后/找全部解”→ 回溯；出现“最优值+重叠子问题”→ DP。

**题型 2：贪心策略判断（是否正确）**。方法：找反例最快。0-1 背包用两件物品：重量 2,3 价值 3,4 容量 4，按性价比可能贪错；找零 1,3,4 对 6 元反例。

**题型 3：补回溯代码/伪代码**。方法：抓住四件套——递归终止（到达叶）、候选枚举、约束剪枝、做出选择-递归-撤销选择。撤销（回改全局数组）是最常挖空的位置。

**题型 4：求复杂度**。方法：贪心看排序主导 O(n log n)；回溯最坏看解空间 O(2^n)/O(n!)，写“剪枝后大幅下降，最坏仍为 …”。

## 四、典型例题

**例 1（选择）** 背包容量 10，物品重量 6、5、5，价值 6、5、4。按“单位价值”贪心（可部分取）得（）思路正确吗？若为 0-1 背包（不可分割）则？
A. 部分背包贪心正确，0-1 背包贪心也正确
B. 部分背包贪心正确，0-1 背包贪心可能错误
C. 两者贪心都错误
D. 0-1 背包贪心正确，部分背包贪心错误

**解析**：选 B。部分背包按 v/w 降序取满可证最优。0-1 背包反例：容量 30，A 重量 10 价值 60，B 重量 20 价值 100，C 重量 30 价值 120。单位价值 A=6,B=5,C=4。贪心取 A、B 总重 30 价值 160？本例中 160>120 还碰巧对；再换一例：容量 50，A(10,60),B(20,100),C(30,110) 单位 6/5/3.67，贪心 A+B=20 重量 30 价值 160，剩余 20 装不下 C；最优是 B+C 重量 50 价值 210。故 0-1 背包贪心会错。

**例 2（活动选择）** 活动及其起止：a1(1,4), a2(3,5), a3(0,6), a4(5,7), a5(3,8), a5 换成 a5(5,9)，a6(8,10)。按结束时间贪心可选几个？是哪些？

**解析**：按结束时间升序：a1(1,4), a2(3,5), a4(5,7), a6(8,10) 先后考虑（a3/a5 结束更晚排后）。
- 选 a1（结束 4）
- a2 起 3 < 4，跳过
- a4 起 5 ≥ 4，选（结束 7）
- a6 起 8 ≥ 7，选（结束 10）
共 **3 个：a1, a4, a6**。这是最大兼容集。

**例 3（回溯框架）** 写出 n 皇后“判断能否放在第 r 行第 c 列”的约束条件。

**解析**：设 queen[r]=c 已放行 r 的皇后列号。对已放的行 i=0..r-1，若：
queen[i] == c（同列）或 i - queen[i] == r - c（同主对角）或 i + queen[i] == r + c（同次对角），
则冲突不能放；三者都不成立才可放。回溯时试遍 c=0..n-1，递归下一行，返回后无须显式“撤销”若用局部变量；若用全局数组则需恢复。

**例 4（找零反例）** 币值 1、3、4，金额 6。用“每次取不超过余额的最大面额”的贪心得到几张？最优是几张？

**解析**：贪心：4+1+1 → **3 张**。最优：3+3 → **2 张**。故此币值体系下贪心不正确，需 DP（min 硬币数）或搜索。

## 五、易错点

1. **贪心 ≠ 总最优**：没有证明就默认“局部最优=全局最优”会错。考题常设找零/0-1 背包反例。
2. **活动选择按开始时间**：正确贪心是**结束时间早优先**，不是开始时间早，也不是时长短。
3. **部分背包与 0-1 背包**：前者可切割贪心正确；后者不可切割。
4. **回溯忘记撤销**：写伪代码时“做完选择递归后必须恢复状态”，这是挖空高频点。
5. **剪枝不写**：对比朴素枚举，回溯答案必须出现约束/限界剪枝，否则只是穷举。
6. **子集树与排列树规模**：2^n 与 n! 弄混会导致复杂度答错。
7. **Dijkstra 要求非负权**：有负权时 Dijkstra 不对，应用 Bellman-Ford；Kruskal/Prim 求的是 MST 不是最短路。
8. **哈夫曼树不唯一**：同权值时合并顺序不同形状可不同，但 WPL 唯一；答“哈夫曼树唯一”是错的。
9. **把 DP 当贪心**：0-1 背包最优值要 DP，别答贪心。

## 六、考点提示

- 上午必背：贪心两性质；活动选择按结束时间；哈夫曼 WPL；0-1 背包不可纯贪心；n 皇后是回溯
- 下午模板：回溯题答“约束条件 + 选择/递归/撤销 + 剪枝”；贪心题答“排序关键字 + 选择规则 + 证明一句”
- 复杂度话术：贪心 O(n log n)；回溯最坏 O(n!) 或 O(2^n)，剪枝可显著降低
- 四策略对比表建议抄到笔记首页，做判断题先跑这张表
`,
    quizzes: [
      {
        id: 'sd-08-03-q1',
        type: 'choice',
        question: '活动选择问题中，为得到最多兼容活动，贪心策略应优先选择（）',
        options: [
          '开始时间最早的活动',
          '持续时间最短的活动',
          '结束时间最早的活动',
          '与其他活动冲突最少的活动',
        ],
        answer: 'C',
        explanation:
          '经典结论是按结束时间升序贪心，能选则选，可证最优。按开始时间或时长贪心都可举出反例。',
      },
      {
        id: 'sd-08-03-q2',
        type: 'choice',
        question: '下列关于算法策略的说法，正确的是（）',
        options: [
          '0-1 背包问题用“按单位价值贪心”一定得到最优解',
          '回溯法通过剪枝搜索解空间树，可求出全部解或最优解',
          '动态规划不要求最优子结构',
          '找零问题币值为 1,3,4 时，每次取最大面额一定最优',
        ],
        answer: 'B',
        explanation:
          'B 正确，回溯=解空间树 DFS+剪枝。A 错，0-1 背包需 DP/回溯；C 错，DP 必须最优子结构；D 错，金额 6 时贪心 4+1+1=3 张，最优 3+3=2 张。',
      },
      {
        id: 'sd-08-03-q3',
        type: 'fill',
        question: '贪心算法的两个重要性质是贪心选择性质和 ______；n 皇后问题的解空间树一般属于 ______ 树。',
        answer: ['最优子结构', '子集'],
        explanation:
          '贪心两性质与 DP 共享最优子结构。n 皇后逐行选列/不选，常按子集树/排列空间理解，标准答案填“子集”树或“解空间”均可酌情，教材多归为子集树或排列树类，填“子集”最常见。',
      },
      {
        id: 'sd-08-03-q4',
        type: 'short-answer',
        question: '比较分治、动态规划、贪心、回溯四种策略在子问题关系、是否回退、解的质量三方面的差别。',
        answer:
          '分治：子问题独立且不重叠，无回退，求精确解（如归并排序）。动态规划：子问题重叠，无回退，填表复用，求精确最优（如 0-1 背包）。贪心：每步选局部最优，无回退，解不一定全局最优（如部分背包、活动选择可证最优；任意币值找零则否）。回溯：在解空间树上深度优先，可回退并剪枝，可求全部解或最优解（如 n 皇后）。',
        explanation:
          '评分要点：每策略一行，三条维度各点到即可给满分 5 分；漏“回退”或“最优性是否保证”每处扣 1 分。',
      },
    ],
    references: [],
  },
];
