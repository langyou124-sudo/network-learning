import { Topic } from '@/types';

export const topics: Topic[] = [
  {
    id: 'sd-09-01',
    moduleId: 'sd-database',
    title: 'ER模型与关系代数',
    description: 'ER图要素与联系转换规则、关系代数基本运算及查询表达式含义',
    content: `# ER模型与关系代数

## 考情分析

- **分值**：数据库系统在上午题约占5-8分，ER模型与关系代数常考2-4分；下午案例中可能要求补全ER图或把ER图转成关系模式
- **题型**：选择题（实体联系类型判断、ER转关系模式规则、关系代数表达式含义）、案例分析题（设计ER图、写出关系模式与主键）
- **考频**：★★★ 几乎每年都有一道直接考ER或关系代数的题，是数据库最稳定得分点之一

## 一、核心概念

### 1. ER模型三要素

ER（Entity-Relationship）模型用**实体、属性、联系**描述现实世界的信息结构。

**实体**是可区分的客观对象，如“学生”“课程”。ER图中用矩形表示。弱实体必须依附强实体存在（如“订单明细”依附“订单”），用双边框矩形。

**属性**是实体或联系的性质，用椭圆表示。常见分类：
- **简单属性**与**复合属性**（如“地址”可再分为省市区）
- **单值属性**与**多值属性**（如“电话号码”一个人可有多个，用双椭圆）
- **派生属性**可由其他属性算出（如“年龄”由出生日期派生），用虚线椭圆
- **码（键）属性**是唯一标识实体的属性集，用下划线标出

**联系**是实体间的语义关联，用菱形表示。按涉及实体个数分为二元联系（最常见）、三元联系等。

### 2. 联系的基数比（最重要考点）

两个实体集E1、E2之间的联系R，按“一个E1对应几个E2、一个E2对应几个E1”分为三类：

| 类型 | 含义 | 例子 |
|------|------|------|
| 1:1 | 双方都至多对应一个 | 班级与班长（一班一班长，一班长一班） |
| 1:N | 一端对多端，多端对一端 | 班级与学生（一班多生，一生属一班） |
| M:N | 双方都可对应多个 | 学生与课程（一生多课，一课多生） |

判断口诀：**分别看两端各能有几个**。记不清就画“一个实例能牵几个对面实例”。

### 3. 参与约束（全部参与/部分参与）

若E1中每个实体都必须参与联系R，称E1**全部参与**（双线连接）；否则**部分参与**。例如“每个学生必须选课”是全部参与，“有的教师本学期不授课”是部分参与。参与约束影响键约束和是否允许空值，在转换时体现为外键能否取NULL。

### 4. 关系代数基本运算

关系代数是以关系为运算对象的抽象查询语言，分**基本运算**与**扩展运算**。

五种基本运算：
- **并、差、笛卡尔积**：传统集合运算（要求关系并相容，属性个数相同且对应域可比）
- **选择（σ）**：按条件选行，σ条件(R)，相当于SQL的WHERE
- **投影（π）**：按属性列表取列，π属性(R)，相当于SQL的SELECT去重

扩展运算由基本运算导出：
- **交**：R∩S = R − (R − S)
- **连接（⋈）**：先笛卡尔积再选择。常用**等值连接**；当连接属性同名同域且结果去重属性列时叫**自然连接**
- **除法（÷）**：求“全部满足”的查询，如“选修了所有课程的学生”

## 二、关键原理/机制

### 1. ER图向关系模式的转换规则

这是下午案例的固定套路，必须按联系基数比分类记牢：

**实体转关系**：每个实体转为一个关系模式，实体码作主键，各属性作属性。

**1:1联系**：
- 可以并入任一端关系（该端加入另一端主键作外键，兼作候选键）
- 也可单独转为一个关系模式，主键可取任一端主键

**1:N联系**：
- 把1端主键作为外键加入N端关系（推荐，可消除冗余）
- 也可单独转关系，主键取N端主键

**M:N联系**：
- **必须**单独转为一个关系模式
- 主键 = 两端主键组成的**属性组**（联合键）
- 联系自身属性一并加入
- 两端主键都作外键

**多值属性**单独建表，主键 = 实体主键 + 多值属性。**弱实体**与识别联系合并，主键 = 识别实体主键 + 弱实体部分键。

### 2. 关键与约束在转换中的落实

- 实体的码 → 关系模式的主键（PRIMARY KEY）
- 联系两端的主键在新关系中 → 外键（FOREIGN KEY）
- 1:1中并入时，若对方有“全部参与”，并入端外键一般不允许NULL
- 候选码：能函数决定全部属性且自身最小的属性集；主码是从候选码中选定的一个

### 3. 关系代数表达式与SQL对照

| 关系代数 | SQL对应 | 语义 |
|----------|---------|------|
| σ条件(R) | SELECT * FROM R WHERE 条件 | 筛行 |
| πA,B(R) | SELECT DISTINCT A,B FROM R | 投影去重 |
| R×S | FROM R, S | 广义笛卡尔积 |
| R ⋈ F S | FROM R JOIN S ON F | 条件连接 |
| R÷S | 双重否定 / NOT EXISTS 嵌套 | 全称量词 |

**自然连接与笛卡尔积的区别**：自然连接只保留同名公共属性相等的元组，并且公共属性只出现一次；笛卡尔积属性直接拼接，行数是两者之积。

**除法**的语义是“在R中找出那些在某个属性上涵盖S全部取值的元组”。设学生选课SC(学号,课程号)，课程表C(课程号)，π学号,课程号(SC) ÷ π课程号(C) 就是“选了所有课程的学号”。除法是“全部”“所有”“每一个”这类全称量词题的代数工具。

### 4. 超码、候选码与主码的关系

超码是能决定全部属性的属性集（允许冗余）；候选码是极小超码（去掉任一属性就不再能决定全部）；主码是选定的一个候选码。包含在任一候选码中的属性叫**主属性**，否则叫**非主属性**。做规范化题目时，主属性与非主属性的区分直接影响“部分依赖”“传递依赖”的判断，务必先把候选码求全。当候选码多于一个时，每个候选码中的属性都是主属性，此时“部分依赖”只能看非主属性。

### 5. 实体与联系的设计原则

设计ER图时遵循“一事一实体、一事一属性”：不要把可再分的对象塞进属性；不要把本来是实体的东西降格成多值属性。联系上尽量少放属性，只有真正描述“发生关系时才有的事实”（如成绩、数量、时间）才挂在联系上。实体的属性应是原子值，满足第一范式的前提。

## 三、常考题型与解题方法

**题型1：判断联系基数比**
方法：分别假设“左边一个”“右边一个”，数对面能有几个。考试常把“班长-班级”“职工-部门”“学生-课程”做成文字辨析，不要背生活常识，要看题干限定（如“一个部门有多个职工，一个职工只属一个部门”→1:N）。

**题型2：ER转关系模式**
步骤：①每个实体先写关系；②按1:1、1:N、M:N三条规则加联系；③标明主键（下划线或文字说明）与外键；④检查多值属性、弱实体是否单独处理。写完用一个小实例验证能否无损还原联系。

**题型3：关系代数表达式含义**
方法：从内向外读。先看σ筛选了什么行，π留下了什么列，最后看连接/除法在做什么。出现“所有”“全部”优先想除法或双重NOT EXISTS。

**题型4：比较自然连接与笛卡尔积结果行数**
方法：笛卡尔积行数 = |R|×|S|；自然连接行数 ≤ 等值连接行数，且公共属性合并。题干常给元组数算行数。

## 四、典型例题

**例1（联系类型）**
某工厂：一个车间有多名职工，一名职工只属一个车间；一名职工可参加多个项目，一个项目有多名职工参加。车间与职工、职工与项目分别是什么联系？

解：车间-职工是1:N；职工-项目是M:N。
分析：先固定“一个车间”，对应多名职工；固定“一名职工”，只属一个车间 → 1:N。固定“一名职工”参加多个项目；固定“一个项目”多名职工 → M:N。M:N联系在转关系模式时必须单独建表，主键是（工号, 项目号）。

**例2（ER转关系模式）**
实体：学生（学号,姓名,性别），课程（课程号,课程名,学分），联系选课（成绩），联系类型为M:N。写出关系模式。

解：
学生(学号, 姓名, 性别)，主键学号
课程(课程号, 课程名, 学分)，主键课程号
选课(学号, 课程号, 成绩)，主键(学号, 课程号)，外键学号引用学生、课程号引用课程
分析：M:N联系单独成表，成绩是联系属性必须放联系表，不能塞进学生表或课程表。若把成绩放学生表，一门多科的成绩无法记录，会破坏第一范式甚至产生插入异常。

**例3（关系代数）**
设有关系R(A,B)={(1,2),(3,4)}，S(A,B)={(1,2),(5,6)}，求R∪S、R∩S、R−S、自然连接R⋈S。

解：
R∪S={(1,2),(3,4),(5,6)}（并去重）
R∩S={(1,2)}
R−S={(3,4)}
自然连接R⋈S={(1,2)}（公共属性A、B都相等才连上，且只保留一份A,B）
分析：集合运算要求两关系并相容。自然连接不是笛卡尔积：若误做笛卡尔积会得到4行，考试常设此陷阱。

## 五、易错点

1. **M:N联系漏建表**——并入任一端都会造成数据冗余或表示不全，这是案例题扣分第一名。
2. **1:N联系方向搞反**——应把1端主键放到N端，放反了会出现N端主键不能唯一决定外键。
3. **自然连接与笛卡尔积混淆**——自然连接有同名属性相等的匹配条件，且属性列不重复。
4. **投影默认去重**——关系代数π是集合语义，自动去重；SQL的SELECT默认可能保留重复，考试以关系代数语义为准。
5. **除法理解成笛卡尔或交**——“选修了全部课程”是除法，不是选修了某一门课。
6. **联系属性放错位置**——如“成绩”属于选课联系，不属于学生或课程实体。
7. **复合属性、多值属性直接塞进一列**——多值属性必须单独建表，否则不满足1NF。

## 六、考点提示

- 优先背熟**三类联系的转换规则**，下午案例年年能用上
- 关系代数重点抓：**σ、π语义**，自然连接行数，除法表示“全部”
- 看到“所有课程”“每一名”“全部供应商”立刻联想到除法或NOT EXISTS双重否定
- 上午选择题常见：基数比判断、转换后主键/外键是什么、代数表达式等价的SQL
- 计算关系基数时先写元组个数再下结论，避免凭感觉
- 设计ER图时先问“这是实体还是属性”，再问“联系是几比几”
- 下午题写关系模式一定标注主键与外键，漏写直接扣分`,
    quizzes: [
      { id: 'sd-09-01a', type: 'choice', question: '设“学生”与“课程”之间为多对多联系“选课”，且联系上有属性“成绩”。将ER图转换为关系模式时，下列做法正确的是（）', options: ['把成绩放入学生关系，主键为学号', '把成绩放入课程关系，主键为课程号', '单独建立选课关系，主键为（学号，课程号）', '单独建立选课关系，主键为学号或课程号任意一个'], answer: 'C', explanation: 'M:N联系必须单独转换成关系模式，联系属性成绩随联系入表，主键是两端主键组成的联合键。若并入学生或课程，一门课多个成绩或多门课成绩无法无损表示；主键只取一端会导致另一端取值重复时无法区分。' },
      { id: 'sd-09-01b', type: 'choice', question: '关系代数中，π_A,B(σ_{A=C}(R×S)) 等价于（）', options: ['自然连接', '等值连接后在A、B上投影', '笛卡尔积', '除法'], answer: 'B', explanation: 'σ_{A=C}(R×S) 是在笛卡尔积上按A=C筛选，即等值连接；再对A、B投影得到结果。自然连接还要求同名公共属性合并去重，与此不完全等价；笛卡尔积无筛选；除法表示“全部”。' },
      { id: 'sd-09-01c', type: 'fill', question: '在ER模型中，用椭圆表示属性，用矩形表示实体，用______表示实体间的联系。', answer: '菱形', explanation: 'ER图约定：矩形=实体，菱形=联系，椭圆=属性；弱实体用双边框矩形，多值属性用双椭圆，派生属性用虚线椭圆。' },
      { id: 'sd-09-01d', type: 'short-answer', question: '某系统有实体部门（部门号，名称）与职工（工号，姓名），一个部门有多名职工，一名职工只属于一个部门。请写出转换后的关系模式并标明主外键。', answer: '部门(部门号，名称)，主键：部门号；职工(工号，姓名，部门号)，主键：工号，外键：部门号引用部门。这是1:N联系，把1端（部门）主键作为外键放入N端（职工）即可，职工中部门号不允许为空（若题干约定职工必属某部门）。', explanation: '评分要点：两关系各1分，1:N规则选择正确1分，主外键标注1分。若单独建“工作”关系也能表示，但不如合并简洁，若采用单独建表，主键应为工号。' },
    ],
    references: [],
  },
  {
    id: 'sd-09-02',
    moduleId: 'sd-database',
    title: 'SQL与规范化',
    description: 'SQL查询语句结构与函数依赖、1NF至BCNF范式判别与分解',
    content: `# SQL与规范化

## 考情分析

- **分值**：规范化（范式判别）是下午高频考点，约占数据库大题一半篇幅；SQL上午题常考语法与查询结果，合计约3-6分
- **题型**：选择题（范式级别、部分函数依赖、SQL语句补全）、案例题（判断最高范式、说明理由、分解到3NF并保持无损连接与依赖保持）
- **考频**：★★★ 2NF/3NF/BCNF的判别与分解几乎逢考必出，SQL以SELECT查询为主

## 一、核心概念

### 1. SQL语言的组成

SQL（Structured Query Language）是关系数据库标准语言，不区分大小写，主要分：
- **DDL**：CREATE、ALTER、DROP、TRUNCATE——定义结构
- **DML**：SELECT、INSERT、UPDATE、DELETE——操纵数据
- **DCL**：GRANT、REVOKE——权限控制
- **TCL**：COMMIT、ROLLBACK、SAVEPOINT——事务控制

SELECT语句逻辑执行顺序：FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY。写多表连接、聚合时按这个顺序排查，比从上往下读可靠得多。

### 2. 函数依赖（FD）

设关系模式R(U)，X、Y是U的子集。若对R的任意合法关系r，X的值唯一决定Y的值（X相同则Y必相同），记作X→Y，称Y**函数依赖于**X，或X**函数决定**Y。

常见类型：
- **平凡依赖**：Y是X的子集（如AB→A）
- **部分函数依赖**：X→Y，但存在X的真子集X'也能决定Y。例：(学号,课程号)→姓名，而学号→姓名，故姓名部分依赖于联合键
- **传递依赖**：X→Y，Y→Z，且Y不能决定X，则Z传递依赖于X。例：学号→系名→系主任

**候选键**：能决定U中所有属性，且自身不含能决定全部的真子集。求候选键的方法：只在FD左边出现、从未单独出现在右边的属性必属于候选键；再求其闭包是否覆盖全属性。

**Armstrong公理**（推理规则）：自反、增广、传递；常用推论：合并规则、分解规则、伪传递规则。合并规则说若X→Y且X→Z则X→YZ；分解规则说若X→YZ则X→Y且X→Z。

### 3. 范式阶梯（核心考点）

| 范式 | 要求 | 消除的异常来源 |
|------|------|----------------|
| 1NF | 所有属性不可再分（原子值） | 多值、嵌套表 |
| 2NF | 在1NF基础上，消除非主属性对候选键的**部分**函数依赖 | 联合键上“半边决定” |
| 3NF | 在2NF基础上，消除非主属性对候选键的**传递**函数依赖 | 非主属性经非键传递 |
| BCNF | 每一个**决定因素**（FD左边）都必须是超键 | 主属性参与的不完美依赖 |

判断口诀：**先找键 → 看非主属性与键的关系 → 再看所有FD左边是否为超键**。

- 1NF看属性是否原子
- 2NF看有没有“联合键的真子集决定某属性”
- 3NF看有没有“非主属性被另一个非主属性决定”（传递）
- BCNF看每一个箭头左边是否都是候选键/超键

### 4. 无损连接分解与依赖保持

**无损连接**：分解后用自然连接还能拼回原关系，不产生多余元组。两模式分解的检验（Chase/表格法简化）：R1∩R2→R1−R2 或 R1∩R2→R2−R1 之一成立，则分解具有无损连接性。

**依赖保持**：把原有函数依赖分散到各分解模式后，能用投影的FD覆盖原来的FD。实际分解到3NF时追求：无损连接 + 依赖保持；BCNF分解总能做到无损，但可能损失依赖。

## 二、关键原理/机制

### 1. SQL查询机制

**连接类型**：
- 等值连接/非等值连接：FROM R, S WHERE R.a = S.b
- 自然连接：NATURAL JOIN，同名属性相等并合并
- 外连接：LEFT/RIGHT/FULL OUTER JOIN，保留未匹配元组，用NULL补齐
- 自连接：同一张表起别名，用于“比某人工资高”这类比较

**聚合与分组**：COUNT、SUM、AVG、MAX、MIN 遇NULL忽略（COUNT(*)计行）。GROUP BY后SELECT里只能出现分组列或聚合函数；WHERE过滤分组前的行，HAVING过滤分组后的组。

**子查询**：
- WHERE中比较、IN、EXISTS、ANY/ALL
- 相关子查询依赖外层列，逐行求值；非相关子查询独立求值
- “选修了全部课程”用 NOT EXISTS (SELECT ... WHERE NOT EXISTS ...) 或除法思想

**空值语义**：比较必须用 IS NULL / IS NOT NULL，因为 NULL = NULL 不为真。

### 2. 规范化分解算法（3NF）

已知关系模式R(U,F)：
1. 求出F的最小依赖覆盖Fmin（左边最简、无冗余FD）
2. 将相同左部的FD合并，每个合并组生成一个关系模式（左部+右部属性）
3. 若没有一个模式包含候选键，则另加入一个以候选键为属性的关系模式
4. 删除被其他模式包含的关系模式（冗余）

该构造性分解保证无损连接，并尽量保持依赖。BCNF分解则用“某个FD左边不是超键就把它投影出去”反复拆分，用表格法验证无损。

### 3. 范式级别与更新异常

级别越低，冗余越大，越可能出现插入异常（缺某个依赖属性就插不进）、删除异常（删一行带走无关信息）、修改异常（同一值改多处不一致）。规范化本质是**按函数依赖把“捆在一起的事实”拆开**，让每个关系只描述一件事。

## 三、常考题型与解题方法

**题型1：求候选键**
步骤：①只在左边出现的属性必然在键中；②算该属性集的闭包X+（反复用FD扩充）；③若X+≠U，尝试加入右边属性再试；④闭包覆盖U的就是候选键（可能不止一个）；⑤含在候选键中的叫主属性。

**题型2：判断最高范式**
口诀流程：
1. 属性都原子？否→1NF都不满足（或未达1NF）
2. 候选键是单属性？若是，则非主属性不可能部分依赖于键 → 至少2NF
3. 存在非主属性对键的部分依赖？→ 最高1NF
4. 消除了部分依赖后仍存在传递依赖（非主属性决定非主属性，或经非主属性决定）？→ 最高2NF
5. 消除传递后，每个FD左边都是超键？是→BCNF；否（存在非主属性对键的依赖或主属性相关的不好依赖）→3NF

答题务必写清：“候选键是……；存在……依赖；因此最高满足……范式”。

**题型3：分解到3NF并说明性质**
按上面分解算法，写出各关系模式，标主键，检查：能否自然连接还原（无损），原FD是否还能单独用某投影得到（依赖保持）。

**题型4：写SQL查询**
步骤：①确定查询涉及的表与连接键；②WHERE过滤行；③需要聚合时GROUP BY；④HAVING过滤组；⑤SELECT输出列；⑥ORDER BY排序。注意NULL、去重DISTINCT、内外连接差别。

**题型5：主属性与非主属性辨析**
方法：先求出全部候选键；凡出现在任一候选键中的属性都是主属性。主属性之间的函数依赖不构成2NF意义上的“部分依赖”，但可能破坏BCNF。这一点是3NF与BCNF分水岭的根基，近年辨析题出现频率上升。

**题型6：SQL补全语句**
方法：先读题意判断查询目标（选行、分组计数、还是“全部满足”），再按FROM→WHERE→GROUP BY→HAVING→SELECT→ORDER BY顺序补关键字。子查询优先想IN与EXISTS，比较聚合值用ANY、ALL或直接HAVING。

## 四、典型例题

**例1（候选键与范式）**
关系模式R(A,B,C,D,E,F)，FD = {AB→C, C→D, D→E, F→A}。求候选键，并判断R最高范式。

解：
（1）属性F只在左边出现？不对，F在F→A左边，从未单独出现在右边；B只在AB→C左边。尝试X=BF：
BF+：由F→A得ABF；由AB→C得ABCF；由C→D得ABCF D；由D→E得ABCDEF=U。
再检查真子集：B+ = B（无左部为B的FD），F+ = FA（不到U），故BF是候选键（唯一）。
（2）主属性：B、F。非主属性：A、C、D、E。
（3）AB→C，而C是非主属性，键BF不含在AB中，这不是部分依赖；但C→D，D→E存在传递：BF→C→D→E，非主属性D、E对键BF是传递依赖；又F→A，A被F决定，而F是键的真子集，故A对BF是**部分函数依赖**。
（4）既存在部分依赖又存在传递依赖，消除部分依赖前连2NF都不满足——严格说：因F→A，属性A部分依赖于候选键BF，**R只有1NF**。

（注：若题目假设所有属性原子且已满足1NF，则最高为1NF。答题写“仅满足1NF”最稳妥。）

**例2（分解到3NF）**
学生关系S(学号, 姓名, 系号, 系主任)，FD：学号→姓名，学号→系号，系号→系主任。分解并说明。

解：
（1）候选键：学号（学号+为全部属性）。
（2）问题：学号→系号→系主任，系主任传递依赖于学号。
（3）分解：
学生(学号, 姓名, 系号)，主键学号
系(系号, 系主任)，主键系号
（4）无损连接：学生∩系=系号，系号→系主任成立（即交属性决定一差），无损。
（5）依赖保持：学号→姓名、学号→系号落在学生表；系号→系主任落在系表，全部保持。
分析：分解后删除某个系不再误删学生姓名；插入新系且尚未有学生时也能插入系表。若原模式中“学号→系号→系主任”不拆开，删光某系最后一名学生就会连系主任信息一起丢失，这就是删除异常。

**例3（SQL）**
表SC(学号,课程号,成绩)，C(课程号,课程名)。查询选修了“数据库”课程且成绩大于90的学号。

解：
\`SELECT 学号 FROM SC, C
 WHERE SC.课程号 = C.课程号
   AND 课程名 = '数据库' AND 成绩 > 90;\`
也可写成 JOIN 形式或 IN 子查询。注意课程名来自C表，成绩来自SC表，必须连接；单引号是SQL字符串常量标准写法。

## 五、易错点

1. **部分依赖与传递依赖搞混**——部分依赖：键的真子集决定某属性；传递依赖：经非主属性中转。
2. **BCNF与3NF区别**——3NF允许“主属性”相关的异常依赖，BCNF要求所有决定因素都是超键，是“所有FD”检查而非只看非主属性。
3. **候选键漏求**——有多个候选键时，主属性会变多，直接影响“是否部分依赖”的判断。
4. **分解后忘记验证无损连接**——分解后若连接出现冗余元组，分解失败。
5. **SQL中把HAVING条件写进WHERE**——WHERE不能用聚合函数；反之HAVING里的非聚合条件上提也可能改变语义。
6. **NULL比较用等于号**——应使用 IS NULL / IS NOT NULL。
7. **忘记GROUP BY约束**——SELECT出现非分组列是经典错误。

## 六、考点提示

- **范式判别是必考**，答题模板固定：写候选键 → 指出违反的依赖类型 → 给出范式等级
- 3NF分解优先用“相同左部合并”算法，一步到位且易于证明依赖保持
- SQL牢记逻辑执行顺序：FROM-WHERE-GROUP BY-HAVING-SELECT-ORDER BY
- “全部”“每一个”类查询用双重否定 NOT EXISTS，不要硬套连接
- 下午案例喜欢先给一个有冗余的表，问范式、再要求规范化——按“异常表现 → FD → 范式 → 分解”四段式作答
- 3NF分解答案不唯一时，优先给出“相同左部合并”得到的那组关系模式
- 写SQL时注意中文表名/列名在真题中很常见，不要因写成英文而偏题`,
    quizzes: [
      { id: 'sd-09-02a', type: 'choice', question: '关系模式R(A,B,C,D)，函数依赖集F={AB→C, C→D, A→D}，则下列关于R的最高范式说法正确的是（）', options: ['R属于BCNF', 'R属于3NF但不属于BCNF', 'R属于2NF但不属于3NF', 'R只满足1NF'], answer: 'D', explanation: '候选键为AB（A在A→D、AB→C中可推出全属性）。存在A→D，A是键的真子集，D是非主属性，故D部分依赖于键AB，因此连2NF都不满足，只满足1NF。若认为3NF是错在没有先消部分依赖。' },
      { id: 'sd-09-02b', type: 'choice', question: '在SQL中，HAVING与WHERE的主要区别是（）', options: ['WHERE过滤分组前的行，HAVING过滤分组后的组', 'WHERE过滤分组后的组，HAVING过滤分组前的行', '两者都可使用聚合函数', '两者都可使用聚合函数，且作用时机相同'], answer: 'A', explanation: '执行顺序是 WHERE → GROUP BY → HAVING。WHERE在分组前过滤行，不能用聚合函数；HAVING在分组后过滤组，可以使用聚合函数，如 HAVING COUNT(*) > 2。' },
      { id: 'sd-09-02c', type: 'fill', question: '在2NF定义中，要求在1NF基础上消除非主属性对候选键的______函数依赖。', answer: '部分', explanation: '2NF消除部分函数依赖；3NF在2NF基础上消除非主属性对候选键的传递函数依赖；BCNF要求每个函数依赖的决定因素都是超键。' },
      { id: 'sd-09-02d', type: 'short-answer', question: '关系模式R(学号, 课程号, 学生名, 课名, 成绩)，FD：学号→学生名，课程号→课名，(学号,课程号)→成绩。判断R的最高范式，并说明理由。', answer: 'R仅满足1NF。候选键是(学号,课程号)。学号→学生名，学号是候选键的真子集，学生名是非主属性，故学生名部分依赖于候选键；同理课程号→课名，课名也部分依赖于候选键。存在非主属性对候选键的部分函数依赖，因此不满足2NF，只满足1NF。', explanation: '评分要点：指出候选键1分；举出部分依赖例子2分；结论1分。注意本题虽然也存在类似传递的表象，但根本原因是联合键的真子集决定非主属性，属于部分依赖。' },
    ],
    references: [],
  },
  {
    id: 'sd-09-03',
    moduleId: 'sd-database',
    title: '事务与并发控制',
    description: '事务ACID特性、并发一致性问题、封锁协议与可串行化调度',
    content: `# 事务与并发控制

## 考情分析

- **分值**：事务ACID、并发问题类型常以选择题出现约2分；可串行化判断可能以小计算/情景题出现
- **题型**：选择题（ACID与各性质对应、三种读数据异常辨析、封锁协议能防止哪些问题）、简答/判断调度是否可串行化
- **考频**：★★☆ 单独命题不如范式多，但“并发问题—封锁—隔离级别”这条线几乎每隔一年就考一次

## 一、核心概念

### 1. 事务（Transaction）

事务是用户定义的一组操作序列，这些操作要么全做、要么全不做，是数据库**恢复**和**并发控制**的基本单位。典型例子：银行转账——A账户扣款与B账户加款是一个事务，不能只发生一半。

### 2. ACID特性（必背）

| 特性 | 含义 | 通俗理解 |
|------|------|----------|
| 原子性 Atomicity | 事务中操作不可分割，全部成功或全部撤销 | 不允许“扣了钱没到账” |
| 一致性 Consistency | 事务把数据库从一个一致状态转到另一个一致状态 | 转账前后总金额不变 |
| 隔离性 Isolation | 并发事务彼此隔离，中间结果互不干扰 | 甲转账时乙看到的是已提交状态，而不是中间态 |
| 持久性 Durability | 已提交事务对数据库的改变是永久的 | 提交后断电也不丢 |

一致性是最根本目标，原子性、隔离性、持久性都是为了一致性服务。题干若问“哪个特性保证事务结束后数据正确”，选一致性；“哪个保证中间互不干扰”，选隔离性。

### 3. 并发操作的三类不一致性

并发事务如果不加控制，会出现：

**丢失修改（丢失更新）**：两个事务同时读同一数据并修改，后写的覆盖先写的，先写事务的修改丢失。例：甲乙同时读机票余量1，各扣1后写回，最后余量仍是1而不是0。

**不可重复读（不可重复读出）**：事务T1读同一数据两次，其间T2修改并提交，T1两次结果不一致。强调“同一行被其他事务修改/删除”。

**读“脏”数据（脏读）**：T1读到了T2**尚未提交**的中间值；随后T2回滚，T1读到的数据等于没发生过的值。

还常提到**幻读**：T1按条件读两次，其间T2插入/删除满足条件的行，T1两次行数不同。与不可重复读的区别：不可重复读针对**已存在行的值**，幻读针对**行数的增减**。

### 4. 封锁与锁类型

**排他锁（X锁、写锁）**：事务T对数据对象加X锁后，其它事务既不能读也不能再加任何锁（直到T释放）。

**共享锁（S锁、读锁）**：T对数据对象加S锁后，其它事务只能再加S锁读，不能加X锁写。

相容矩阵：S与S相容；S与X、X与S、X与X都不相容。

### 5. 可串行化与隔离级别

**可串行化调度**：多个事务并发执行的某个调度结果，与某一**串行**调度结果相同，则该调度是可串行化的，正确性与串行执行一致。两段锁协议（2PL）是可串行化的充分条件。

SQL隔离级别（从低到高）：读未提交（允许脏读）→ 读已提交（防脏读）→ 可重复读（防不可重复读）→ 可串行化（防幻读，完全串行效果）。级别越高一致性越好、并发度越低。

## 二、关键原理/机制

### 1. 两段锁协议（2PL）

**内容**：任何事务对数据加锁和解锁分两阶段——
- **扩展阶段**：只能申请新锁，不能释放锁
- **收缩阶段**：只能释放锁，不能申请新锁

一旦事务释放过一个锁，就不能再申请任何新锁。

**定理**：若所有事务都遵从两段锁协议，则它们的任何并发调度都是可串行化的。注意：2PL是**充分不必要**条件——不满足2PL的调度也可能可串行化；2PL还可能带来死锁。

### 2. 死锁的诊断与处理

**产生原因**：多个事务互相持有对方需要的锁并互相等待。

**预防/处理**：
- **死锁预防**：一次封锁法（一次性申请所有锁，否则全不给）、顺序封锁法（规定加锁顺序）
- **死锁检测与解除**：用事务等待图找回路，回滚代价最小的事务
- **超时法**：等待超时即回滚

### 3. 故障与恢复机制（常与事务一起考）

**故障类型**：
- 事务内部故障：通过ROLLBACK撤销，保证原子性
- 系统故障（软故障，死机/断电）：未完成事务回滚，已提交事务用日志重做（REDO），保证原子性与持久性
- 介质故障（硬故障）：需备份+日志恢复

**日志与恢复策略**：
- **UNDO**：对未完成事务，反向撤销已做修改（保证原子性）
- **REDO**：对已提交事务，正向重做（保证持久性）
- 影子页、检查点可减少恢复扫描量

### 4. 隔离级别与各读异常的对应

| 隔离级别 | 脏读 | 不可重复读 | 幻读 |
|----------|------|------------|------|
| 读未提交 | 可能 | 可能 | 可能 |
| 读已提交 | 防止 | 可能 | 可能 |
| 可重复读 | 防止 | 防止 | 可能 |
| 可串行化 | 防止 | 防止 | 防止 |

记法：隔离级别每提高一档，多防一类问题；默认多为“读已提交”或“可重复读”（看具体产品）。

## 三、常考题型与解题方法

**题型1：辨析三种读数据异常**
方法：抓住关键词——“写覆盖写=丢失修改”；“读到未提交后来又撤销=脏读”；“同一事务两次读不一样（行内容变）=不可重复读”；“两次读行数变=幻读”。

**题型2：给调度判断是否可串行化**
方法：①按时间顺序列出各事务读写的数据项；②找冲突操作（不同事务、同一数据、至少一个写）；③由冲突顺序画前趋图；④图中有环→非冲突可串行化；无环→冲突可串行化（必可串行化）。做题时优先抓“写-读、读-写、写-写”冲突。

**题型3：封锁协议与故障恢复概念**
方法：直接套定义对照表。问“防止死锁”选顺序封锁/一次封锁；问“保证并发正确”选2PL可串行化；问“断电后已提交不丢”对应持久性+REDO。

**题型4：ACID性质填空**
方法：分清“中途失败全部撤销=原子性”“转账守恒=一致性”“互不干扰=隔离性”“提交后永久=持久性”。

**题型5：锁相容与阻塞判断**
方法：列一个2×2相容矩阵（S/S相容，其余不相容），再看持有锁与申请锁的组合，不相容则等待。涉及“读加什么锁、写加什么锁”的题，读S写X是默认结论，读加X锁属于保守封锁，题目若未说明按基本定义答。

**题型6：故障恢复手段匹配**
方法：事务内部故障对应ROLLBACK；系统故障对未完成事务ROLLBACK、对已提交事务REDO；介质故障靠备份加日志装入。看到“断电后已提交数据不丢”就选“持久性 + 重做日志”。

## 四、典型例题

**例1（三种读异常）**
简述丢失修改、读脏数据、不可重复读的含义。

解：
（1）丢失修改：事务T1、T2先后读入同一数据并修改，T1写完后T2的写覆盖T1，T1的修改丢失。
（2）读脏数据：T1读入T2尚未提交的修改；若T2回滚，T1所读即无效中间值。
（3）不可重复读：T1读某数据后，T2修改（或删除）该数据并提交，T1再读同一数据得到不同值。
分析：答题要写出“谁读了谁、读到什么状态”。幻读是“两次查询结果集行数因插入/删除而不同”，若题目单独问要能区分。

**例2（可串行化判断）**
调度中：T1: r1(A)=100, w1(A)=50；T2: r2(A)=50, w2(A)=30；顺序为 r1(A), w1(A), r2(A), w2(A)。判断是否可串行化。

解：该调度本身就是 T1 → T2 的串行执行，结果A=30，可串行化。
对照：若顺序改为 r1(A), r2(A), w1(A), w2(A)，则T1、T2都基于A=100计算，最后A只被w2写成30（假设各自算A-50、A-70），且T1读到的结果可能丢失，冲突序存在环状依赖的循环使用问题，需用前趋图严格判定；而题给顺序无交错，必可串行化。
分析：完全串行执行的调度一定是可串行化的；交错执行不一定。判断题先看是否严格串行，不是则画冲突前趋图。

**例3（ACID）**
转账事务“从A扣100，给B加100”中，若扣款成功但加款失败，为保证A不被扣钱而B未加，系统必须具有（）性质。请解释。

解：原子性。原子性要求事务不可分割，全部操作要么都执行要么都不执行；恢复机制通过UNDO把已执行的扣款撤销。一致性是转账前后总额不变的结果；隔离性是与其他事务互不干扰；持久性是提交后不丢失。
分析：题目描述“要么全做要么全不做”，就是原子性的定义原文。

**例4（封锁相容性）**
事务T1对数据D加了S锁，此时T2希望读D、T3希望写D，是否允许？

解：允许T2加S锁继续读（S与S相容）；不允许T3加X锁写（S与X不相容），T3必须阻塞等待T1释放S锁。
分析：相容矩阵只有一对是相容的，即S与S。其余三种组合都互斥。答题写出“允许谁、阻塞谁、理由是相容矩阵”即可得满分。

## 五、易错点

1. **脏读与不可重复读混淆**——脏读是读了**未提交**数据且通常伴随对方回滚；不可重复读读的都是已提交数据，只是两次之间被别人改过。
2. **2PL是充分条件被当成充要**——满足2PL一定可串行化，但可串行化不一定满足2PL。
3. **S锁X锁相容性记反**——S与S才相容，只要有X就互斥。
4. **一致性当成并发控制问题**——一致性是结果性质，靠其他三项支撑；并发控制主要服务隔离性。
5. **幻读与不可重复读不分**——幻读看行数（增删），不可重复读看已有行的值（改）。
6. **认为提交后断电丢数据是原子性问题**——那是持久性（配REDO）问题。
7. **隔离级别顺序记乱**——从低到高：读未提交、读已提交、可重复读、可串行化。

## 六、考点提示

- ACID四词中英文与对应故障现象要能**一一配对**
- 三类读异常的**典型场景描述**是选择题原文来源
- 2PL：先只加锁后只解锁 → 可串行化（充分条件）
- 死锁与可串行化不要混：死锁是资源互等，可串行化是结果等价串行
- 若出现调度小题，优先找冲突操作画前趋图，比凭感觉猜可靠
- 两段锁协议只保证可串行化，**不保证不死锁**，死锁要单独处理
- 恢复类题抓住“已提交做REDO、未完成做UNDO”这句固定答案`,
    quizzes: [
      { id: 'sd-09-03a', type: 'choice', question: '事务的ACID特性中，保证“事务要么全部执行，要么全部不执行”的是（）', options: ['原子性', '一致性', '隔离性', '持久性'], answer: 'A', explanation: '原子性指事务是不可再分的工作单位，要么全部执行要么全部不执行，通过回滚（UNDO）实现。一致性指数据从一种合法状态变到另一种合法状态；隔离性指并发事务互不干扰；持久性指已提交的修改永久有效。' },
      { id: 'sd-09-03b', type: 'choice', question: '事务T1读出某数据后，事务T2修改了该数据并提交，T1再次读该数据时得到与第一次不同的值。这种现象称为（）', options: ['丢失修改', '脏读', '不可重复读', '幻读'], answer: 'C', explanation: '不可重复读是指同一事务内两次读同一数据，因其他事务修改（或删除）并提交而结果不同。脏读读的是未提交数据；丢失修改是写覆盖；幻读是因插入/删除导致两次查询结果集行数不同。' },
      { id: 'sd-09-03c', type: 'fill', question: '两段锁协议将事务的加锁分为扩展阶段和收缩阶段，一旦释放过锁就不再申请新锁。所有事务遵守两段锁协议是调度可串行化的______条件。', answer: '充分', explanation: '两段锁协议是可串行化的充分（不必要）条件：遵守2PL则任何调度可串行化；但存在可串行化却不符合2PL的调度。2PL还可能引起死锁，需另行处理。' },
      { id: 'sd-09-03d', type: 'short-answer', question: '说明丢失修改、脏读、不可重复读的区别，并指出“读已提交”隔离级别能防止其中哪些问题。', answer: '丢失修改：两个事务读同一数据做修改，后写覆盖先写，先写者的更新丢失。脏读：一个事务读到了另一个尚未提交事务的数据，对方回滚后所读无效。不可重复读：同一事务两次读同一行，其间另一事务修改并提交，导致两次读结果不同。“读已提交”只能防止脏读，不能防止丢失修改和不可重复读（防止不可重复读需可重复读及以上）。', explanation: '评分要点：三类异常各1分；隔离级别对应关系1分。注意区分不可重复读针对行值变化，幻读针对行数变化。' },
    ],
    references: [],
  },
];
