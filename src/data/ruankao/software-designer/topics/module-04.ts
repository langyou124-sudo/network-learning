import { Topic } from '@/types';

export const topics: Topic[] = [
  {
    id: 'sd-04-01',
    moduleId: 'sd-operating-system',
    title: '进程管理',
    description: '进程状态与调度、信号量与 PV 操作、死锁四条件与银行家算法，操作系统最高频考点',
    content: `# 进程管理

## 考情分析

- **分值归属**：操作系统上午题约 4–6 分，进程管理可占一半以上。下午题若考 PV 操作填空，分值可达 5–8 分。
- **题型**：选择题（状态转换、调度算法周转时间、死锁条件、银行家安全序列）+ 下午题 PV 操作程序填空（生产者消费者、读者写者等）。
- **考频**：★★★★★。进程状态、调度计算、PV、死锁四大块几乎每年至少两块出现，是必须拿满的章节。

## 一、核心概念

**进程**是程序在某个数据集上的一次运行，是系统资源分配和调度的独立单位。**程序**是静态指令序列，进程是动态执行过程。进程由程序段、数据段和**进程控制块 PCB** 组成。PCB 是进程存在的唯一标志，包含进程号、状态、优先级、寄存器现场、打开文件、资源清单等。

**进程 vs 线程**：线程是 CPU 调度的基本单位，进程仍是资源分配的基本单位。同一进程内线程共享地址空间与资源，切换线程比切换进程开销小。线程也有就绪、运行、阻塞等状态。

**进程状态**（三态模型）：
- **运行**：占有 CPU 正在执行。
- **就绪**：万事俱备只欠 CPU（缺 CPU）。
- **阻塞/等待**：因等待 I/O、信号量、消息等事件而暂停（缺的不是 CPU，是事件）。
转换关系：
- 运行 → 就绪：时间片用完、被更高优先级抢占。
- 进程就绪 → 运行：调度程序选中。
- 运行 → 阻塞：进程主动等待某事件（如 P 操作阻塞、I/O 请求）。
- 阻塞 → 就绪：等待的事件完成（I/O 完成、V 操作唤醒）。
注意：**阻塞不能直接到运行**，必须先就绪；**就绪不能直接到阻塞**，因为就绪态并不占有资源去等待事件。五态模型再加新建、终止。引起创建的事件有用户登录、作业调度、提供服务、应用请求；引起终止的有正常结束、异常结束、外界干预。同一程序段在不同数据集上可构成不同进程，同程序段被多个进程共享代码时数据集仍各自独立。

**临界资源与临界区**：一次只允许一个进程使用的资源是临界资源；访问临界资源的代码段是临界区。互斥的硬件方法（关中断、TSL 指令）与软件方法（Peterson）了解即可，考试主考**信号量**。

**信号量与 PV**：信号量 S 是一个整型变量，仅能通过 P、V 操作访问。
- P(S)：S := S-1；若 S < 0 则进程阻塞进入等待队列。
- V(S)：S := S+1；若 S <= 0 则唤醒一个等待进程。
互斥信号量初值一般为 1（资源数）；同步信号量初值为 0（事件是否发生）或缓冲产品数。

**经典同步问题**：
1. **生产者消费者**：mutex 互斥缓冲池（初值 1），empty 空格数（初值 n），full 产品数（初值 0）。生产者 P(empty), P(mutex), 放入, V(mutex), V(full)；消费者对称。**P 操作顺序不能颠倒**（先同步后互斥），否则易死锁。
2. **读者写者**：读进程可共享，写进程与任何进程互斥。count 记读者数，count 的互斥用 mutex，读写互斥用 rw。
3. **哲学家进餐**：资源分级拿、奇偶策略或最多允许 4 人同时拿，破环路。

**死锁**：各进程互相等待对方持有的资源，全体无限期阻塞。
- **四个必要条件**（缺一不死锁）：互斥、占有并等待（请求并保持）、不可抢占、循环等待。
- **处理**：预防（破坏四条件之一）、避免（银行家）、检测+解除、鸵鸟（忽略）。
- **银行家算法**：Need = Max − Allocation。试探分配后若存在**安全序列**（每个进程都能靠剩余资源+回收资源跑完）则分配，否则拒绝。Available 向量、Max 矩阵、Allocation 矩阵、Need 矩阵四件套。

**调度算法**与评价指标：
- **FCFS** 先来先服务：简单，利于长作业，不利于短作业（护航效应）。
- **SJF** 短作业优先：平均等待时间最短，但可能饿死长作业；是否抢占分为 SJF 与 SRTF（最短剩余时间）。
- **HRN** 高响应比：响应比 = (等待时间 + 要求运行时间) / 要求运行时间。等待越久优先级越高，避免饿死。
- **时间片轮转**：分时系统，时间片太大退化为 FCFS，太小切换开销大。
- **优先级调度**：可结合老化技术防饿死。
- **多级反馈队列**：综合型，UNIX/Linux 变种常见。

指标：周转时间 = 完成时间 − 到达时间；带权周转 = 周转 / 运行时间；平均周转、平均带权周转。吞吐量是单位时间完成的作业数；响应时间是从请求提交到首次响应。批处理系统重视吞吐量与周转时间，分时系统重视响应时间，实时系统重视截止时间与可预测性。调度层次分三级：高级调度（作业调度，决定谁进内存）、中级调度（交换，决定谁被换出）、低级调度（进程调度，决定谁上 CPU），考试最常考低级调度。

## 二、关键原理/公式/机制

### 1. 进程状态转换合法性

判断题/选择题高频。口诀：
- 「就绪缺 CPU，阻塞缺事件。」
- 「阻塞→就绪→运行是单行道，不能跳站。」
- 「时间片到：运行→就绪；I/O 请求：运行→阻塞。」

### 2. 信号量取值语义

- S > 0：可用资源数。
- S < 0：|S| 为等待队列中进程数。
- 互斥：P/V 必须成对出现在同一进程的临界区前后。
- 同步：P 在等待方，V 在事件发生方，可在不同进程。

### 3. 生产者消费者骨架（必须会写）

设 mutex=1, empty=n, full=0：
- 生产者：P(empty); P(mutex); 送产品; V(mutex); V(full);
- 消费者：P(full); P(mutex); 取产品; V(mutex); V(empty);
**先同步后互斥**。若两对 P 顺序颠倒，可能占用 mutex 后又阻塞在 empty，造成死锁。

### 4. 银行家安全序列判别

1. Work := Available；Finish[i] := false。
2. 找一个 Finish=false 且 Need[i] <= Work 的进程 i；若找到：Work := Work + Allocation[i]，Finish[i]=true，重复。
3. 若所有 Finish=true 则安全，且记录下即安全序列；否则不安全（未必立即死锁，但系统可能进入死锁）。

### 5. 调度时间计算式

- 完成时间看甘特图分段。
- 周转 Ti = Ci − Ai。
- 带权 Wi = Ti / 运行时间。
- 平均周转 = ΣTi / n。

## 三、常考题型与解题方法

**题型 1：状态转换判断**
方法：画三态图，看箭头是否合法。常错项「阻塞→运行」「就绪→阻塞」直接排除。

**题型 2：调度甘特图与平均周转**
方法：
1. 按到达时间排作业，画时间轴。
2. FCFS 按到达顺序；SJF 在就绪队列中挑最短；时间片轮转把运行超时者排到队尾。
3. 标完成时间，算周转与带权，求平均。
短题 2–3 作业必须手算一遍，考场上才快。

**题型 3：PV 填空**
方法：
1. 找出临界资源 → mutex（初值 1）；找出同步事件 → empty/full、S1/S2（初值 0/资源数）。
2. 生产者消费者等价转换：有「放」必有对应「满」的 V，有「取」必有对应「空」的 V。
3. 顺序：进入临界区前先做同步 P，再做互斥 P；离开先 V(mutex) 再 V(同步)。
4. 检查：同一临界区的 P(mutex)/V(mutex) 成对；不同进程的同步 P/V 配对。

**题型 4：死锁条件与资源数**
方法：
- 四条件能默写，并能说出破坏办法（如静态分配破「占有等待」，资源有序分配破「循环等待」）。
- **资源分配题**：n 个进程各最多要 k 个同类资源，要保证不死锁至少需要 n×(k−1)+1 个资源。推导：最坏每进程都持有 k−1 个还在等，再加 1 个必能放行一个。
- 抢占时间片与死锁无关，别乱选。

**题型 5：银行家算法**
方法：四矩阵写清楚，按 Need <= Work 逐个放行。若问「是否可再分配某请求」，先试分配再做安全检查。

**题型 6：进程与线程对比**
方法：记住三句——进程是资源分配单位，线程是调度执行单位，同一进程的线程共享地址空间。选择题凡说「线程拥有独立地址空间」「线程是资源分配单位」都错。

**题型 7：信号量取值含义**
方法：互斥信号量初值 = 资源个数；同步信号量初值 = 可用事件/产品数，常为 0。问「S = −3 表示什么」，答：已有 3 个进程在该信号量上等待。

## 四、典型例题

**例1**（资源数）某系统有同类资源 12 个，有 4 个进程，每个进程最多需要 4 个资源，系统是否可能死锁？

解：不死锁至少需要 n(k−1)+1 = 4×3+1 = 13 个。现在只有 12 < 13，**可能死锁**（当 4 进程各持 3 个时全体等待）。

**例2**（调度）3 个作业 A、B、C 到达时间 0、1、2，运行时间 4、2、3。按 FCFS 与 SJF 分别求平均周转时间。

解：
- FCFS 顺序 A→B→C：完成 A=4，B=6，C=9。周转 4、5、7。平均 = 16/3 ≈ 5.33。
- SJF（非抢占，t=0 只有 A）：A 仍先跑完（t=4），此时 B、C 都到，选短的 B（2）→完成 6，再 C 完成 9。平均同 5.33。
若 SRTF 抢占：t=1 时 A 剩 3，B 为 2，B 抢占跑完于 t=3；再比 A 剩 3 与 C 3，按到达 A 先，A 完成 6，C 完成 9。周转：A=6，B=2，C=7，平均 = 5.0，优于 FCFS。

**例3**（银行家）Available=(3,3,2)，P1 的 Max=(5,5,3)、Allocation=(2,1,0)。求 Need，并判断是否存在安全序列（其余进程略，按经典教材题：总资源 (10,5,7)，各进程 Max/Allocation 见分析）。

解：Need = Max − Allocation = (3,4,3)。安全序列判别按 Work 递推找 Need <= Work 的进程；若按经典数据能排出 P1,P3,P4,P2,P0 之一即安全。答题规范：写 Work 变化过程，不能只写「安全」。

## 五、易错点

1. **阻塞态缺的不是 CPU**，是事件；从阻塞不能直接进运行。
2. **PV 顺序**：生产者/消费者中先 P(empty/full) 再 P(mutex)，反了会死锁。
3. **V 操作不会阻塞**，P 才会；V 唤醒的是等待队列中一个进程，不是「让运行进程变就绪」的空话。
4. **临界区是代码**，临界资源是硬件/数据对象，别混。
5. **死锁四条件是「必要条件」**，破坏任一即可预防；题目说「只要满足四条件就一定死锁」是错误表述——四条件同时成立是可能死锁，还要看进程推进顺序。
6. **银行家「不安全 ≠ 已死锁」**，只是没有安全保证。
7. **线程不能独立拥有资源**（除少量栈与寄存器），资源仍按进程分配。
8. **SJF 平均等待最优是理论结论**，前提是已知运行时间；实际靠估计。
9. **资源公式 n(k−1)+1** 中是「每进程最多 k 个」；若题目写「共需要 k 个」含义不同，先读题。
10. **进程是资源分配单位、线程是调度单位**：两类单位不可对调，PCB 也随进程不随线程（线程有 TCB）。
11. **高响应比优先**不是优先级固定，它随等待时间动态变化，公式必须带「等待 + 要求」两项。
12. **时间片轮转在单作业时等价 FCFS**；时间片无穷大时也退化为 FCFS，两个极端都要会判断。

## 六、考点提示

- 三态转换图、调度指标公式、死锁四条件是上午题「秒选」项，必须熟。
- PV 操作是下午题硬骨头：把生产者消费者、读者写者两个模型的骨架默写到能独立写对。
- 银行家算法按矩阵练 1–2 题，掌握安全序列书写格式。
- 记「n(k−1)+1 资源公式」，选择题直接套。
- 线程与进程的对比表（谁调度、谁拥有资源、开销）常出概念题。
- 优先级老化、多级反馈队列只需记名称与目的（防饿死、综合折中）。
- 调度计算题务必写甘特图再求周转，口算平均值在带权周转时极易算错。
- 读者写者问题只要求读者互斥计数、写者独占，核心是 count 的临界区保护，比生产者消费者略简，但思路同源。
- 把「PV 顺序、n(k−1)+1、状态转换图」当成三张卡片反复自问，这三点覆盖进程管理大半分值。`,
    quizzes: [
      {
        id: 'sd-quiz-04-01a',
        type: 'choice',
        question: '进程从等待状态（阻塞）转换到运行状态，下列说法正确的是？',
        options: [
          '只要等待的事件发生就能直接进入运行状态',
          '必须先转换到就绪状态，再由调度进入运行状态',
          '不能转换到运行状态',
          '时间片用完时发生该转换'
        ],
        answer: 'B',
        explanation: '阻塞态进程等待事件完成后先变就绪，占有 CPU 才能运行。阻塞→运行、就绪→阻塞都是非法转换。'
      },
      {
        id: 'sd-quiz-04-01b',
        type: 'choice',
        question: '在生产者-消费者问题中，生产者进程进入临界区前的 P 操作顺序应为？',
        options: [
          '先 P(mutex) 再 P(empty)',
          '先 P(empty) 再 P(mutex)',
          '两者顺序可以任意',
          '只需 P(mutex)，不需 P(empty)'
        ],
        answer: 'B',
        explanation: '先同步（P(empty) 申请空格）再互斥（P(mutex) 进入缓冲池）。若先持有 mutex 再等 empty，可能造成死锁。'
      },
      {
        id: 'sd-quiz-04-01c',
        type: 'choice',
        question: '某系统有 4 个进程竞争同一类资源，每个进程最多需要 3 个资源，为保证系统不发生死锁，至少需要该类资源的个数是？',
        options: ['9', '10', '11', '12'],
        answer: 'B',
        explanation: '套用公式 n(k-1)+1 = 4×(3-1)+1 = 9+1 = 10。'
      },
      {
        id: 'sd-quiz-04-01d',
        type: 'choice',
        question: '死锁产生的四个必要条件不包括？',
        options: ['互斥条件', '请求并保持条件', '不可抢占条件', '时间片用完'],
        answer: 'D',
        explanation: '死锁四条件为：互斥、请求并保持（占有并等待）、不可抢占、循环等待。时间片用完是进程调度事件，与死锁四条件无关。'
      }
    ],
    references: [],
  },
  {
    id: 'sd-04-02',
    moduleId: 'sd-operating-system',
    title: '存储管理',
    description: '分区与页式段式存储、地址重定位、虚拟存储与页面置换（FIFO/LRU/OPT）、抖动与工作集',
    content: `# 存储管理

## 考情分析

- **分值归属**：上午题约 2–4 分，集中在页式地址计算、缺页次数、页面置换过程、有效访问时间计算。
- **题型**：选择题/简单计算题。典型问法：给逻辑地址求物理地址；给访问串求 FIFO/LRU/OPT 缺页次数；比较段页式与页式。
- **考频**：★★★★。页式地址变换与置换算法几乎年年有变体题，属于「套公式+手推」的稳定得分点。

## 一、核心概念

**存储管理目标**：内存分配与回收、地址变换、内存扩充（虚拟存储）、内存保护、共享。用户程序逻辑地址（相对地址）必须经**重定位**才能变成物理地址：静态重定位在装入时一次完成（装入后不能移动），动态重定位在执行时由硬件地址映射机构（基址+限长或页表）完成，支持程序浮动。

**单一连续分配 / 分区分配**：
- **固定分区**：预划分，有内碎片，无外碎片。
- **动态分区**：按需划分，无内碎片，有外碎片。放置策略：首次适应（按地址）、最佳适应（按容量递增，易留碎渣）、最坏适应（按容量递减）、邻近适应（循环首次）。
- **紧凑/拼接**：把外碎片拼成连续大块，开销大。
- **伙伴系统**：块大小为 2 的幂，分裂合并都成对，折中方案。

**覆盖与交换**：早期单道内存不够用时的两种技术。覆盖让不会同时执行的模块共用同一分区；交换把整个进程换出到外存（中级调度用到）。

**页式存储**：逻辑空间分**页**（页号+页内偏移），物理空间分**块/页框**（块号+块内偏移），页与块等长。逻辑地址被硬件分为页号 P 与页内偏移 W。**页表**（每进程一张）存于内存，实现页号→块号映射；页表寄存器指出页表始址与长度。
优点：无外碎片，内存利用率高。缺点：有内碎片（平均半页），不易共享与保护（按页不按逻辑模块）。

**段式存储**：按逻辑模块（段）分配，段长不等。**段表**含段号、段长、基址。优点：便于共享保护、动态增长。缺点：有外碎片，段长受内存连续空闲区限制。

**段页式**：先分段、段内再分页。地址结构：段号 + 页号 + 页内偏址。每次访问数据需三次访存（段表、页表、数据），引入**快表 TLB** 缓存段页表项可降至约一次多。

**虚拟存储器**：基于局部性原理，只把当前要用的页/段装入内存，其余留外存。特征：多次性、对换性、虚拟性。容量由指令中地址字长决定，与实际内存无关。实现：请求分页、请求分段。硬件支持：页表/段表中的存在位、修改位、访问字段、外存地址，以及缺页中断机制、地址变换机构。

**页面置换算法**：
- **OPT 最佳**：淘汰未来最长时间不用的页。理论最优，不可实现，作标尺。
- **FIFO**：淘汰最早进入的页。可能出现 **Belady 异常**（分配块增多缺页反而增加）。
- **LRU 最近最少使用**：淘汰最久未访问的页。性能接近 OPT，无 Belady 异常，但需硬件栈或计数器支持。
- **Clock（二次机会）**：LRU 的近似，用访问位循环扫描。
- **LFU 最不经常使用**：按访问次数淘汰，可能留下很久以前的「热点」。

**抖动（颠簸）**：进程缺页率过高，CPU 利用率反而下降（因为都在换页）。原因：进程分到的物理块太少、进程过多。对策：工作集模型、增大内存、控制多道程度。抖动出现时增加多道程序度只会更糟，这是「不是进程越多越好」的经典论据。
**工作集**：某段时间窗口 Δ 内进程访问的页面集合。让工作集整体驻留可显著降缺页。工作集模型本质是用局部性原理在「装入过少导致缺页」与「装入过多浪费内存」之间找平衡。

**地址保护**：页表设保护位；段式天然可段级保护（R/W/E/X）。段页式兼顾。

## 二、关键原理/公式/机制

### 1. 页式地址变换（必会计算）

设页面大小 2^k，则：
- 页号 = 逻辑地址 / 2^k（或右移 k 位）
- 页内偏移 = 逻辑地址 % 2^k（或取低 k 位）
- 物理地址 = 块号 × 2^k + 页内偏移

例：页面 4KB = 2^12，逻辑地址 0x3A7F → 页号 3，偏移 0xA7F；若页表[3]→块 5，则物理地址 = 5×4096 + 0xA7F = 0x5A7F。

段式：物理地址 = 段基址 + 段内偏移（需检查偏移 < 段长，否则越界中断）。

### 2. 有效访问时间（EAT）

设访存时间 t，查 TLB 时间可忽略或为 t'，命中率 h：
- 仅页表：EAT = 2t（一次页表 + 一次数据）。
- 带 TLB：EAT = h×t + (1−h)×2t（命中一次访存，未命中两次）。
- 若考虑缺页：EAT 要加上缺页率 × 缺页处理时间（缺页中断：中断处理 + 页面传输 + 重启动），选择题按题干给的数值代入。

### 3. 缺页次数手推格式

写表：访问串逐列，当前内存块内容逐步更新。注意：
- FIFO 看「进入时间」，先进先淘汰。
- LRU 看「上次使用时间」，最久未用淘汰。
- OPT 看「未来」，最晚再用/不再用的淘汰。
首次访问某页必缺页。块数相同才可比算法优劣。

### 4. 内存/地址容量关系

- 逻辑地址空间 = 页号位数决定的页数 × 页大小。
- 物理块数 = 内存大小 / 块大小。
- 页表项数 = 进程页数（不是块数）。
- 页表本身占用空间 = 页表项数 × 项长，常单独考「页表占多少内存」。

### 5. 分区分配与碎片

- 内碎片：分给进程的分区大于其需求，多出来的在分区内部。固定分区、页式有。
- 外碎片：空闲分区总量够但不连续。动态分区、段式有。
- 紧凑消除外碎片，代价是内存搬移。

### 6. Belady 与工作集

- FIFO 可能 Belady 异常：如访问串 1 2 3 4 1 2 5 1 2 3 4 5，3 块与 4 块缺页次数反常。经典考点直接给数字比较。
- 工作集大小 w(t,Δ) 随时间变；给驻留集 < 工作集时抖动风险高。

## 三、常考题型与解题方法

**题型 1：逻辑地址→物理地址**
方法：
1. 看清页面大小，算出页号与偏移的位数。
2. 查题干页表得块号。
3. 物理地址 = 块号 × 页大小 + 偏移。
4. 若是段式加基址并检查段长；段页式先段后页。

**题型 2：缺页次数对比**
方法：画三列以上的空框代表物理块，按访问串填页号并标缺页。换出策略按算法定义，不凭印象。算完检查总访问次数 = 缺页 + 命中。

**题型 3：EAT 计算**
方法：把命中/未命中分支列成期望式。有 TLB 先算 TLB 项；有缺页率再把缺页代价加权进期望。

**题型 4：碎片与分配策略**
方法：内碎片想「块/分区大于需求」，外碎片想「空闲不连续」。最佳适应易留小碎片，最坏适应留中等碎片，首次适应较均衡——选择题按教材结论。

**题型 5：抖动与多道程序度**
方法：并非「进程越多 CPU 越忙」。超过临界值缺页暴增、CPU 利用率下降。对策选工作集、降低多道度。

**题型 6：地址保护与共享**
方法：页式保护粒度粗、共享按页不方便；段式按逻辑模块共享保护自然；段页式兼有两者特征。题目问「便于共享与保护」选段式或段页式，问「没有外碎片」选页式。

**题型 7：重定位方式判断**
方法：装入时一次完成、不能移动 = 静态重定位；执行时硬件映射、支持虚拟存储与程序浮动 = 动态重定位。操作系统普遍用动态。

## 四、典型例题

**例1**（地址转换）某系统页面大小 4KB，某进程逻辑地址空间 32KB，页表如下：页0→块2，页1→块3，页2→块1，页3→块5，页4→块4。求逻辑地址 12296 对应的物理地址。

解：4KB = 4096。12296 / 4096 = 3 余 12296 − 12288 = 8。页号 3，偏移 8。查表页3→块5。物理地址 = 5×4096 + 8 = 20488。

**例2**（LRU）页面访问串 1,2,3,4,1,2,5,1,2,3,4,5，分配 3 个物理块，用 LRU 求缺页次数。

解：过程（|分隔内存）：
1 进(缺)|2 进(缺)|3 进(缺满)|4 换1(缺)|1 换3(缺)|2 命中|5 换4(缺)|1 命中|2 命中|3 换5(缺)|4 换1(缺)|5 换2(缺)
数缺页标记：除「2 命中」「1 命中」「2 命中」外皆缺，共 **9** 次缺页。（自检：12 次访问中 3 次命中。）
同串 FIFO 在 3 块下也是 9 次左右，但 4 块时 FIFO 会出现 Belady 异常，可对照记忆。

**例3**（EAT）访存 100ns，TLB 命中率 0.9，忽略 TLB 本身查找时间，求有效访问时间。

解：EAT = 0.9×100 + 0.1×200 = 90 + 20 = 110ns。若再给缺页率 0.01、缺页中断 20000ns，则需把缺页分支加进期望，按题干公式再算。

## 五、易错点

1. **页大小是 2 的幂**，用位移算页号偏移最快；用十进制除法注意余数。
2. **页表项数 = 进程页数**，不是物理块数；页表登记的是「页号→块号」。
3. **内碎片外碎片**张冠李戴：页式有内无外，段式有外无内，动态分区有外，固定分区有内。
4. **FIFO 才有 Belady 异常**，LRU、OPT 没有。
5. **OPT 看未来，LRU 看过去**，别把淘汰页判反。
6. **抖动不是缺页一次**，是缺页率异常升高导致系统性能下降；缺页本身是正常机制。
7. **虚拟内存容量**由地址结构（字长）决定，不是「内存+外存」之和。
8. **段页式每次访问约需 3 次访存**（段表、页表、数据），这是它相对页式的主要代价。
9. **静态重定位后程序不能移动**，动态重定位支持浮动与虚拟存储。
10. **段页式是「先分段再分页」**，地址结构顺序是段号、页号、页内地址；顺序写反则题型全错。
11. **缺页中断属于内部中断/异常**，是在指令执行期间检测到的，与一般 I/O 中断的处理时机不同，但都要保护现场。
12. **伙伴系统合并条件**是两个伙伴都空闲且大小相同，不能跨级合并；它同时存在内碎片，是折中方案。

## 六、考点提示

- 页式地址计算是送分题，公式写熟：P = A/页大小，W = A%页大小，物理 = 块×页大小 + W。
- FIFO/LRU/OPT 三种置换必须能完整手推一串访问，考场上画表格防看错。
- EAT 计算记通用期望式，按题干是否给 TLB、缺页率决定展开哪几层。
- 碎片、分配策略、段页式优缺点属概念题，用对比表记忆。
- 抖动与工作集理解「多道程序度不是越大越好」这一结论即可。
- 伙伴系统、覆盖交换属于低频了解点，知道名词和用途。
- 页式地址计算建议直接把「页大小 4K = 位移 12」这种换算写在草稿上，避免十进制除法算错偏移。
- 替换算法题自检：访问次数 = 命中 + 缺页；每块是否在该时刻都被占用；换出的是不是按算法规则该换的那一页。
- 重点背：无外碎片是页式，无内碎片是段式/动态分区，两者对比是选择题常见对立选项。`,
    quizzes: [
      {
        id: 'sd-quiz-04-02a',
        type: 'choice',
        question: '某系统页面大小为 4KB，逻辑地址为 8194，则页号和页内偏移分别为？',
        options: ['页号 2，偏移 2', '页号 2，偏移 10', '页号 3，偏移 2', '页号 1，偏移 4098'],
        answer: 'A',
        explanation: '4KB=4096。8194÷4096=2 余 2，故页号为 2，页内偏移为 2。'
      },
      {
        id: 'sd-quiz-04-02b',
        type: 'choice',
        question: '关于页面置换算法，下列说法正确的是？',
        options: [
          'FIFO 算法可能会出现 Belady 异常，LRU 算法不会',
          'OPT 算法在实际系统中可以很方便地实现',
          'LRU 算法可能出现 Belady 异常，FIFO 不会',
          '三种算法在任何访问串下缺页次数都相同'
        ],
        answer: 'A',
        explanation: 'FIFO 属于栈算法之外的置换策略，块数增加缺页次数反而可能增加（Belady 异常）；LRU 与 OPT 满足栈算法性质，无此异常。OPT 依赖未来访问序列，无法实用。'
      },
      {
        id: 'sd-quiz-04-02c',
        type: 'choice',
        question: '页式存储管理产生的碎片是？',
        options: ['外碎片', '内碎片', '内碎片与外碎片都有', '不产生碎片'],
        answer: 'B',
        explanation: '页式按固定大小的页分配，最后一页装不满时产生页内碎片（内碎片），但不存在外碎片。段式与动态分区才会产生外碎片。'
      },
      {
        id: 'sd-quiz-04-02d',
        type: 'choice',
        question: '虚拟存储器的最大容量取决于？',
        options: ['内外存容量之和', 'CPU 的寻址范围（地址字长）', '页表大小', '内存容量'],
        answer: 'B',
        explanation: '虚拟地址空间由指令中地址字段的位数决定，例如 n 位地址理论上可寻址 2^n，与实际内存容量无直接关系。'
      }
    ],
    references: [],
  },
  {
    id: 'sd-04-03',
    moduleId: 'sd-operating-system',
    title: '文件与设备管理',
    description: '文件逻辑结构与目录、外存分配（连续/链接/索引）、空闲空间管理、磁盘调度与设备 I/O 控制',
    content: `# 文件与设备管理

## 考情分析

- **分值归属**：上午题约 2–3 分。文件分配方式、磁盘调度移臂距离计算是两大计算点；文件目录、设备分类与 I/O 控制方式多为概念题。
- **题型**：选择题为主。给磁道访问序列求 FCFS/SSTF/SCAN 移动总道数；给文件大小判断索引分配能否放下；比较三种外存分配优缺点。
- **考频**：★★★★。磁盘调度几乎每隔一年就换数字重考，文件分配与位图/链接空闲管理属常青概念题。

## 一、核心概念

**文件**是具有符号名的一组相关信息的集合（数据项、记录、文件三级）。**文件系统**负责按名存取、外存分配、目录管理、存取控制、安全与共享。

**逻辑结构**：
- **流式**：无结构字节流（UNIX 传统）。
- **记录式**：定长/变长记录序列。
- 索引文件、索引顺序文件用于快速检索关键字段。

**物理结构 = 外存分配方式**（考试核心）：
1. **连续分配**：文件占连续物理块。优点：顺序、随机存取都快（起始块+长度即可）。缺点：动态增长难，产生外碎片，需紧凑。
2. **链接分配**：块间用指针串联。优点：无外碎片，易增长。缺点：只能顺序存取，指针占空间，可靠性差（一处断链丢一片）。改进：**文件分配表 FAT** 把指针集中存表中。
3. **索引分配**：为文件建**索引表**（索引块），项为「逻辑块号→物理块号」。支持随机存取，无外碎片。大文件用多级索引、混合索引（UNIX i-node 的直接/一级/二级/三级索引）。缺点：索引块有额外开销。
4. 直接/散列：按关键字散列定位，适合定长记录。

**文件目录**：
- **FCB 文件控制块**：文件名、大小、权限、物理位置、时间等，是「文件存在的标志」。
- 目录结构：一级（易重名）、二级（主文件目录+用户目录）、树形（多级，现代主流）、无环图（便于共享，链接计数）。
- 路径名：绝对/相对。工作目录/当前目录缩短路径。
- 目录检索：顺序检索、Hash 检索。

**存取控制与安全**：访问控制表 ACL（每文件一份，对主体授权）、能力表（每主体一份）。保护域、访问矩阵是理论模型。口令与加密是保密手段。

**空闲空间管理**：
- **空闲表法**：类似分区表，适合连续分配。
- **空闲链表**：空闲块成链。
- **位示图/位图**：一位代表一块，0 空闲 1 占用（或相反），易找连续区，内存常驻位图适合大文件系统。
- **成组链接法**：UNIX 思想，空闲块分组链起来。

**文件的使用**：创建、删除、打开（把 FCB 调入内存活动文件表，之后用文件号）、关闭、读写、截断。**打开**的意义是避免每次访问重复检索目录。

**设备管理**：
- **分类**：按传输速率（低速键盘、中速打印机、高速磁盘）、按信息交换单位（字符设备、块设备）、按共享（独占、共享、虚拟设备）。
- **I/O 控制方式**：
  1. **程序查询**：CPU 轮询忙等，效率最低。
  2. **中断**：设备就绪发中断，CPU 不必忙等。
  3. **DMA**：直接内存访问，以块为单位在设备与内存间传送，CPU 只在开始/结束介入。
  4. **通道**：I/O 专用处理机，执行通道程序，以一组块为单位，CPU 干预最少。
- **SPOOLing**（假脱机）：用磁盘做缓冲，把独占设备改造为共享的虚拟设备。经典：打印机守护进程 + 打印队列。
- **缓冲**：单缓冲、双缓冲、循环缓冲、缓冲池。缓解 CPU 与 I/O 速度不匹配。双缓冲可并行处理与输入。
- **设备分配**：设备控制表 DCT、控制器表 COCT、通道表 CHCT、系统设备表 SDT。分配应考虑设备固有属性（独占/共享）、分配算法、安全性、独立性（设备独立性：用逻辑设备名，由系统映射物理设备）。
- **磁盘结构**：磁道（柱面）、扇区。访问时间 = 寻道时间 + 旋转延迟 + 传输时间。寻道占大头，故调度重在**移臂**。磁盘属于共享的块设备，可随机寻址；磁带只能顺序存取。同一磁盘上同时只有一个读写臂移动，因此移臂调度的优劣直接影响整个系统 I/O 吞吐。

**磁盘调度算法**：
- **FCFS**：公平，按请求到达顺序。
- **SSTF 最短寻道优先**：每次选距当前磁道最近者。可能饿死远端。
- **SCAN 电梯**：单方向扫描到底再折返。
- **CSCAN 循环扫描**：单向到底后立即回起点重新扫描，响应更均匀。
- LOOK / CLOOK：不走到尽头，到最远请求即折返。

## 二、关键原理/公式/机制

### 1. 磁盘移臂距离计算

总移道数 = 相邻服务磁道号之差的绝对值之和。起始磁头位置题干必给。
- SSTF：每步画当前位置，挑最近。
- SCAN：先判方向（常由题干「向磁道号增大方向移动」指定），同方向服务完再反向。
比较大小时，把总道数算出来再比，不要凭「感觉哪个快」。

### 2. 旋转延迟与传输时间

平均旋转延迟 ≈ 转半圈时间 = (60 / RPM) / 2 秒。传输时间 = 文件大小 / 传输率。题干若给 RPM 与传输率就代入；否则只比较寻道。

### 3. 索引文件最大长度计算

设索引块大小 B，块号占 d 字节（或一项大小），一块可放 B/d 个索引项：
- 单级索引最大文件 = (B/d) × 块大小。
- 混合索引（UNIX 风格 10 直接 + 一级 + 二级 + 三级）按题干给的直接/间接指针数累加：
最大长度 = 直接指针数×块大小 + 一级可寻址块数×块大小 + 二级…（注意二级是「每项再指一个索引块」的平方级增长）。

### 4. 位图占用空间

位图字节数 = 磁盘块总数 / 8（1 位 1 块）。查找连续空闲块用位图最方便。

### 5. FAT 链接存取

逻辑块号转换为 FAT 表项跳转次数。FAT 把指针集中，可一次读入内存，随机存取好于朴素链接分配。

### 6. SPOOLing 组成

输入井、输出井、输入缓冲区、输出缓冲区、输入进程、输出进程。共享打印机：各进程把打印数据送「输出井」排队，守护进程真正独占打印机。

## 三、常考题型与解题方法

**题型 1：磁盘调度移道数**
方法：
1. 写出请求磁道序列与当前磁头。
2. 按算法重排服务顺序（SSTF 贪心；SCAN 看方向；CSCAN 同向到底回零）。
3. 逐段求 |道差| 累加。
4. 需要比较平均等待时间时，再按服务次序算每个请求的等待。

**题型 2：文件分配方式优缺点**
方法：口诀——连续「快但不灵活有外碎片」，链接「灵活但只能顺序」，索引「可随机但有索引开销」。FAT 是链接的改良，i-node 是索引的混合。

**题型 3：索引最大文件长度**
方法：画出索引结构层次，按「每索引块项数 = 块大小 / 块号长度」逐级相乘。别把二级索引写成项数×2，应是项数² 规模。

**题型 4：空闲管理方式选择**
方法：连续分配配空闲表；链接配空闲链；通用大磁盘配位图/成组链接。看题干关键词。

**题型 5：I/O 控制方式排序**
方法：CPU 介入程度：查询 > 中断 > DMA > 通道。题干问「效率最高/CPU 干预最少」选通道或 DMA（块级看 DMA，多块大批量看通道）。

**题型 6：SPOOLing 判断**
方法：独占设备改共享、虚设备、打印队列 —— 三个关键词出现就选 SPOOLing。

**题型 7：缓冲的作用**
方法：缓和速度不匹配、减少中断次数、提高并行。单缓冲「处理与输入可部分并行」，双缓冲并行度更高，缓冲池用于多进程共享。问「为什么要加缓冲」选速度匹配与减少 CPU 干预，不选「提高磁盘转速」之类无关项。

**题型 8：目录与 FCB**
方法：按名存取靠目录检索，FCB 是文件存在的标志；打开文件后活动文件表减少重复检索。多级目录解决重名与检索效率，无环图目录解决共享（注意链接计数）。

## 四、典型例题

**例1**（SSTF）磁头当前在 53，请求队列 98,183,37,122,14,124,65,67。求 SSTF 总移道数。

解：服务顺序 53→65→67→37→14→98→122→124→183。
距离：12 + 2 + 30 + 23 + 84 + 24 + 2 + 59 = 236。
（对照 FCFS：53→98→183→37→122→14→124→65→67，移道明显更长；SCAN 向大方向则是另一序列。）

**例2**（SCAN）同上请求，磁头在 53 且向磁道号增加方向移动（到 199 折返）。求 SCAN 总移道数。

解：同向服务 65,67,98,122,124,183，到 199 折返后服务 37,14。
路径：53→65→67→98→122→124→183→199→37→14。
距离：12+2+31+24+2+59+16+162+23 = 331。若 LOOK 则 183 就折返，省去 199 那 16 道。

**例3**（索引）块大小 4KB，块号占 4 字节，一个索引块可存多少索引项？单级索引支持的最大文件多大？

解：索引项数 = 4096/4 = 1024 项。最大文件 = 1024 × 4KB = 4MB。若为混合索引（10 直接 + 一级 + 二级），再按「10×4KB + 1024×4KB + 1024²×4KB」分层累加。

## 五、易错点

1. **磁盘调度只优化寻道**，旋转延迟与传输时间常被忽略；比较算法时只移臂是题目默认。
2. **SCAN 与 LOOK**：SCAN 扫到物理尽头，LOOK 到最远请求就折返；CSCAN 回起点不服务回头路。
3. **SSTF 可能饿死**远磁道请求，不是最优全局。
4. **连续分配有外碎片无内碎片**（文件按整块时几乎不谈内碎片）；索引有索引块开销不算「碎片」。
5. **打开文件 ≠ 读内容**：打开是把目录项/FCB 装入内存，返回句柄。
6. **位图一位一块**，别算成一字节一块。
7. **DMA 与通道**：DMA 受 CPU 启动结束干预，通道有自己的指令系统，CPU 干预更少；题干问「以块为单位」偏 DMA，「以一组块/通道程序」偏通道。
8. **SPOOLing 不提高单台物理设备速度**，而是提高利用率与并发感受。
9. **FAT 属于链接分配的改进**，不是索引分配。
10. **设备独立性**指逻辑设备名与物理设备名的映射，不是「不用驱动」。
11. **磁盘访问时间三段**：寻道 + 旋转延迟 + 传输；调度算法只优化寻道，算平均等待时别漏了旋转延迟若题干给出。
12. **索引文件的索引块里存的是物理块号映射**，不是文件数据；算最大长度时用「索引块可容纳项数 × 块大小」逐级乘开。
13. **SPOOLing 的井在磁盘上**，缓冲区在内存中，两者不要混称。

## 六、考点提示

- 磁盘调度移道数计算是本课题最高频计算题，FCFS/SSTF/SCAN/CSCAN 四种都要会推表。
- 三种外存分配的优缺点对比表必须能默写，这是概念题的「题库母本」。
- 索引文件最大长度、位图大小是第二计算点，注意单位换算（KB、字节）。
- I/O 控制方式按「CPU 干预从多到少」排序记忆：查询、中断、DMA、通道。
- SPOOLing、缓冲、设备独立性是三个必考概念名词，会识别场景即可。
- i-node 混合索引结构理解「直接指针快、间接指针支持大文件」的设计权衡，有助于下午题软件设计思路。
- 磁盘调度四算法建议用同一道访问序列各推一遍，对比总道数，形成肌肉记忆。
- 文件分配「连续快而僵、链接灵而慢、索引折中但有开销」三句总结，可直接当选项排除依据。
- I/O 控制方式、SPOOLing、缓冲、设备独立性是设备管理的四个必考名词，看到场景要能准确对号。`,
    quizzes: [
      {
        id: 'sd-quiz-04-03a',
        type: 'choice',
        question: '磁盘调度算法中，每次选择距当前磁头最近的磁道请求的算法是？',
        options: ['FCFS', 'SSTF', 'SCAN', 'CSCAN'],
        answer: 'B',
        explanation: 'SSTF（最短寻道时间优先）贪心地服务最近请求。SCAN 是电梯式单向扫描，CSCAN 是单向扫描后回起点，FCFS 按到达顺序。'
      },
      {
        id: 'sd-quiz-04-03b',
        type: 'choice',
        question: '下列文件物理结构中，最适合随机存取且不会产生外部碎片的是？',
        options: ['连续分配', '链接分配', '索引分配', '连续分配与链接分配'],
        answer: 'C',
        explanation: '索引分配通过索引表可直接定位任意逻辑块，支持随机存取；文件不占用连续空间，无外碎片。连续分配有外碎片，链接分配不支持高效随机存取。'
      },
      {
        id: 'sd-quiz-04-03c',
        type: 'choice',
        question: '下列 I/O 控制方式中，CPU 干预最少、以数据块为单位在设备与内存间直接传送的是？',
        options: ['程序查询方式', '中断方式', 'DMA 方式', '以上都不对'],
        answer: 'C',
        explanation: 'DMA 由 DMA 控制器完成成块传送，CPU 仅在开始与结束时介入。查询需 CPU 忙等，中断方式以字节/字为单位且频繁打断 CPU。'
      },
      {
        id: 'sd-quiz-04-03d',
        type: 'choice',
        question: '用位示图管理磁盘空闲空间，若磁盘共有 8192 个物理块，则位示图占用的字节数为？',
        options: ['1024', '2048', '4096', '8192'],
        answer: 'A',
        explanation: '一位对应一个物理块，8192 块需要 8192 位，即 8192÷8 = 1024 字节。'
      }
    ],
    references: [],
  },
];
