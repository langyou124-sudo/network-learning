export const topic_06_04 = {
  title: "生成树协议",
  description: "STP 防止环路、RSTP 快速收敛",
  content: `# 生成树协议

  ## 开篇：为什么需要生成树？

  交换机之间为了冗余，通常会连接多条线路。但以太网有一个致命问题：**环路会导致广播风暴**。

  想象你在一个房间里喊"谁是张三？"，如果有回声，你的声音会不断循环，越来越大。网络中的广播帧也是如此——在环路中不断循环，最终耗尽所有带宽。

  **生成树协议（STP）** 就是为了解决这个问题：在有冗余的网络中，自动"切断"一些链路，消除环路。

  <Glossary terms="%5B%7B%22term%22%3A%22STP%22%2C%22english%22%3A%22Spanning%20Tree%20Protocol%22%2C%22definition%22%3A%22%E7%94%9F%E6%88%90%E6%A0%91%E5%8D%8F%E8%AE%AE%EF%BC%8C%E9%80%9A%E8%BF%87%E7%A6%81%E7%94%A8%E5%86%97%E4%BD%99%E9%93%BE%E8%B7%AF%E6%B6%88%E9%99%A4%E4%BB%A5%E5%A4%AA%E7%BD%91%E7%8E%AF%E8%B7%AF%22%7D%2C%7B%22term%22%3A%22%E6%A0%B9%E6%A1%A5%22%2C%22english%22%3A%22Root%20Bridge%22%2C%22definition%22%3A%22STP%E7%BD%91%E7%BB%9C%E4%B8%AD%E7%9A%84%E2%80%9C%E4%B8%BB%E8%8A%82%E7%82%B9%E2%80%9D%EF%BC%8C%E6%89%80%E6%9C%89%E8%B7%AF%E5%BE%84%E9%83%BD%E4%BB%A5%E5%AE%83%E4%B8%BA%E5%8F%82%E8%80%83%E7%82%B9%22%7D%2C%7B%22term%22%3A%22RSTP%22%2C%22english%22%3A%22Rapid%20Spanning%20Tree%20Protocol%22%2C%22definition%22%3A%22%E5%BF%AB%E9%80%9F%E7%94%9F%E6%88%90%E6%A0%91%E5%8D%8F%E8%AE%AE%EF%BC%8CSTP%E7%9A%84%E5%8D%87%E7%BA%A7%E7%89%88%EF%BC%8C%E6%94%B6%E6%95%9B%E9%80%9F%E5%BA%A6%E6%9B%B4%E5%BF%AB%22%7D%2C%7B%22term%22%3A%22%E5%B9%BF%E6%92%AD%E9%A3%8E%E6%9A%B4%22%2C%22english%22%3A%22Broadcast%20Storm%22%2C%22definition%22%3A%22%E5%B9%BF%E6%92%AD%E5%B8%A7%E5%9C%A8%E7%8E%AF%E8%B7%AF%E4%B8%AD%E4%B8%8D%E6%96%AD%E5%BE%AA%E7%8E%AF%EF%BC%8C%E8%80%97%E5%B0%BD%E5%B8%A6%E5%AE%BD%22%7D%5D" />

  ## 环路问题演示

  假设有三台交换机 A、B、C，它们两两相连形成一个三角形：

  >       A
  >      / \\
  >     B---C

  当 A 收到一个广播帧时：
  1. A 把帧发给 B 和 C
  2. B 收到后发给 A 和 C
  3. C 收到后发给 A 和 B
  4. A 又收到这个帧，继续转发……

  **结果**：广播帧在网络中无限循环，最终导致网络瘫痪。

  环路带来的问题主要有三种：

  **1. 广播风暴（Broadcast Storm）**
  广播帧在环路中被无限复制和转发，每台交换机都会不断收到并再次泛洪这些帧。短短几秒内，网络中的广播帧数量呈指数级增长，最终耗尽所有链路带宽和交换机 CPU 资源，导致正常业务流量完全中断。

  **2. MAC 地址表震荡（MAC Table Instability）**
  交换机从不同端口反复收到同一源 MAC 地址的帧，导致 MAC 地址表中的条目被不断覆盖更新。例如，交换机先从端口 1 学习到 MAC-A，随后又从端口 2 收到同一 MAC-A 的帧，表项就在两个端口之间来回切换。这使得交换机无法正确转发帧——本应发往端口 1 的帧可能被错误地发往端口 2。

  **3. 重复帧（Duplicate Frame）**
  同一数据帧通过不同路径多次到达目的主机。接收端的网卡会收到同一帧的多个副本，轻则浪费处理资源，重则导致上层应用（如 TCP）出现序列号混乱等异常。

  ## STP 的工作原理

  STP 的核心思想是：**选择一些链路"激活"，禁用其他链路，让网络形成一棵没有环路的"树"。**

  STP 通过交换 **BPDU（网桥协议数据单元）** 报文来完成拓扑计算。每个 BPDU 报文携带以下关键信息：

  - **根桥 ID**：发送者认为的根桥的 Bridge ID
  - **根路径开销**：发送者到达根桥的累计路径开销
  - **发送者桥 ID**：发送该 BPDU 的交换机的 Bridge ID
  - **端口 ID**：发送该 BPDU 的端口编号

  <Diagram type="stp-topology" />

  ### 第一步：选举根桥

  **根桥选举规则：比较 Bridge ID，值最小者胜出。**

  Bridge ID 由两部分组成：**优先级（2 字节）** + **MAC 地址（6 字节）**。比较时先比较优先级，优先级相同再比较 MAC 地址。默认优先级为 32768，因此在未手动配置的情况下，MAC 地址最小的交换机会成为根桥。

  选举过程：启动初期，每台交换机都认为自己是根桥，向外发送 BPDU。当收到 Bridge ID 更小的 BPDU 时，发送方停止宣告自己为根桥，转而转发更优的 BPDU。最终全网达成一致，选出唯一的根桥。

  所有交换机参与选举，**Bridge ID 最小的成为根桥**。

  Bridge ID = 优先级（2字节）+ MAC 地址（6字节）

  默认优先级都是 32768，所以通常 MAC 地址最小的交换机成为根桥。

  **可以手动设置优先级来控制根桥选举：**
  > Switch(config)# spanning-tree vlan 1 root primary

  ### 第二步：选举根端口

  每个非根桥交换机选择一个**根端口**——到达根桥路径 cost 最小的端口。

  Cost 根据链路带宽计算：

  | 带宽 | Cost |
  |------|------|
  | 10 Mbps | 100 |
  | 100 Mbps | 19 |
  | 1 Gbps | 4 |
  | 10 Gbps | 2 |

  **路径开销的计算方法**：每台交换机在收到 BPDU 时，将接收端口的开销值加到 BPDU 中携带的根路径开销上，得到通过该端口到达根桥的总开销。如果从多个端口收到 BPDU，则选择总开销最小的端口作为根端口。当开销相同时，依次比较：发送者 Bridge ID → 发送者端口 ID → 本端端口 ID。

  ### 第三步：选举指定端口

  每条链路选择一个**指定端口**——负责在这条链路上转发数据的端口。

  规则：离根桥更近的一方成为指定端口。

  ### 第四步：阻塞非指定端口

  既不是根端口也不是指定端口的端口，进入**阻塞状态**，不转发数据。

  ### 最终结果

  环路被打破，形成一棵以根桥为根的生成树。

  >       Root(A)
  >       /    \\
  >    端口    端口
  >     |        |
  >    B -------C
  >     端口(阻塞)

  ## STP 的端口状态

  STP 端口有五种状态：

  | 状态 | 持续时间 | 行为 |
  |------|----------|------|
  | 禁用 | - | 端口关闭 |
  | 阻塞 | 20 秒 | 不转发，只接收 BPDU |
  | 监听 | 15 秒 | 不转发，参与选举 |
  | 学习 | 15 秒 | 不转发，学习 MAC 地址 |
  | 转发 | - | 正常转发 |

  **问题**：从阻塞到转发需要 30-50 秒，太慢了！

  ## RSTP：快速生成树

  **RSTP（Rapid Spanning Tree Protocol）** 是 STP 的升级版，收敛时间从 30-50 秒缩短到**几秒钟**。

  ### RSTP 的改进

  **1. 新的端口角色**：
     - **替代端口（Alternate）**：根端口的备份。当根端口失效时，替代端口可立即接替，无需等待定时器超时。替代端口通过接收其他交换机的 BPDU 来学习到根桥的路径。
     - **备份端口（Backup）**：指定端口的备份。当本交换机的指定端口失效时，备份端口可接替。备份端口接收的是自己发送的 BPDU（说明同一网段有多个端口连接），主要用于共享介质环境。

  **2. 提议/同意（Proposal/Agreement）机制**：
     这是 RSTP 实现快速收敛的核心。当新链路建立或拓扑发生变化时：
     - 上游交换机的指定端口发送带有 **Proposal 标志** 的 BPDU
     - 下游交换机收到后，立即阻塞自己的非边缘端口（防止临时环路），然后回复带有 **Agreement 标志** 的 BPDU
     - 上游收到 Agreement 后，立即将端口切换到转发状态
     整个过程仅需 **1-2 个 Hello 时间间隔（2-4 秒）**，远快于 STP 的 30-50 秒。

  **3. 边缘端口（Edge Port）**：
     连接终端设备（PC、服务器等）的端口可配置为边缘端口。边缘端口**跳过监听和学习状态，直接进入转发状态**，无需等待任何定时器。这消除了终端设备接入网络时的延迟。安全机制：如果边缘端口意外收到 BPDU，它会自动失去边缘端口身份，转为普通端口参与 STP 计算，防止用户私接交换机导致环路。

  > Switch(config)# interface fa0/1
  > Switch(config-if)# spanning-tree portfast

  ## MSTP：多生成树协议

  STP 和 RSTP 在整个网络中只维护**一棵生成树**，所有 VLAN 共享相同的拓扑。这意味着冗余链路始终被阻塞，无法实现负载均衡。

  **MSTP（Multiple Spanning Tree Protocol，IEEE 802.1s）** 解决了这个问题：它允许将多个 VLAN 映射到不同的**生成树实例**，每个实例拥有独立的根桥和端口状态。

  ### 核心概念

  - **实例（Instance）**：一棵独立的生成树。例如，实例 1 承载 VLAN 1-10，实例 2 承载 VLAN 11-20。两个实例可以有不同的根桥，从而在不同链路上实现负载均衡。
  - **CIST（公共和内部生成树）**：所有 MSTP 域和传统 STP/RSTP 区域之间的公共生成树，保证与旧设备的兼容性。
  - **MST 域**：一组配置相同的交换机组成一个域。域内交换机必须配置相同的**域名、修订号和 VLAN 到实例的映射表**。域内的生成树计算独立于域外。

  ### 协议兼容性

  MSTP 向下兼容 RSTP 和 STP。当 MSTP 交换机连接到运行 STP 或 RSTP 的设备时，会自动降级为兼容模式。在实际网络中，通常部署 MSTP 以获得最佳的灵活性和兼容性。

  三种协议的对比：

  - **STP（802.1D）**：收敛时间 30-50 秒，单实例，仅用于消除环路
  - **RSTP（802.1w）**：收敛时间 1-2 秒，单实例，快速收敛
  - **MSTP（802.1s）**：收敛时间 1-2 秒，多实例，支持负载均衡

  ## 链路聚合

  除了 STP，还有一种技术可以**既利用冗余链路，又避免环路**——**链路聚合（Link Aggregation）**。

  把多条物理链路捆绑成一条逻辑链路：

  > 交换机A ==== 4条1G线 ==== 交换机B
  >          逻辑上 = 1条4G线

  **好处：**
  - 带宽翻倍（4 条 1G = 4G）
  - 冗余（一条断了，其他三条继续工作）
  - 无环路（逻辑上只有一条链路）

  **配置（LACP）：**
  > Switch(config)# interface range fa0/1-4
  > Switch(config-if-range)# channel-group 1 mode active

  ## 本课小结

  - **环路**会导致广播风暴，使网络瘫痪
  - **STP** 通过禁用冗余链路消除环路
  - **根桥选举**是 STP 的第一步，Bridge ID 最小的成为根桥
  - **RSTP** 大幅加快了收敛速度，引入替代端口和提议/同意机制
  - **MSTP** 支持多实例生成树，可在不同 VLAN 间实现负载均衡
  - **链路聚合**可以同时利用多条链路，既增加带宽又提供冗余`,
  quizzes: [
          {
            id: 'quiz-06-04-01',
            type: 'choice',
            question: 'STP 的主要作用是什么？',
            options: ['提高网速', '消除环路', '划分 VLAN', '路由选择'],
            answer: 'B',
            explanation: 'STP（生成树协议）的主要作用是通过禁用冗余链路来消除以太网环路。'
          },
          {
            id: 'quiz-06-04-02',
            type: 'choice',
            question: 'STP 中根桥是根据什么选举的？',
            options: ['MAC 地址最小', 'Bridge ID 最小', '端口数最多', '带宽最高'],
            answer: 'B',
            explanation: 'STP 中 Bridge ID（优先级 + MAC 地址）最小的交换机成为根桥。'
          },
          {
            id: 'quiz-06-04-03',
            type: 'choice',
            question: 'RSTP 相比 STP 的主要改进是什么？',
            options: ['支持更多 VLAN', '收敛速度更快', '支持更高带宽', '更安全'],
            answer: 'B',
            explanation: 'RSTP 的主要改进是将收敛时间从 30-50 秒缩短到几秒钟。'
          },
          {
            id: 'quiz-06-04-04',
            type: 'fill',
            question: 'STP 端口从阻塞状态到转发状态需要______秒。',
            answer: ['30-50'],
            explanation: 'STP 端口需要经过阻塞（20秒）→ 监听（15秒）→ 学习（15秒）→ 转发，总共约 30-50 秒。'
          }
        ],
  references: [
          'IEEE 802.1D - Spanning Tree Protocol',
          'IEEE 802.1w - Rapid Spanning Tree Protocol'
        ]
};
