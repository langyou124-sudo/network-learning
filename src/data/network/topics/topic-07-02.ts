export const topic_07_02 = {
  title: "防火墙与入侵检测",
  description: "防火墙类型、IDS/IPS 与安全部署",
  content: `# 防火墙与入侵检测

  ## 开篇：网络的"门卫"和"监控"

  想象你住在一个小区里：
  - **防火墙**就像小区的门卫，检查每一个进出的人，不符合规定的不让进
  - **IDS** 就像小区的监控摄像头，发现可疑行为就报警
  - **IPS** 不仅报警，还能直接把可疑的人拦住

  这一课，我们来认识这些网络安全设备。

  <Glossary terms="%5B%7B%22term%22%3A%22%E9%98%B2%E7%81%AB%E5%A2%99%22%2C%22english%22%3A%22Firewall%22%2C%22definition%22%3A%22%E7%BD%91%E7%BB%9C%E5%AE%89%E5%85%A8%E8%AE%BE%E5%A4%87%EF%BC%8C%E6%A0%B9%E6%8D%AE%E5%AE%89%E5%85%A8%E8%A7%84%E5%88%99%E6%8E%A7%E5%88%B6%E7%BD%91%E7%BB%9C%E6%B5%81%E9%87%8F%E8%BF%9B%E5%87%BA%22%7D%2C%7B%22term%22%3A%22IDS%22%2C%22english%22%3A%22Intrusion%20Detection%20System%22%2C%22definition%22%3A%22%E5%85%A5%E4%BE%B5%E6%A3%80%E6%B5%8B%E7%B3%BB%E7%BB%9F%EF%BC%8C%E7%9B%91%E6%8E%A7%E7%BD%91%E7%BB%9C%E6%B5%81%E9%87%8F%E5%B9%B6%E5%8F%91%E7%8E%B0%E5%BC%82%E5%B8%B8%E8%A1%8C%E4%B8%BA%22%7D%2C%7B%22term%22%3A%22IPS%22%2C%22english%22%3A%22Intrusion%20Prevention%20System%22%2C%22definition%22%3A%22%E5%85%A5%E4%BE%B5%E9%98%B2%E5%BE%A1%E7%B3%BB%E7%BB%9F%EF%BC%8C%E6%A3%80%E6%B5%8B%E5%B9%B6%E9%98%BB%E6%AD%A2%E6%81%B6%E6%84%8F%E6%B5%81%E9%87%8F%22%7D%2C%7B%22term%22%3A%22ACL%22%2C%22english%22%3A%22Access%20Control%20List%22%2C%22definition%22%3A%22%E8%AE%BF%E9%97%AE%E6%8E%A7%E5%88%B6%E5%88%97%E8%A1%A8%EF%BC%8C%E5%AE%9A%E4%B9%89%E5%85%81%E8%AE%B8%E6%88%96%E6%8B%92%E7%BB%9D%E7%9A%84%E6%B5%81%E9%87%8F%E8%A7%84%E5%88%99%22%7D%5D" />

  ## 防火墙类型

  防火墙按照工作层次和技术原理，可以分为四大类型。理解它们的区别，是掌握网络安全的基础。

  ### 包过滤防火墙（Packet Filtering）

  **工作原理**：工作在网络层和传输层，逐个检查数据包的头部字段——源 IP、目标 IP、源端口、目标端口、协议类型，然后根据预设的 ACL 规则表决定"放行"或"丢弃"。

  **类比**：门卫只看你的工牌号，不看你是谁，也不管你带了什么。

  **处理流程**：收到数据包 → 提取五元组 → 从上到下匹配 ACL 规则 → 匹配到 permit 放行，deny 丢弃 → 无匹配则隐式拒绝

  **优点**：速度快、实现简单、对网络性能影响极小

  **缺点**：
  - 不检查数据内容，可能放过携带恶意载荷的数据包
  - 无法跟踪连接状态，容易被 IP 分片攻击绕过
  - 每个数据包独立判断，缺乏上下文语义

  ### 状态检测防火墙（Stateful Inspection）

  **工作原理**：在包过滤的基础上增加了**连接状态表**（State Table）。不仅检查单个数据包的头部，还会记录每条连接的状态（NEW、ESTABLISHED、RELATED、INVALID），只有属于已建立连接的合法回程流量才会被放行。

  **类比**：门卫不仅看工牌，还记录你什么时候进来、什么时候出去，只允许"有进有出"的正常出入。

  **状态跟踪的关键机制**：
  - **TCP 状态机跟踪**：监控 SYN、SYN-ACK、ACK 三次握手过程，只有完整握手的连接才标记为 ESTABLISHED
  - **UDP 虚拟连接**：UDP 无连接，防火墙通过"请求-响应"配对创建虚拟状态条目，超时后自动清除
  - **ALG（应用层网关）**：对于 FTP、SIP 等动态端口协议，ALG 模块会解析控制通道，自动为数据通道创建临时放行规则

  **优点**：
  - 安全性比包过滤高得多，能识别合法的返回流量
  - 自动阻止未经请求的入站连接
  - 对用户透明，无需配置客户端

  **缺点**：
  - 需要维护状态表，高并发时消耗大量内存
  - 对 UDP 和 ICMP 的状态跟踪不如 TCP 精确
  - 状态表可能遭受 SYN Flood 攻击导致资源耗尽

  ### 应用层防火墙（代理防火墙 / Proxy Firewall）

  **工作原理**：作为客户端和服务器之间的"中间人"，完全终止客户端连接，自己与服务器建立新连接，深度解析应用层协议（HTTP、FTP、SMTP、DNS 等）的每一个字段和载荷内容。

  **类比**：门卫打开你的包，检查里面每一样东西，确认安全后再帮你重新打包送进去。

  **两种代理模式**：
  - **电路级网关**：工作在会话层，只验证 TCP 握手，不检查数据内容
  - **应用级网关**：工作在应用层，完全解析 HTTP 请求方法、URL、Header、Body 等

  **优点**：
  - 安全性最高，能检测 SQL 注入、XSS、缓冲区溢出等应用层攻击
  - 可以做内容过滤（URL 黑白名单、文件类型过滤、关键词过滤）
  - 隐藏内部网络拓扑，外部只能看到代理地址

  **缺点**：
  - 性能开销最大，每个连接都需要完整的协议解析
  - 需要为不同协议分别开发代理模块
  - 可能不支持新兴协议或加密流量

  ### 下一代防火墙（NGFW）

  **工作原理**：在传统防火墙基础上融合多种安全功能，核心能力包括**深度包检测（DPI）**、**应用识别**、**用户身份识别**和**威胁情报集成**。

  **NGFW 的五大核心能力**：

  | 能力 | 说明 |
  |------|------|
  | 深度包检测（DPI） | 不仅看包头，还解析到应用层载荷，识别具体的协议和应用行为 |
  | 应用识别 | 区分同一端口上的不同应用（如 80 端口上的 HTTP 视频 vs HTTP 网页） |
  | URL 过滤 | 维护数十亿 URL 分类库，阻止访问恶意或不当网站 |
  | 威胁情报集成 | 接入全球威胁情报 feed，实时更新恶意 IP、域名拦截列表 |

  **与传统防火墙的关键区别**：传统防火墙基于"IP + 端口"做决策，NGFW 基于"应用 + 用户 + 内容"做决策。例如，NGFW 可以实现"允许市场部员工使用微信，但禁止通过微信传输文件"这样精细的策略。

  ### 防火墙类型对比总结

  | 特性 | 包过滤 | 状态检测 | 应用层代理 | NGFW |
  |------|--------|----------|-----------|------|
  | 工作层次 | L3-L4 | L3-L4 | L7 | L3-L7 |
  | 检查深度 | 仅包头 | 包头+状态 | 完整载荷 | 完整载荷+应用识别 |
  | 连接状态跟踪 | 无 | 有 | 有 | 有 |
  | 性能影响 | 最小 | 较小 | 大 | 中等（硬件加速） |
  | 安全性 | 低 | 中 | 高 | 最高 |
  | 部署难度 | 简单 | 简单 | 复杂 | 中等 |

  <Diagram type="firewall-types" />

  ## ACL 访问控制列表

  ACL（Access Control List）是防火墙和路由器上最常用的流量过滤机制，通过一系列有序规则决定数据包的命运。

  ### ACL 分类

  **标准 ACL（Standard ACL）**：
  - 编号范围：1-99
  - 仅根据**源 IP 地址**做匹配
  - 应尽量放在**靠近目标**的位置，避免误拦

  **扩展 ACL（Extended ACL）**：
  - 编号范围：100-199
  - 可根据**源 IP、目标 IP、协议、端口号、标志位**等做精细匹配
  - 应尽量放在**靠近源**的位置，尽早拦截不需要的流量

  ### 通配符掩码（Wildcard Mask）

  通配符掩码与子网掩码相反——0 表示"必须精确匹配"，1 表示"忽略此位"。例如 192.168.1.0 **0.0.0.255** 匹配整个 /24 网段，**any** 匹配所有地址。

  ### ACL 规则结构

  每条规则包含：
  - **源地址**：从哪来的
  - **目标地址**：到哪去
  - **端口号**：什么服务
  - **动作**：允许（permit）或拒绝（deny）

  ### ACL 配置示例（Cisco）

  **扩展 ACL 示例**：允许内网访问 HTTP 和 HTTPS，拒绝其他
  - access-list 100 permit tcp 192.168.1.0 0.0.0.255 any eq 80
  - access-list 100 permit tcp 192.168.1.0 0.0.0.255 any eq 443
  - access-list 100 deny ip any any
  - interface fa0/0 → ip access-group 100 in

  ### ACL 匹配规则

  - **从上到下**逐条匹配
  - **一旦匹配就停止**，不再继续
  - **隐式拒绝**：如果没有匹配的规则，默认拒绝

  > 重要：ACL 的规则顺序很重要！把最常匹配的规则放在前面。把范围更具体的规则放在前面，范围更宽的规则放在后面。

  ## IDS 入侵检测系统

  IDS（Intrusion Detection System）就像网络的"监控摄像头"，被动监控流量，发现异常就报警。

  ### 三种检测方法

  **基于签名的检测（Signature-based）**
  - 维护已知攻击特征库，将流量与特征库逐条对比
  - **优点**：准确率高，误报少；**缺点**：只能检测已知攻击

  **基于异常的检测（Anomaly-based）**
  - 通过统计分析建立正常行为基线，检测偏离基线的异常行为
  - **优点**：能检测零日攻击；**缺点**：误报率高

  **基于启发式的检测（Heuristic-based）**
  - 结合签名和异常检测，使用规则引擎评估流量的"可疑分数"
  - **优点**：平衡准确率和未知攻击检测；**缺点**：规则设计复杂

  ### Snort——最著名的开源 IDS

  Snort 是由 Sourcefire（现属 Cisco）开发的开源入侵检测系统，支持嗅探、数据包记录和 NIDS 三种工作模式。

  **Snort 架构**：数据包解码器 → 预处理器（分片重组、协议解码）→ 检测引擎（规则匹配）→ 输出模块（日志、告警）

  **告警级别（从高到低）**：Emergency（严重攻击）→ Alert（需立即关注）→ Warning（异常端口扫描）→ Notice（非标准端口通信）

  **Snort 规则语法**：规则由**规则头**（动作+协议+地址+端口+方向）和**规则选项**（sid、msg、content、classtype 等）组成。

  ### IDS 的局限

  - 只能检测和报警，不能阻止攻击
  - 高速网络中可能丢包
  - 需要人工分析告警，加密流量下无法解析载荷内容

  ## IPS 入侵防御系统

  IPS（Intrusion Prevention System）是 IDS 的升级版——**不仅能检测，还能阻止**。

  ### 工作方式

  IPS 以**串联**方式部署在网络路径中，所有流量都必须经过它：

  外部网络 → [IPS] → 内部网络

  当检测到恶意流量时，IPS 可以执行以下动作：
  1. **丢弃恶意数据包**：直接丢弃匹配攻击特征的数据包
  2. **重置连接**：向攻击方发送 TCP RST 包，终止连接
  3. **阻断攻击源 IP**：在防火墙上动态添加黑名单规则

  ### IDS vs IPS 对比

  | 特性 | IDS | IPS |
  |------|-----|-----|
  | 部署方式 | 旁路（并联） | 串联 |
  | 功能 | 检测+报警 | 检测+阻止 |
  | 对性能影响 | 小（只分析镜像流量） | 大（所有流量经过） |
  | 误报影响 | 只是报警，不影响业务 | 可能阻断正常流量 |
  | 响应速度 | 需人工介入 | 自动阻断 |

  ## 防火墙部署策略

  ### 单防火墙

  最简单的部署，内外网之间一个防火墙。

  外部网络 ←→ [防火墙] ←→ 内部网络

  ### DMZ 非军事区

  在内外网之间增加一个"缓冲区"：

  外部网络 ←→ [外部防火墙] ←→ DMZ ←→ [内部防火墙] ←→ 内部网络

  **DMZ 放置对外服务**：Web 服务器、邮件服务器

  **好处**：
  - 即使 DMZ 被攻破，内部网络仍然安全
  - 对外服务和内部网络隔离

  ## 本课小结

  - **防火墙**分为包过滤、状态检测、应用层代理、NGFW 四种类型
  - **状态检测防火墙**通过连接状态表跟踪会话，是当前主流技术
  - **NGFW** 融合 DPI、应用识别和威胁情报，实现精细策略控制
  - **ACL** 分标准 ACL 和扩展 ACL，规则从上到下匹配，隐式拒绝
  - **IDS** 被动监控，通过签名、异常、启发式三种方法检测攻击
  - **IPS** 串联部署，能主动阻止攻击，但误报可能影响正常业务
  - **Snort** 是最著名的开源 IDS，支持丰富的规则语法
  - **DMZ** 在内外网之间建立缓冲区，放置对外服务`,
  quizzes: [
          {
            id: 'quiz-07-02-01',
            type: 'choice',
            question: '哪种防火墙能检查数据内容？',
            options: ['包过滤防火墙', '状态检测防火墙', '应用层防火墙', '以上都不能'],
            answer: 'C',
            explanation: '应用层防火墙（代理防火墙）能完全解析应用层协议，检查数据内容。'
          },
          {
            id: 'quiz-07-02-02',
            type: 'choice',
            question: 'IDS 和 IPS 的主要区别是什么？',
            options: ['IDS 更快', 'IPS 能阻止攻击', 'IDS 更安全', 'IPS 只能检测'],
            answer: 'B',
            explanation: 'IPS 是 IDS 的升级版，不仅能检测还能主动阻止恶意流量。'
          },
          {
            id: 'quiz-07-02-03',
            type: 'choice',
            question: 'DMZ 中通常放置什么类型的服务器？',
            options: ['数据库服务器', '对外服务（Web、邮件）', '域控制器', '备份服务器'],
            answer: 'B',
            explanation: 'DMZ 中放置对外提供服务的服务器，如 Web 服务器、邮件服务器，与内部网络隔离。'
          },
          {
            id: 'quiz-07-02-04',
            type: 'fill',
            question: 'ACL 规则从______到______逐条匹配，一旦匹配就______。',
            answer: ['上', '下', '停止'],
            explanation: 'ACL 规则从上到下逐条匹配，一旦匹配成功就停止，不再继续检查后续规则。'
          }
        ],
  references: [
          'RFC 2979 - Behavioral Requirements for Firewalls'
        ]
};
